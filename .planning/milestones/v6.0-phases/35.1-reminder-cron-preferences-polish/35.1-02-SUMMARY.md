---
phase: 35.1-reminder-cron-preferences-polish
plan: 02
subsystem: api
tags: [reminders, crud, fire-at, zod, supabase, calendar-events]

requires:
  - phase: 35.1-01
    provides: "Database schema, types, DB classes, and validation schemas for reminders"
provides:
  - "Reminder CRUD API routes (GET, POST, PATCH, DELETE)"
  - "Reminder defaults API routes (GET, PUT)"
  - "computeFireAt utility for fire_at timestamp computation"
  - "Reschedule fire_at recomputation in calendar event PATCH"
affects: [35.1-03, 35.1-04, 35.1-05]

tech-stack:
  added: []
  patterns: ["computeFireAt pure function for reminder scheduling", "Zod .and() for extending schemas with additional fields"]

key-files:
  created:
    - lib/reminders/fire-at.ts
    - app/api/reminders/route.ts
    - app/api/reminders/[id]/route.ts
    - app/api/reminder-defaults/route.ts
    - tests/lib/reminders/fire-at.test.ts
    - tests/app/api/reminders/route.test.ts
    - tests/app/api/reminder-defaults/route.test.ts
  modified:
    - lib/db/reminders.ts
    - app/api/calendar-events/[id]/route.ts

key-decisions:
  - "Extended reminderCreateSchema with .and() for event_start_time rather than modifying shared schema"
  - "Reminder fire_at recomputation on reschedule catches errors silently to not fail the event update"

patterns-established:
  - "computeFireAt as pure function: takes reminder input + event start ISO, returns fire_at ISO"
  - "Zod .and() for route-specific schema extensions without modifying shared validation schemas"

requirements-completed: [REMN-01, REMN-02, REMN-03, REMN-04, REMN-05, REMN-06, REMN-09]

duration: 8min
completed: 2026-04-03
---

# Phase 35.1 Plan 02: Reminder CRUD API & fire_at Computation Summary

**Reminder CRUD API routes with fire_at pre-computation, defaults management, and automatic fire_at recomputation on calendar event reschedule**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-03T17:53:11Z
- **Completed:** 2026-04-03T18:01:10Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- computeFireAt utility handles both relative (subtract minutes) and absolute (direct timestamp) reminders
- Full reminder CRUD: POST creates with fire_at computation, GET lists by source, PATCH/DELETE for individual reminders
- Reminder defaults GET/PUT for per-source-type user preferences
- Calendar event PATCH recomputes fire_at for all pending relative reminders when start_date/start_time changes (REMN-09)
- 21 unit tests covering all routes, auth, validation, and fire_at computation

## Task Commits

Each task was committed atomically:

1. **Task 1: fire_at computation utility** - `395542e` (feat, TDD)
2. **Task 2: Reminder CRUD, defaults API, reschedule wiring** - `934ba95` (feat, TDD)

## Files Created/Modified
- `lib/reminders/fire-at.ts` - Pure function computing fire_at from reminder type + event start
- `app/api/reminders/route.ts` - GET (list by source) and POST (create with fire_at) endpoints
- `app/api/reminders/[id]/route.ts` - PATCH (update) and DELETE (remove) endpoints
- `app/api/reminder-defaults/route.ts` - GET (list defaults) and PUT (upsert default) endpoints
- `lib/db/reminders.ts` - Added updateReminder and deleteReminder methods
- `app/api/calendar-events/[id]/route.ts` - Added fire_at recomputation on event reschedule
- `tests/lib/reminders/fire-at.test.ts` - 6 tests for computeFireAt
- `tests/app/api/reminders/route.test.ts` - 10 tests for reminder CRUD routes
- `tests/app/api/reminder-defaults/route.test.ts` - 5 tests for defaults routes

## Decisions Made
- Extended reminderCreateSchema with Zod `.and()` to add event_start_time field rather than modifying the shared schema used elsewhere
- Wrapped reminder fire_at recomputation in try/catch so reschedule errors don't fail the event update itself

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- Test source_id used non-UUID string "event-1" but Zod schema requires valid UUID format - fixed to use proper UUID in tests
- Test expected `relative_minutes: undefined` for absolute reminders but route normalizes to `null` via `?? null` - fixed test expectation

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Reminder API layer complete, ready for cron job (plan 03) to poll pending reminders and dispatch notifications
- computeFireAt utility available for any future code that needs to compute/recompute fire times
- updateReminder method available for cron job to mark reminders as sent/failed

## Self-Check: PASSED

All 7 created files verified present. Both task commits (395542e, 934ba95) verified in git log.

---
*Phase: 35.1-reminder-cron-preferences-polish*
*Completed: 2026-04-03*
