---
phase: 21-budgets-spending-analytics
plan: 04
type: execute
wave: 4
depends_on: [21-03]
files_modified:
  - tests/components/money/budget-ring.test.tsx
  - tests/components/money/budget-overview.test.tsx
  - tests/validations/budget.test.ts
autonomous: false
requirements: [BUDG-01, BUDG-02, BUDG-03, BUDG-04, BUDG-05, BUDG-06]

must_haves:
  truths:
    - "Budget ring renders correct colors at different thresholds"
    - "Budget overview handles empty, loading, and data states"
    - "Budget validation schemas catch invalid input"
    - "All Phase 21 features verified end-to-end by human"
  artifacts:
    - path: "tests/components/money/budget-ring.test.tsx"
      provides: "BudgetRing component tests"
    - path: "tests/components/money/budget-overview.test.tsx"
      provides: "BudgetOverview component tests"
    - path: "tests/validations/budget.test.ts"
      provides: "Budget Zod schema validation tests"
  key_links:
    - from: "tests/components/money/budget-ring.test.tsx"
      to: "components/money/budget-ring.tsx"
      via: "component import"
      pattern: "import.*BudgetRing"
    - from: "tests/validations/budget.test.ts"
      to: "lib/validations/budget.ts"
      via: "schema import"
      pattern: "import.*budgetCreateSchema"
---

<objective>
Write automated tests for budget components and validation schemas, then verify the complete budget feature end-to-end.

Purpose: Ensure quality and correctness before moving to the next phase. Tests catch regressions, human verification catches UX issues.
Output: Test files for components and validation, human-verified budget flow.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-budgets-spending-analytics/21-RESEARCH.md
@.planning/phases/21-budgets-spending-analytics/21-03-SUMMARY.md
@components/money/budget-ring.tsx
@components/money/budget-overview.tsx
@lib/validations/budget.ts
@tests/components/money/money-page-shell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write component and validation tests</name>
  <files>
    tests/components/money/budget-ring.test.tsx
    tests/components/money/budget-overview.test.tsx
    tests/validations/budget.test.ts
  </files>
  <action>
**BudgetRing tests** (`tests/components/money/budget-ring.test.tsx`):

Test the SVG rendering and color threshold logic:

1. Renders SVG element with correct size
2. Shows sage color for 0-74% (e.g., 50%)
3. Shows amber color for 75-89% (e.g., 80%)
4. Shows caution color for 90%+ (e.g., 95%)
5. Shows caution color for over 100% (e.g., 120%) — ring fully filled
6. Handles 0% correctly (no progress arc visible)
7. Handles exactly 100% (full ring)
8. Accepts custom color override
9. Accepts custom size and strokeWidth
10. Accessibility: has appropriate role or aria-label

Mock pattern: Direct render with `@testing-library/react`, no SWR or API mocks needed.

**BudgetOverview tests** (`tests/components/money/budget-overview.test.tsx`):

Follow the sub-component mocking pattern from Phase 20 (mock child components to isolate BudgetOverview):

```typescript
vi.mock("@/components/money/budget-ring", () => ({ BudgetRing: () => <div data-testid="budget-ring" /> }));
vi.mock("@/components/money/spending-donut", () => ({ SpendingDonut: () => <div data-testid="spending-donut" /> }));
vi.mock("@/components/money/spending-trend-bar", () => ({ SpendingTrendBar: () => <div data-testid="spending-trend-bar" /> }));
vi.mock("@/components/money/budget-form", () => ({ BudgetForm: () => <div data-testid="budget-form" /> }));
```

Mock SWR hooks:
```typescript
vi.mock("@/lib/hooks/use-budgets", () => ({ useBudget: vi.fn() }));
vi.mock("@/lib/hooks/use-spending-analytics", () => ({
  useSpendingAnalytics: vi.fn(),
  useSpendingTrends: vi.fn(),
}));
```

Test cases:
1. Shows loading skeleton when data is loading
2. Shows "Create Budget" CTA when no budget exists for month
3. Shows budget summary when budget exists (total, spent, remaining)
4. Shows category cards with BudgetRing components
5. Shows SpendingDonut and SpendingTrendBar chart components
6. Month navigation: clicking back arrow changes to previous month
7. Month navigation: forward arrow is disabled on current month
8. Shows over-budget indicator when spending exceeds allocation
9. Rollover display: shows "$200 + $35 rollover" format when rollover_cents present
10. Accessibility: no violations (use vitest-axe)

**Budget validation tests** (`tests/validations/budget.test.ts`):

Test all Zod schemas from `lib/validations/budget.ts`:

budgetCreateSchema:
1. Valid: complete budget with categories summing less than total (buffer allowed)
2. Valid: categories summing exactly to total
3. Invalid: missing month
4. Invalid: month not first day (e.g., "2026-02-15")
5. Invalid: total is zero or negative
6. Invalid: categories empty array
7. Invalid: category amount is negative
8. Invalid: categories sum exceeds total (envelope violation)
9. Invalid: category_id not UUID format
10. Valid: rollover_enabled defaults to false when omitted

budgetUpdateSchema:
11. Valid: partial update with just total
12. Valid: partial update with just rollover_enabled
13. Invalid: empty update (no fields)
14. Valid: update with new categories

rolloverConfirmSchema:
15. Valid: correct from_budget_id UUID
16. Invalid: missing from_budget_id

spendingQuerySchema:
17. Valid: "2026-02" format
18. Invalid: "2026-2" (missing leading zero)
19. Invalid: "2026-02-01" (day included)

trendQuerySchema:
20. Valid: months=12
21. Valid: omitted (defaults to 12)
22. Invalid: months=0
23. Invalid: months=25
  </action>
  <verify>
Run `pnpm test:run` — all tests pass including new ones. Run `pnpm lint` — no errors. Check test count increased. Verify no pre-existing test failures introduced.
  </verify>
  <done>
BudgetRing has 10 tests covering rendering and color thresholds. BudgetOverview has 10 tests covering states, navigation, and accessibility. Budget validation has 23 tests covering all schemas. All pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete budget and spending analytics flow</name>
  <files></files>
  <action>
Present the following verification checklist to the user. This is a visual/functional check of the complete Phase 21 feature set.

What was built:
- Budget page at /money/budgets with month navigation (left/right arrows, no future months)
- Envelope-style budget creation form (total amount + per-category allocations)
- Circular progress rings per category with 3-tier color thresholds (sage/amber/caution)
- Donut chart for spending breakdown by category (Recharts)
- Bar chart for 12-month spending trends (Recharts)
- Category drill-down sheet on chart click (slide-in panel)
- Budget edit and delete functionality
- Rollover confirmation prompt with per-category breakdown
- All i18n strings in en, zh, zh-TW

How to verify:
1. Navigate to `/money/budgets` — should show the budgets page with month navigation
2. If no budget exists for current month, click "Create Budget":
   - Set a total amount (e.g., $2,000)
   - Allocate amounts to categories (e.g., Groceries $500, Dining $300, etc.)
   - Verify "Allocated" and "Unallocated" totals update live as you type
   - Verify you cannot allocate more than the total
   - Enable rollover toggle
   - Submit — budget should appear with progress rings per category
3. Verify the budget overview:
   - Total budget, spent, remaining shown at top
   - Circular rings per category with correct fill percentages
   - If spending > 90% of allocation, ring should be amber/caution color
   - If over budget, ring is full with "$X over" indicator
4. Verify charts:
   - Donut chart shows spending breakdown by category
   - Hover on donut shows tooltip with amount and percentage
   - Click on donut slice opens transaction drill-down sheet on the right
   - Bar chart shows budget vs spent for recent months
5. Verify month navigation:
   - Left arrow goes to previous month
   - Right arrow disabled on current month (no future months)
   - Data updates when switching months
6. Edit the budget — click Edit, change amounts, verify update saves
7. If rollover was enabled, navigate to previous month then back — verify rollover prompt appears
8. Delete the budget — verify confirmation dialog and successful deletion
9. Verify Calm Finance styling (sage/amber colors, no aggressive red/green)
10. Verify on mobile viewport — components stack correctly

Expected: All budget features functional with Calm Finance aesthetic. Charts interactive. No console errors.
  </action>
  <verify>User confirms all 10 verification items pass. Type "approved" to mark Phase 21 complete, or describe issues to fix.</verify>
  <done>All Phase 21 budget and spending analytics features verified by human — creation, progress rings, charts, drill-down, navigation, rollover, and Calm Finance styling all working.</done>
</task>

</tasks>

<verification>
1. `pnpm test:run` — all tests pass (existing + new)
2. `pnpm lint` — no errors
3. Human verifies budget creation, progress rings, charts, drill-down, and rollover flow
4. No console errors during testing
5. Mobile responsive layout works
</verification>

<success_criteria>
- 40+ new tests pass covering BudgetRing, BudgetOverview, and all budget validation schemas
- Human verified: budget creation flow works end-to-end
- Human verified: charts render and are interactive (donut click → drill-down)
- Human verified: month navigation works, no future months accessible
- Human verified: Calm Finance aesthetic maintained throughout
</success_criteria>

<output>
After completion, create `.planning/phases/21-budgets-spending-analytics/21-04-SUMMARY.md`
</output>
