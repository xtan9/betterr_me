---
phase: 21-budgets-spending-analytics
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/budgets.ts
  - app/api/money/analytics/spending/route.ts
  - components/money/budget-overview.tsx
  - components/money/rollover-prompt.tsx
autonomous: true
gap_closure: true
requirements:
  - BUDG-01
  - BUDG-02
  - BUDG-03
  - BUDG-04
  - BUDG-05
  - BUDG-06

must_haves:
  truths:
    - "Spending trend bar chart shows actual budget totals per month (not $0)"
    - "Rollover prompt calls the API with the previous month budget ID as the source"
    - "Rollover confirmation successfully carries amounts forward to the current month budget"
  artifacts:
    - path: "lib/db/budgets.ts"
      provides: "getBudgetTotalsByMonth method returning total_cents per month for a date range"
      contains: "getBudgetTotalsByMonth"
    - path: "app/api/money/analytics/spending/route.ts"
      provides: "Budget totals included in trends API response"
      contains: "budget_total_cents"
    - path: "components/money/budget-overview.tsx"
      provides: "trendData populated with real budget totals from API"
    - path: "components/money/rollover-prompt.tsx"
      provides: "Rollover POST uses previousBudget.id in URL path"
  key_links:
    - from: "components/money/budget-overview.tsx"
      to: "app/api/money/analytics/spending/route.ts"
      via: "useSpendingTrends hook returning budget_total_cents per month"
      pattern: "budget_total_cents"
    - from: "components/money/rollover-prompt.tsx"
      to: "app/api/money/budgets/[id]/rollover/route.ts"
      via: "fetch POST with previousBudget.id as URL parameter"
      pattern: "previousBudget\\.id"
---

<objective>
Close 2 verification gaps from Phase 21 that prevent BUDG-04 and BUDG-06 from being fully satisfied.

Purpose: The spending trend bar chart shows "Budget vs Spent" in its legend but the Budget bars are all $0 because historical budget totals are never fetched. The rollover prompt calls the API with the wrong budget ID (current instead of previous), making rollover confirmation fail.

Output: Both gaps resolved — trend chart displays real budget data per month, and rollover works end-to-end.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-budgets-spending-analytics/21-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich spending trends API with budget totals per month</name>
  <files>lib/db/budgets.ts, app/api/money/analytics/spending/route.ts, lib/hooks/use-spending-analytics.ts, components/money/budget-overview.tsx</files>
  <action>
1. In `lib/db/budgets.ts`, add a new method `getBudgetTotalsByMonth(householdId: string, months: string[]): Promise<Map<string, number>>` that queries the `budgets` table for rows matching the given months and household, returning a Map of month string (YYYY-MM-01 format) to `total_cents`. Use a single Supabase query with `.in("month", months)` for efficiency.

2. In `app/api/money/analytics/spending/route.ts`, in the trends mode (type=trends) block:
   - After computing the `trends` array from `monthMap`, extract the list of month strings from the trends.
   - Call `budgetsDB.getBudgetTotalsByMonth(householdId, monthStrings)` to get budget totals.
   - Add `budget_total_cents` field to each trend object in the response by looking up the month in the budget totals map. Default to 0 if no budget exists for that month.

3. In `lib/hooks/use-spending-analytics.ts`, update the `MonthlyTrend` interface to include `budget_total_cents: number`.

4. In `components/money/budget-overview.tsx`, update the `trendData` useMemo (around line 89-95):
   - Replace `budget: 0` with `budget: trend.budget_total_cents || 0` so the bar chart receives real budget amounts per month.
   - Remove the comment `// Will be enriched when we have budget data per month`.
  </action>
  <verify>
Run `pnpm test:run` to confirm existing tests pass. Visually inspect that the `MonthlyTrend` interface includes `budget_total_cents`. Grep for `budget: 0` in budget-overview.tsx to confirm it is gone. Grep for `getBudgetTotalsByMonth` in budgets.ts to confirm method exists.
  </verify>
  <done>
The spending trends API returns `budget_total_cents` per month. The SpendingTrendBar chart displays actual budget amounts alongside spending amounts — no more invisible $0 Budget bars. BUDG-04 fully satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix rollover prompt wiring and clean up unused schema</name>
  <files>components/money/rollover-prompt.tsx</files>
  <action>
1. In `components/money/rollover-prompt.tsx`, fix the `handleConfirm` function (around lines 42-76):
   - The current code fetches the current month's budget to get `currentBudget.id`, then calls `POST /api/money/budgets/${currentBudget.id}/rollover`. This is backwards because the rollover route treats `[id]` as the **source** (previous month) budget ID.
   - Simplify the handler: the component already receives `previousBudget` as a prop. Use `previousBudget.id` directly in the rollover URL.
   - Remove the intermediate fetch to `/api/money/budgets?month=${currentMonth}` — it is unnecessary since we already have `previousBudget.id`.
   - The corrected call should be: `fetch(\`/api/money/budgets/${previousBudget.id}/rollover\`, { method: "POST" })`.
   - The request body (`{ from_budget_id: previousBudget.id }`) is also unnecessary since the route ignores it — the [id] param is the source budget. Remove the body and `Content-Type` header from the fetch call for cleanliness.

2. Note: The `rolloverConfirmSchema` in `lib/validations/budget.ts` is defined but unused by the rollover route. Leave it in place — it is harmless and the type `RolloverConfirmInput` is exported. Removing it could be a separate cleanup if desired but is not blocking.
  </action>
  <verify>
Run `pnpm test:run` to confirm existing tests pass (especially `tests/components/money/budget-overview.test.tsx` which includes rollover-related tests). Grep for `currentBudget.id` in rollover-prompt.tsx to confirm it is gone. Grep for `previousBudget.id` in rollover-prompt.tsx to confirm it is present in the fetch URL.
  </verify>
  <done>
RolloverPrompt calls `POST /api/money/budgets/${previousBudget.id}/rollover`, correctly passing the previous month budget as the source. The rollover route receives the correct source budget ID, computes rollover from that month, and writes to the next month's budget categories. BUDG-06 fully satisfied.
  </done>
</task>

</tasks>

<verification>
1. `pnpm test:run` passes (all existing tests including budget-ring, budget-overview, and budget validation tests)
2. `pnpm lint` passes with no new errors
3. No remaining `budget: 0` stub in budget-overview.tsx trendData computation
4. No remaining `currentBudget.id` in rollover-prompt.tsx fetch URL
5. `getBudgetTotalsByMonth` method exists in BudgetsDB class
6. Trends API response includes `budget_total_cents` field per month
</verification>

<success_criteria>
- SpendingTrendBar chart receives real budget totals per month from the API, not hardcoded $0
- RolloverPrompt correctly calls the rollover endpoint with the previous month's budget ID
- All existing tests pass without modification
- Both BUDG-04 and BUDG-06 verification gaps are closed
</success_criteria>

<output>
After completion, create `.planning/phases/21-budgets-spending-analytics/21-05-SUMMARY.md`
</output>
