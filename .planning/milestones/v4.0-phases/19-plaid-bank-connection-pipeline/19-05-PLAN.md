---
phase: 19-plaid-bank-connection-pipeline
plan: 05
type: execute
wave: 3
depends_on: [19-04]
files_modified:
  - app/money/accounts/page.tsx
  - app/money/accounts/layout.tsx
  - app/money/accounts/oauth/page.tsx
  - components/money/money-page-shell.tsx
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
  - tests/components/money/accounts-list.test.tsx
  - tests/components/money/sync-status-badge.test.tsx
autonomous: true
requirements: [PLAD-01, PLAD-05, PLAD-06]

must_haves:
  truths:
    - "Accounts page renders at /money/accounts with AccountsList component"
    - "OAuth redirect page re-initializes Plaid Link for OAuth bank flows"
    - "i18n strings exist in all three locale files for money.accounts and money.transactions namespaces"
    - "SyncStatusBadge tests cover all four states including syncing"
    - "AccountsList tests cover empty state, net worth, grouped accounts, and error banner"
  artifacts:
    - path: "app/money/accounts/page.tsx"
      provides: "Accounts page with server-side rendering"
      min_lines: 10
    - path: "app/money/accounts/oauth/page.tsx"
      provides: "OAuth redirect handler for bank flows"
      min_lines: 15
    - path: "tests/components/money/accounts-list.test.tsx"
      provides: "AccountsList component tests"
      min_lines: 60
    - path: "tests/components/money/sync-status-badge.test.tsx"
      provides: "SyncStatusBadge component tests"
      min_lines: 40
  key_links:
    - from: "app/money/accounts/page.tsx"
      to: "components/money/accounts-list.tsx"
      via: "renders AccountsList client component"
      pattern: "AccountsList"
    - from: "app/money/accounts/oauth/page.tsx"
      to: "react-plaid-link"
      via: "usePlaidLink with receivedRedirectUri"
      pattern: "usePlaidLink"
---

<objective>
Create the accounts page routes, OAuth redirect handler, update money page shell, add i18n strings in all three locales, and write component tests for SyncStatusBadge and AccountsList.

Purpose: Wires Plan 04's UI components into routable pages and completes internationalization and test coverage.
Output: 3 page routes, updated page shell, i18n in 3 locales, 2 test files.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-plaid-bank-connection-pipeline/19-CONTEXT.md
@.planning/phases/19-plaid-bank-connection-pipeline/19-04-SUMMARY.md
@.planning/phases/18-database-foundation-household-schema/18-02-SUMMARY.md
@components/money/money-page-shell.tsx
@components/money/accounts-list.tsx
@components/money/sync-status-badge.tsx
@app/money/page.tsx
@app/money/layout.tsx
@lib/hooks/use-habits.ts
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create accounts page routes, OAuth handler, and update money page shell</name>
  <files>
    app/money/accounts/layout.tsx
    app/money/accounts/page.tsx
    app/money/accounts/oauth/page.tsx
    components/money/money-page-shell.tsx
  </files>
  <action>
    **1. `app/money/accounts/layout.tsx`** -- Server component:
    - Simple pass-through layout (children only), inherits from money layout

    **2. `app/money/accounts/page.tsx`** -- Server component:
    - PageHeader with title "Accounts" (from i18n)
    - Render AccountsList client component

    **3. `app/money/accounts/oauth/page.tsx`** -- "use client":
    - OAuth redirect handler for banks that require it
    - On mount: reads URL search params (oauth_state_id)
    - Re-initializes Plaid Link with `receivedRedirectUri: window.location.href`
    - Fetches a new link_token and opens Plaid Link automatically
    - Shows loading state while initializing
    - If no oauth state in URL, redirects to /money/accounts

    **4. Update `components/money/money-page-shell.tsx`**:
    - Replace the current "coming soon" empty state with actual content
    - Show AccountsEmptyState for new users OR a quick summary linking to /money/accounts
    - This page becomes the money home/landing -- links to accounts page
  </action>
  <verify>
    - `pnpm lint` passes
    - `pnpm tsc --noEmit` compiles
    - `/money/accounts` route resolves (page.tsx exists)
    - `/money/accounts/oauth` route resolves
    - Money page shell no longer shows "coming soon"
  </verify>
  <done>Accounts page at /money/accounts with PageHeader, OAuth redirect handler, and updated money home page all created.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n strings in all three locales and write component tests</name>
  <files>
    i18n/messages/en.json
    i18n/messages/zh.json
    i18n/messages/zh-TW.json
    tests/components/money/accounts-list.test.tsx
    tests/components/money/sync-status-badge.test.tsx
  </files>
  <action>
    **1. i18n strings** -- Add to money namespace in all three locale files:

    **en.json** additions to `money` key:
    ```json
    "accounts": {
      "title": "Accounts",
      "connectBank": "Connect Bank Account",
      "otherOptions": "Other options",
      "manualEntry": "Enter transaction manually",
      "netWorth": "Net Worth",
      "noAccounts": "No accounts connected",
      "emptyHeading": "Connect your bank",
      "emptyDescription": "Link your bank accounts to automatically track transactions and balances.",
      "orManual": "Or enter transactions manually",
      "syncStatus": {
        "syncing": "Syncing",
        "synced": "Synced",
        "stale": "Stale",
        "error": "Error"
      },
      "syncErrors": "Some accounts have sync errors. Check individual accounts for details.",
      "resync": "Re-sync",
      "disconnect": "Disconnect",
      "disconnectTitle": "Disconnect {institution}?",
      "disconnectDescription": "This will revoke access to your bank account. Choose what to do with existing transactions.",
      "keepTransactions": "Keep Transactions",
      "deleteTransactions": "Delete Transactions",
      "syncingTransactions": "Syncing transactions...",
      "connectionError": "Connection error: {message}",
      "subtotal": "Subtotal"
    },
    "transactions": {
      "title": "Transactions",
      "manual": {
        "title": "Add Transaction",
        "amount": "Amount",
        "description": "Description",
        "date": "Date",
        "category": "Category",
        "account": "Account",
        "cashAccount": "Cash",
        "submit": "Add Transaction",
        "success": "Transaction added"
      }
    }
    ```

    **zh.json** -- Translate all keys to Simplified Chinese.
    **zh-TW.json** -- Translate all keys to Traditional Chinese.

    Ensure the key structure matches exactly across all three files. Include the `syncStatus.syncing` key for the new syncing state.

    **2. Tests:**

    **`tests/components/money/sync-status-badge.test.tsx`** (5 tests):
    - Renders "syncing" variant with spinner icon
    - Renders "synced" variant with correct styling
    - Renders "stale" variant with correct styling
    - Renders "error" variant with correct styling
    - Has no accessibility violations (axe)

    **`tests/components/money/accounts-list.test.tsx`** (6 tests):
    - Shows empty state when no connections
    - Shows net worth summary
    - Groups accounts by institution
    - Shows sync status badges
    - Shows dismissable error banner when connection has error status
    - Has no accessibility violations

    Mock SWR with `vi.mock("swr")` pattern per CLAUDE.md. Mock `react-plaid-link` with `vi.mock("react-plaid-link", () => ({ usePlaidLink: () => ({ open: vi.fn(), ready: true }) }))`.
  </action>
  <verify>
    - `pnpm lint` passes
    - `pnpm test:run` passes all tests (new + existing)
    - All three locale files have matching key structures including syncStatus.syncing
    - Tests cover sync status badge 4 variants and accounts list states including error banner
  </verify>
  <done>i18n strings in all three locales (including syncing + syncErrors keys), 5 SyncStatusBadge tests (including syncing spinner), and 6 AccountsList tests (including error banner) all passing.</done>
</task>

</tasks>

<verification>
- `pnpm lint` passes
- `pnpm test:run` passes all tests
- `pnpm tsc --noEmit` compiles
- Navigate to /money/accounts in browser shows accounts page
- All three locale files have matching money.accounts.* and money.transactions.* keys
- SyncStatusBadge tests cover 4 states
- AccountsList tests cover error banner
</verification>

<success_criteria>
1. Accounts page exists at /money/accounts with AccountsList
2. OAuth redirect page handles bank OAuth flows
3. Money page shell updated from "coming soon" to real content
4. i18n strings in all three locale files with matching structure
5. 11+ component tests pass covering sync badges (4 states) and accounts list (empty, net worth, groups, error banner)
</success_criteria>

<output>
After completion, create `.planning/phases/19-plaid-bank-connection-pipeline/19-05-SUMMARY.md`
</output>
