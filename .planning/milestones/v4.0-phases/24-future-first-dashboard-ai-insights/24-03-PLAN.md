---
phase: 24-future-first-dashboard-ai-insights
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - components/money/money-dashboard.tsx
  - components/money/dashboard-hero.tsx
  - components/money/upcoming-bills-list.tsx
  - components/money/cash-flow-projection.tsx
  - components/money/income-confirmation.tsx
  - components/money/money-page-shell.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "User visiting /money sees a forward-looking dashboard with three key numbers as hero"
    - "Hero shows available money until next paycheck, upcoming bills total, and projected end-of-month balance"
    - "Upcoming bills section shows next 30 days of bills with merchant names and amounts"
    - "Income confirmation prompt appears when income patterns are detected but not yet confirmed"
    - "Dashboard handles empty/partial data gracefully — no crashes when bills, budgets, or income missing"
  artifacts:
    - path: "components/money/money-dashboard.tsx"
      provides: "Main money dashboard orchestrator replacing old MoneyPageShell internals"
      min_lines: 40
    - path: "components/money/dashboard-hero.tsx"
      provides: "Three-number summary row (available, bills, projected)"
      min_lines: 30
    - path: "components/money/upcoming-bills-list.tsx"
      provides: "Next-30-day bill preview list"
      min_lines: 25
    - path: "components/money/cash-flow-projection.tsx"
      provides: "Projected balance section with confidence labels"
      min_lines: 25
    - path: "components/money/income-confirmation.tsx"
      provides: "One-time income confirmation prompt"
      min_lines: 30
  key_links:
    - from: "components/money/money-dashboard.tsx"
      to: "lib/hooks/use-dashboard-money.ts"
      via: "SWR hook for all dashboard data"
      pattern: "useDashboardMoney"
    - from: "components/money/money-page-shell.tsx"
      to: "components/money/money-dashboard.tsx"
      via: "renders MoneyDashboard when connections exist"
      pattern: "MoneyDashboard"
    - from: "components/money/income-confirmation.tsx"
      to: "app/api/money/income/route.ts"
      via: "POST to confirm income"
      pattern: "fetch.*api/money/income"
---

<objective>
Build the forward-looking money dashboard UI that replaces the current MoneyPageShell hub with a hero summary row, upcoming bills list, cash flow projection, and income confirmation prompt.

Purpose: This is the core UI that satisfies DASH-01 through DASH-05 — transforming /money from a simple navigation hub into a forward-looking financial dashboard aligned with Calm Finance design language.

Output: 5 new UI components + updated MoneyPageShell to render the new dashboard.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-future-first-dashboard-ai-insights/24-RESEARCH.md
@.planning/phases/24-future-first-dashboard-ai-insights/24-01-SUMMARY.md
@.planning/phases/24-future-first-dashboard-ai-insights/24-02-SUMMARY.md

@components/money/money-page-shell.tsx
@lib/hooks/use-dashboard-money.ts
@app/api/money/dashboard/route.ts
@lib/money/arithmetic.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard hero row, upcoming bills list, and cash flow projection components</name>
  <files>components/money/dashboard-hero.tsx, components/money/upcoming-bills-list.tsx, components/money/cash-flow-projection.tsx</files>
  <action>
All components use Calm Finance design tokens (--money-sage, --money-amber, --money-surface, --money-border, --money-caution). Use useTranslations("money.dashboard") for all user-facing text.

**`components/money/dashboard-hero.tsx`:**
Per locked decision: "Combined summary row as the hero: three numbers side-by-side — available now, upcoming bills total, projected end-of-month balance — equal weight, no single dominant metric."

- "use client" component
- Props: `{ availableCents: number; upcomingBillsTotalCents: number; endOfMonthBalanceCents: number; confidenceLabel: string; hasConfirmedIncome: boolean; }`
- Layout: Three equal-width cards in a responsive row (`grid grid-cols-1 sm:grid-cols-3 gap-3`)
- Each card:
  - Rounded border using `border-money-border bg-money-surface`
  - Small label text (muted-foreground) describing the metric
  - Large number using `formatMoney()` from lib/money/arithmetic.ts
  - Calm Finance styling: generous padding (p-5), large type for numbers (text-2xl font-bold tabular-nums)
- Available money label: "Available until [next paycheck / end of month]" depending on hasConfirmedIncome
- Bills label: "Upcoming bills (30 days)"
- Projected label: "End of month balance"
- Confidence indicator: small subtle text below the projected balance — e.g., "Estimated" or "Based on confirmed income" — using `text-xs text-muted-foreground italic`
- Color coding (subtle, Calm Finance compliant):
  - Available money: text color uses --money-sage if positive, --money-caution if negative
  - End of month: same logic
  - Bills: always neutral text (bills are expected outflows)

**`components/money/upcoming-bills-list.tsx`:**
- "use client" component
- Props: `{ bills: { merchant_name: string; amount_cents: number; due_date: string }[]; }`
- If no bills: return null (don't render section)
- Renders a compact list inside a Card with money-surface styling
- Section heading: "Upcoming Bills" with bill count
- Each bill row: merchant name (left), amount (right, formatMoney), due date (below merchant, text-xs muted-foreground, formatted as "Feb 28")
- Max 7 bills shown (next 7 days urgency per research recommendation), with "View all bills →" link to /money/bills if more exist
- Sort by due_date ascending (soonest first)

**`components/money/cash-flow-projection.tsx`:**
- "use client" component
- Props: `{ dailyBalances: DailyBalance[]; dailySpendingRateCents: number; }`
- If no daily balances: return null
- Simple visual: a compact sparkline-style summary showing the projected balance trend
- Actually, keep it simple per Calm Finance — just show the key number with context rather than a chart
- Show: "Your balance is projected to [increase/decrease] by [amount] over the next [N] days"
- If any day has danger zone (projected <= 0): show a subtle warning using --money-caution: "Watch for [date] — projected balance may go negative"
- Use getDangerZoneStatus from projections.ts for color logic
- This is a compact informational section, not a full chart
  </action>
  <verify>Run `pnpm lint` to check all components compile. Verify no hardcoded color classes (use Calm Finance tokens only).</verify>
  <done>Three dashboard sub-components render hero metrics, upcoming bills, and cash flow info using Calm Finance tokens. Hero row shows three equal-weight numbers. Upcoming bills sorts by urgency. Cash flow warns on danger zones.</done>
</task>

<task type="auto">
  <name>Task 2: Income confirmation, money dashboard orchestrator, and MoneyPageShell update</name>
  <files>components/money/income-confirmation.tsx, components/money/money-dashboard.tsx, components/money/money-page-shell.tsx</files>
  <action>
**`components/money/income-confirmation.tsx`:**
Per locked decision: "Auto-detect recurring deposits, then show a one-time confirmation prompt. User confirms or corrects before projections use the data."

- "use client" component
- Props: `{ detectedIncome: DetectedIncome[]; onConfirm: (income: { merchant_name: string; amount_cents: number; frequency: string; next_expected_date: string }) => Promise<void>; onDismiss: () => void; }`
- Renders inside a Card with a subtle amber border (this needs attention, not an error)
- Heading: "We noticed regular deposits" (i18n)
- For each detected pattern (show top 1-2 highest confidence):
  - "It looks like you receive [formatMoney(amount)] from [merchant_name] [frequency label]."
  - "Sound right?" with Confirm / Not quite buttons
- Confirm button: calls onConfirm with the pattern data
- "Not quite" or dismiss: calls onDismiss — the pattern won't be used for projections
- Per locked decision: "once confirmed, it's used until the pattern changes" — so this prompt only shows when income_status.needs_confirmation is true
- Use sonner toast for confirmation success feedback

**`components/money/money-dashboard.tsx`:**
Main dashboard orchestrator. Replaces the nav-hub content of MoneyPageShell when user has connected accounts.

- "use client" component
- Uses `useDashboardMoney(viewMode)` hook for all data
- Import and render `InsightList` from `@/components/money/insight-list` (created in Plan 04 Task 1, but since Plan 03 wave=3 and Plan 04 wave=3 run in parallel, InsightList may not exist yet). **Resolution:** Do NOT import InsightList here. Instead, add a placeholder comment `{/* InsightList wired in Plan 04 */}` in the render order. Plan 04 will add `money-dashboard.tsx` to its files_modified and wire InsightList there.
- Loading state: skeleton matching the hero + bills layout
- Renders in order (per Claude's discretion on section order, research recommends: urgency first):
  1. `<DashboardHero />` — always visible, three key numbers
  2. `<IncomeConfirmation />` — only when income_status.needs_confirmation is true
  3. `<UpcomingBillsList />` — next 7 days of bills (urgency)
  4. `<CashFlowProjection />` — balance trend info
  5. `{/* InsightList page="dashboard" — wired in Plan 04 */}`
  6. Quick nav links grid (keep from old MoneyPageShell — links to transactions, budgets, bills, goals, net-worth, settings)
- Props: `{ viewMode: ViewMode }`
- Handle all empty-data states gracefully:
  - No bills? Skip UpcomingBillsList section
  - No income detected? Don't show IncomeConfirmation, hero shows "end of month" instead of "next paycheck"
  - No budget? Cash flow projection uses spending rate only

Income confirmation handler:
```typescript
const handleConfirmIncome = async (income) => {
  await fetch("/api/money/income", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(income),
  });
  toast.success(t("dashboard.incomeConfirmed"));
  mutate(); // Revalidate dashboard data
};
```

**Update `components/money/money-page-shell.tsx`:**
- Keep the existing structure: loading skeleton, empty state (AccountsEmptyState), HouseholdViewTabs
- Replace the current "quick summary + nav links" section with `<MoneyDashboard viewMode={viewMode} />`
- The new MoneyDashboard component handles its own data fetching via useDashboardMoney hook
- Remove the net worth display and navigation links from MoneyPageShell — MoneyDashboard handles this now
- Keep HouseholdViewTabs above MoneyDashboard
  </action>
  <verify>Run `pnpm lint`. Visually verify the component tree: MoneyPageShell → (loading | empty | HouseholdViewTabs + MoneyDashboard). Run `pnpm test:run` for regressions.</verify>
  <done>MoneyPageShell renders MoneyDashboard for connected users. Dashboard shows hero row, income confirmation (when needed), upcoming bills, cash flow info, and nav links. Empty/partial data states handled gracefully. Lint passes.</done>
</task>

</tasks>

<verification>
- `MoneyPageShell` renders `MoneyDashboard` when connections exist (not the old nav hub)
- `DashboardHero` shows three equal-weight numbers with Calm Finance styling
- `IncomeConfirmation` only appears when needs_confirmation is true
- `UpcomingBillsList` shows soonest bills first, max 7
- `CashFlowProjection` warns on danger zone dates
- All components use --money-* CSS tokens, no hardcoded colors
- All user-facing text uses useTranslations (i18n keys to be added in Plan 05)
- `pnpm lint` passes
- `pnpm test:run` passes
</verification>

<success_criteria>
The /money page is a forward-looking dashboard with three hero numbers, upcoming bills, and cash flow projection. Income detection prompt works for first-time confirmation. Calm Finance aesthetic maintained throughout.
</success_criteria>

<output>
After completion, create `.planning/phases/24-future-first-dashboard-ai-insights/24-03-SUMMARY.md`
</output>
