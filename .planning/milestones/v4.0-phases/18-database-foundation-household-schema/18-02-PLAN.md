---
phase: 18-database-foundation-household-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - tailwind.config.ts
  - components/layouts/app-sidebar.tsx
  - app/money/layout.tsx
  - app/money/page.tsx
  - components/money/money-page-shell.tsx
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
  - tests/components/money/money-page-shell.test.tsx
  - tests/components/layouts/app-sidebar.test.tsx
autonomous: true
requirements:
  - FOUN-05
  - FOUN-06
  - FOUN-07

must_haves:
  truths:
    - "Sidebar shows Money navigation item after Tasks, linking to /money"
    - "Money pages use Calm Finance design tokens (sage/seafoam, amber) in both light and dark mode"
    - "Money page shell displays a calm welcome message with 'Bank connection coming soon' interim state"
    - "All money UI strings exist in en, zh, and zh-TW locale files"
  artifacts:
    - path: "app/globals.css"
      provides: "Calm Finance CSS custom properties"
      contains: "--money-sage"
    - path: "components/layouts/app-sidebar.tsx"
      provides: "Money nav item in sidebar"
      contains: "/money"
    - path: "app/money/page.tsx"
      provides: "Money page route"
    - path: "components/money/money-page-shell.tsx"
      provides: "Empty state shell with welcome message"
    - path: "i18n/messages/en.json"
      provides: "English money translations"
      contains: "money"
    - path: "i18n/messages/zh.json"
      provides: "Chinese Simplified money translations"
      contains: "money"
    - path: "i18n/messages/zh-TW.json"
      provides: "Chinese Traditional money translations"
      contains: "money"
  key_links:
    - from: "components/layouts/app-sidebar.tsx"
      to: "app/money/page.tsx"
      via: "href: /money nav item"
      pattern: "href.*[\"']/money[\"']"
    - from: "components/money/money-page-shell.tsx"
      to: "i18n/messages/en.json"
      via: "useTranslations('money')"
      pattern: "useTranslations.*money"
    - from: "app/globals.css"
      to: "tailwind.config.ts"
      via: "CSS variables registered as Tailwind utilities"
      pattern: "--money-"
---

<objective>
Create the visual foundation for money features: Calm Finance design tokens, sidebar navigation, i18n money namespace, and the /money page shell with welcoming empty state.

Purpose: Establish the UI layer so users can navigate to money features and see a calm, intentional empty state while feature phases build the actual functionality.

Output: CSS design tokens, sidebar nav update, money page with empty state, and i18n strings in all three locales.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-database-foundation-household-schema/18-CONTEXT.md
@.planning/phases/18-database-foundation-household-schema/18-RESEARCH.md
@app/globals.css
@tailwind.config.ts
@components/layouts/app-sidebar.tsx
@i18n/messages/en.json
@app/dashboard/page.tsx
@components/layouts/sidebar-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Calm Finance design tokens, sidebar nav, and i18n strings</name>
  <files>
    app/globals.css
    tailwind.config.ts
    components/layouts/app-sidebar.tsx
    i18n/messages/en.json
    i18n/messages/zh.json
    i18n/messages/zh-TW.json
    tests/components/layouts/app-sidebar.test.tsx
  </files>
  <action>
**CSS tokens: `app/globals.css`**

Add Calm Finance tokens in both `:root` and `.dark` sections, placed after the existing `--highlight` block (following the pattern from Phase 10 decisions). Use raw HSL format (space-separated numbers, no hsl() wrapper) matching all existing tokens.

Light mode (`:root`):
```css
/* === Calm Finance tokens === */
--money-sage: 155 25% 55%;
--money-sage-light: 155 20% 92%;
--money-sage-foreground: 155 30% 25%;
--money-amber: 35 60% 55%;
--money-amber-light: 35 40% 92%;
--money-amber-foreground: 35 50% 30%;
--money-surface: 155 10% 98%;
--money-border: 155 15% 88%;
```

Dark mode (`.dark`):
```css
--money-sage: 155 20% 50%;
--money-sage-light: 155 15% 16%;
--money-sage-foreground: 155 25% 75%;
--money-amber: 35 50% 55%;
--money-amber-light: 35 30% 16%;
--money-amber-foreground: 35 40% 70%;
--money-surface: 155 8% 15%;
--money-border: 155 10% 22%;
```

These colors create a spa-like, desaturated sage/seafoam palette — very calming, no aggressive red/green. Amber for expenses is warm and gentle, not alarming.

**Tailwind config: `tailwind.config.ts`**

Register the money tokens as Tailwind color utilities in the `extend.colors` section:
```typescript
money: {
  sage: {
    DEFAULT: "hsl(var(--money-sage))",
    light: "hsl(var(--money-sage-light))",
    foreground: "hsl(var(--money-sage-foreground))",
  },
  amber: {
    DEFAULT: "hsl(var(--money-amber))",
    light: "hsl(var(--money-amber-light))",
    foreground: "hsl(var(--money-amber-foreground))",
  },
  surface: "hsl(var(--money-surface))",
  border: "hsl(var(--money-border))",
},
```

**Sidebar: `components/layouts/app-sidebar.tsx`**

Add `Wallet` icon import from `lucide-react`. Add money nav item to the `mainNavItems` array AFTER the tasks entry (position: Dashboard > Habits > Tasks > Money per user decision, noting that "Projects" was mentioned in CONTEXT.md but sidebar currently has Dashboard > Habits > Tasks — so Money goes after Tasks):

```typescript
{
  href: "/money",
  icon: Wallet,
  labelKey: "money",
  match: (p: string) => p.startsWith("/money"),
},
```

Using `Wallet` icon — simple, recognizable, clean lines, matches the minimal style of existing icons (Home, ClipboardList, ListChecks). Per RESEARCH.md recommendation.

**i18n: All three locale files**

Add `"money"` key to the sidebar navigation namespace in each locale file (wherever sidebar nav labels are stored — check existing structure and add `"money": "Money"` for en).

Add `"money"` top-level namespace to all three locale files with the following structure:

**en.json:**
```json
"money": {
  "nav": {
    "money": "Money"
  },
  "page": {
    "title": "Your Financial Picture",
    "subtitle": "A calm view of your finances"
  },
  "emptyState": {
    "heading": "Your calm financial picture starts here",
    "description": "Connect your bank accounts to see your complete financial overview in one peaceful place.",
    "comingSoon": "Bank connection coming soon",
    "comingSoonDescription": "We're building a seamless way to connect your accounts. Stay tuned."
  },
  "format": {
    "currency": "USD"
  }
}
```

**zh.json (Simplified Chinese):**
```json
"money": {
  "nav": {
    "money": "财务"
  },
  "page": {
    "title": "你的财务全景",
    "subtitle": "平静地查看你的财务状况"
  },
  "emptyState": {
    "heading": "你的平静财务画面从这里开始",
    "description": "连接你的银行账户，在一个安静的地方查看完整的财务概览。",
    "comingSoon": "银行连接即将推出",
    "comingSoonDescription": "我们正在构建一种无缝连接账户的方式。敬请期待。"
  },
  "format": {
    "currency": "USD"
  }
}
```

**zh-TW.json (Traditional Chinese):**
```json
"money": {
  "nav": {
    "money": "財務"
  },
  "page": {
    "title": "你的財務全景",
    "subtitle": "平靜地查看你的財務狀況"
  },
  "emptyState": {
    "heading": "你的平靜財務畫面從這裡開始",
    "description": "連接你的銀行帳戶，在一個安靜的地方查看完整的財務概覽。",
    "comingSoon": "銀行連接即將推出",
    "comingSoonDescription": "我們正在構建一種無縫連接帳戶的方式。敬請期待。"
  },
  "format": {
    "currency": "USD"
  }
}
```

Also add the `"money"` key to the sidebar nav labels section in each locale file (check where `"dashboard"`, `"habits"`, `"tasks"` labels are — add `"money": "Money"` / `"money": "财务"` / `"money": "財務"` there).

**Sidebar test update: `tests/components/layouts/app-sidebar.test.tsx`**

Update the existing sidebar test to account for the new Money nav item:
- Update any test assertions that count nav items (e.g., from 3 to 4)
- Add a test case verifying the Money nav item renders with correct label and links to `/money`
- Add a test case verifying `/money/accounts` triggers active state on Money nav item
  </action>
  <verify>
Run `pnpm lint` — no errors. Run `pnpm test:run tests/components/layouts/app-sidebar.test.tsx` — all tests pass including new Money nav tests. Verify all three locale files have matching `money.*` key structure with no missing keys.
  </verify>
  <done>
Calm Finance CSS tokens (sage, amber, surface, border) exist in both light and dark mode. Sidebar shows Money nav with Wallet icon after Tasks. All three locale files have complete `money.*` namespace. Sidebar tests updated and passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create money page shell with empty state</name>
  <files>
    app/money/layout.tsx
    app/money/page.tsx
    components/money/money-page-shell.tsx
    tests/components/money/money-page-shell.test.tsx
  </files>
  <action>
**Layout: `app/money/layout.tsx`**

Create a server component layout for money pages that wraps children in `SidebarShell` (same pattern as existing authenticated layouts like `app/dashboard/layout.tsx` or `app/habits/layout.tsx`). The layout provides the sidebar shell wrapper for all money sub-routes.

**Page: `app/money/page.tsx`**

Create a server component that renders the money page shell. Use `getTranslations("money")` for the page title. Render `PageHeader` with the translated title and subtitle, then `MoneyPageShell` below. The page should use the same content layout pattern as other pages (wrapped in the sidebar content area).

**Money page shell: `components/money/money-page-shell.tsx`**

Create a `"use client"` component that displays the Phase 18 interim empty state:

1. Import `useTranslations` from `next-intl`
2. Use `useTranslations("money")` to get translated strings
3. Render a centered card/container with:
   - The Calm Finance surface background (`bg-money-surface` or appropriate token class)
   - A subtle money-border colored border
   - A calming icon (e.g., `Wallet` from lucide-react, rendered large and in sage color)
   - `emptyState.heading` as h2 — the main welcome message
   - `emptyState.description` as paragraph text
   - A visually distinct "coming soon" section:
     - `emptyState.comingSoon` as a label/badge
     - `emptyState.comingSoonDescription` as supporting text
   - The overall feel should be spa-like, calm, intentional — not broken or missing
   - Use money design tokens: `text-money-sage-foreground`, `bg-money-sage-light`, `border-money-border`

The component should look like a welcoming landing page, not an error state. The "coming soon" badge should feel intentional — user knows features are on the way per CONTEXT.md decisions.

**Test: `tests/components/money/money-page-shell.test.tsx`**

Test the MoneyPageShell component:
- Mock `next-intl` with `useTranslations: () => (key: string) => key` pattern
- Verify the heading text renders (the translation key)
- Verify the "coming soon" text renders
- Verify no accessibility violations using `vitest-axe` (`expect(await axe(container)).toHaveNoViolations()`)
  </action>
  <verify>
Run `pnpm test:run tests/components/money/money-page-shell.test.tsx` — all tests pass. Run `pnpm lint` — no errors. Run `pnpm build` — build succeeds with new money route. Run `pnpm test:run` — all existing tests pass (1207+ baseline plus new tests).
  </verify>
  <done>
Money page accessible at `/money` route with SidebarShell layout. Empty state shows calm welcome message with Calm Finance design tokens. "Bank connection coming soon" interim state feels intentional. MoneyPageShell tests pass with accessibility check. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. Run `pnpm lint` — zero errors
2. Run `pnpm test:run` — all tests pass (existing + new sidebar + money page shell tests)
3. Run `pnpm build` — production build succeeds with `/money` route
4. Verify CSS tokens exist in both `:root` and `.dark` sections of globals.css
5. Verify all three locale files have identical `money.*` key structure
6. Verify sidebar nav shows 4 items (Dashboard, Habits, Tasks, Money) with Money last
</verification>

<success_criteria>
- Calm Finance CSS tokens (`--money-sage`, `--money-amber`, etc.) exist in both light and dark mode
- Tailwind utilities (`bg-money-sage`, `text-money-amber-foreground`, etc.) registered and usable
- Sidebar shows "Money" with Wallet icon after Tasks, active state works on `/money/*` paths
- `/money` page renders with welcoming empty state using money design tokens
- "Bank connection coming soon" CTA visible and styled intentionally (not broken)
- i18n strings present in all three locales (en, zh, zh-TW) with no missing keys
- All existing tests pass, new tests pass, build succeeds, lint clean
</success_criteria>

<output>
After completion, create `.planning/phases/18-database-foundation-household-schema/18-02-SUMMARY.md`
</output>
