---
phase: 21-history-personal-records-progression
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-03"]
files_modified:
  - components/fitness/progress/exercise-progress-chart.tsx
  - components/fitness/progress/pr-banner.tsx
  - components/fitness/workout-logger/workout-exercise-card.tsx
  - components/fitness/workout-history/workout-detail-view.tsx
  - lib/hooks/use-active-workout.ts
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
autonomous: true
requirements:
  - HIST-03
  - HIST-04
  - HIST-05
  - I18N-01

must_haves:
  truths:
    - "User can view a per-exercise progression chart showing max weight and volume over time"
    - "User can toggle the chart date range between 1m, 3m, 6m, and all"
    - "User sees a PR banner mid-workout when a new personal record is set"
    - "PR banner appears for the correct exercise type (weight PR for barbell exercises, reps PR for bodyweight exercises, duration PR for timed exercises)"
    - "PRs are computed on-demand from workout_sets data via computePersonalRecords(), not stored in a separate DB table (per research Anti-Pattern 4 — avoids sync complexity for solo-user scale)"
    - "Workout detail view includes ExerciseProgressChart for each exercise (integrated from Plan 03's detail view)"
    - "All progression and PR strings are translated in en, zh, and zh-TW"
  artifacts:
    - path: "components/fitness/progress/exercise-progress-chart.tsx"
      provides: "Recharts LineChart with date range selector for exercise progression"
    - path: "components/fitness/progress/pr-banner.tsx"
      provides: "Inline congratulatory PR banner component"
  key_links:
    - from: "components/fitness/progress/exercise-progress-chart.tsx"
      to: "lib/hooks/use-exercise-history.ts"
      via: "useExerciseHistory() SWR hook"
      pattern: "useExerciseHistory"
    - from: "components/fitness/workout-logger/workout-exercise-card.tsx"
      to: "lib/fitness/personal-records.ts"
      via: "isNewPR() for mid-workout detection"
      pattern: "isNewPR"
    - from: "components/fitness/progress/exercise-progress-chart.tsx"
      to: "components/ui/chart.tsx"
      via: "ChartContainer, ChartTooltip, ChartConfig"
      pattern: "ChartContainer"
---

<objective>
Build the per-exercise progression chart with date range selector, create the mid-workout PR banner, and integrate PR detection into the active workout flow.

Purpose: Users can track their exercise progression over time with visual charts (HIST-03), see when they achieve personal records (HIST-04), and get congratulated mid-workout when setting new PRs (HIST-05).
Output: Progression chart component, PR banner component, PR detection integrated into workout logger.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-history-personal-records-progression/21-RESEARCH.md
@.planning/phases/21-history-personal-records-progression/21-01-SUMMARY.md
@.planning/phases/21-history-personal-records-progression/21-03-SUMMARY.md
@components/ui/chart.tsx
@components/fitness/workout-history/workout-detail-view.tsx
@lib/hooks/use-exercise-history.ts
@lib/fitness/personal-records.ts
@lib/fitness/units.ts
@lib/fitness/exercise-fields.ts
@lib/hooks/use-active-workout.ts
@components/fitness/workout-logger/workout-exercise-card.tsx
@components/fitness/workout-logger/workout-set-row.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exercise progression chart with date range selector</name>
  <files>
    components/fitness/progress/exercise-progress-chart.tsx
    i18n/messages/en.json
    i18n/messages/zh.json
    i18n/messages/zh-TW.json
  </files>
  <action>
    **Step 1: Create `components/fitness/progress/exercise-progress-chart.tsx`.**
    Client component ("use client") following the research code example closely:
    - Props: `exerciseId: string`, `exerciseName: string`, `weightUnit: WeightUnit`
    - Uses `useExerciseHistory(exerciseId)` hook to fetch ALL data once (per Pitfall 2 -- fetch all, filter client-side)
    - Local state for date range: `useState<"1m" | "3m" | "6m" | "all">("3m")`
    - `useMemo` to filter data by date range client-side (compare against cutoff date computed from range)
    - `useMemo` to transform filtered data into chart format: convert weight_kg to display unit via `displayWeight()`, compute volume in display units
    - Render using shadcn chart components:
      - `ChartContainer` with `ChartConfig` defining two series: weight (--chart-1 color) and volume (--chart-2 color)
      - `LineChart` with `accessibilityLayer` prop
      - `CartesianGrid vertical={false}`
      - `XAxis` with date formatting (month short + day: "Feb 24"), no axis line, no tick line
      - `YAxis` with no axis line, no tick line, width=50
      - `ChartTooltip` with `ChartTooltipContent`
      - Two `Line` components: weight and volume, both `type="monotone"`, `strokeWidth={2}`, `dot={{ r: 3 }}`, `connectNulls`
    - ToggleGroup for date range selector (1M, 3M, 6M, All) -- use shadcn `ToggleGroup` + `ToggleGroupItem`
    - Handle empty state: show message "Not enough data for chart" when filtered data has < 2 points
    - Handle loading state: show Skeleton placeholder matching chart dimensions
    - Weight unit label in Y axis or tooltip (e.g., "kg" or "lbs")

    **Step 2: Integrate progression chart into workout detail view.**
    Plan 03 has already created `components/fitness/workout-history/workout-detail-view.tsx`. Add the `ExerciseProgressChart` to this existing file:
    - After each exercise's set table in `workout-detail-view.tsx`, render `<ExerciseProgressChart exerciseId={exercise.exercise_id} exerciseName={exercise.exercise.name} weightUnit={weightUnit} />`
    - Import the component at the top of the file
    - The chart should appear below the sets table for each exercise, providing visual progression context alongside the set data

    **Step 3: Add i18n strings to all 3 locale files.**
    Add under `"workouts"` namespace:
    ```
    "progression": "Progression",
    "maxWeight": "Max Weight",
    "totalVolume": "Total Volume",
    "dateRange1m": "1M",
    "dateRange3m": "3M",
    "dateRange6m": "6M",
    "dateRangeAll": "All",
    "notEnoughData": "Not enough data to show chart",
    "notEnoughDataDescription": "Complete more workouts with this exercise to see progression"
    ```
    Add equivalent translations for zh and zh-TW.
  </action>
  <verify>
    - `pnpm tsc --noEmit` passes
    - `pnpm lint` passes
    - `ls components/fitness/progress/exercise-progress-chart.tsx` exists
  </verify>
  <done>
    - Exercise progression chart renders LineChart with max weight and volume series
    - Date range selector toggles between 1m/3m/6m/all with client-side filtering
    - Chart uses shadcn ChartContainer for automatic dark mode theming
    - Weight values displayed in user's preferred unit
    - Empty state handled when insufficient data points
    - All chart strings translated in en, zh, zh-TW
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PR banner and integrate PR detection into workout flow</name>
  <files>
    components/fitness/progress/pr-banner.tsx
    components/fitness/workout-logger/workout-exercise-card.tsx
    lib/hooks/use-active-workout.ts
    i18n/messages/en.json
    i18n/messages/zh.json
    i18n/messages/zh-TW.json
  </files>
  <action>
    **Step 1: Create `components/fitness/progress/pr-banner.tsx`.**
    Client component for the congratulatory PR inline banner:
    - Props: `exerciseName: string`, `prType: "weight" | "volume" | "reps" | "duration"`, `value: number`, `weightUnit?: WeightUnit`, `onDismiss?: () => void`
    - Visual design: themed inline banner within exercise card area
      - Background: `bg-amber-50 dark:bg-amber-950/30` (gold/celebration theme)
      - Border: `border border-amber-200 dark:border-amber-800`
      - Rounded: `rounded-lg`
      - Trophy icon from lucide-react (`Trophy`) in amber color
      - Text: "New PR!" + exercise name + formatted value (e.g., "100 kg" for weight, "12 reps" for reps, "60s" for duration)
      - Auto-dismiss after 5 seconds using setTimeout + state
      - Animate in with simple opacity transition (CSS animation or Tailwind animate-in)
    - Use `useTranslations("workouts")` for all strings
    - Respect `motion-reduce:transition-none` for accessibility

    **Step 2: Integrate PR detection into workout flow.**
    Modify `components/fitness/workout-logger/workout-exercise-card.tsx`:
    - Import `useExerciseRecords` from `lib/hooks/use-exercise-history` -- fetch current PR for each exercise in the workout
    - Import `isNewPR` from `lib/fitness/personal-records`
    - When a set is marked complete (the existing `onComplete` handler or similar), capture the set data and exercise info BEFORE the async mutation (per Pitfall 3 -- avoid race condition)
    - Run `isNewPR(completedSet, exerciseType, currentPR)` synchronously with captured values
    - If any PR is detected (isWeightPR || isVolumePR || isRepsPR || isDurationPR), set local state to show the PR banner for that exercise
    - Render `<PRBanner>` component inline within the exercise card when a PR is detected
    - The PR banner auto-dismisses after 5 seconds

    **Alternative approach if WorkoutExerciseCard doesn't handle set completion directly:**
    The set completion may be handled by WorkoutSetRow or by the parent WorkoutLogger. In that case:
    - Add a `prDetected` state to WorkoutExerciseCard
    - Pass an `onPRDetected(exerciseId, prType, value)` callback from the parent
    - In the parent (WorkoutLogger), after completeSet succeeds, check for PR and call onPRDetected
    - WorkoutExerciseCard renders the PR banner when its exerciseId matches

    Read the actual component code to determine the correct integration point.

    **Step 3: Add i18n strings to all 3 locale files.**
    Add under `"workouts"` namespace:
    ```
    "newPR": "New PR!",
    "personalRecord": "Personal Record",
    "weightPR": "Weight PR: {value}",
    "volumePR": "Volume PR: {value}",
    "repsPR": "Reps PR: {value} reps",
    "durationPR": "Duration PR: {value}s",
    "prAchieved": "PR"
    ```
    Add equivalent translations for zh and zh-TW.
  </action>
  <verify>
    - `pnpm tsc --noEmit` passes
    - `pnpm lint` passes
    - `pnpm test:run` passes (existing tests still green)
    - `ls components/fitness/progress/pr-banner.tsx` exists
  </verify>
  <done>
    - PR banner shows inline within exercise card with trophy icon and celebration styling
    - PR detection runs client-side on set completion with no server round-trip
    - PR comparison uses exercise-type-aware logic (weight for barbell, reps for bodyweight, duration for timed)
    - Banner auto-dismisses after 5 seconds
    - Race condition prevented by capturing set data before async mutation
    - All PR strings translated in en, zh, zh-TW
  </done>
</task>

</tasks>

<verification>
1. `pnpm tsc --noEmit` — no type errors
2. `pnpm lint` — no lint errors
3. `pnpm test:run` — all existing tests pass
4. `pnpm build` — production build succeeds (validates Recharts SSR compatibility)
5. `ls components/fitness/progress/` — both components exist
</verification>

<success_criteria>
- Progression chart renders with max weight and volume series, toggleable date ranges
- Chart uses shadcn dark mode theming automatically
- PR banner appears inline in exercise card during active workout when a new PR is set
- PR detection is exercise-type-aware (correct PR type per exercise)
- Banner auto-dismisses after 5 seconds
- All new strings translated in all 3 locales
- No regression in active workout functionality
</success_criteria>

<output>
After completion, create `.planning/phases/21-history-personal-records-progression/21-04-SUMMARY.md`
</output>
