---
phase: 22-bug-fix-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/fitness/workout-logger/workout-finish-dialog.tsx
  - components/fitness/workout-logger/workout-logger.tsx
  - components/fitness/workout-logger/workout-rest-timer.tsx
  - lib/fitness/workout-session.ts
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
autonomous: true
requirements: [SETT-02]

must_haves:
  truths:
    - "WorkoutFinishDialog shows volume in user's preferred unit (kg or lbs), not hardcoded 'kg'"
    - "Orphaned WorkoutRestTimer component file no longer exists"
    - "loadWorkoutFromStorage function no longer exported from workout-session.ts"
    - "restTimerSkip i18n key removed from all three locale files"
  artifacts:
    - path: "components/fitness/workout-logger/workout-finish-dialog.tsx"
      provides: "Volume display using formatWeight with user's weightUnit"
      contains: "formatWeight"
    - path: "components/fitness/workout-logger/workout-logger.tsx"
      provides: "weightUnit prop passed to WorkoutFinishDialog"
      contains: "weightUnit={weightUnit}"
  key_links:
    - from: "components/fitness/workout-logger/workout-logger.tsx"
      to: "components/fitness/workout-logger/workout-finish-dialog.tsx"
      via: "weightUnit prop"
      pattern: "weightUnit=\\{weightUnit\\}"
    - from: "components/fitness/workout-logger/workout-finish-dialog.tsx"
      to: "lib/fitness/units.ts"
      via: "formatWeight import"
      pattern: "import.*formatWeight.*from.*lib/fitness/units"
---

<objective>
Fix the SETT-02 requirement (all weight displays respect user's unit preference) by wiring the user's weight unit to WorkoutFinishDialog, and clean up dead code from Phases 18-19 (orphaned component, unused export, stale i18n key).

Purpose: Close the last gap for SETT-02 and remove dead code that could confuse future development.
Output: Corrected finish dialog volume display, no orphaned files or unused exports.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bug-fix-dead-code-cleanup/22-RESEARCH.md
@components/fitness/workout-logger/workout-finish-dialog.tsx
@components/fitness/workout-logger/workout-logger.tsx
@lib/fitness/units.ts
@lib/fitness/workout-session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WorkoutFinishDialog volume to use user's weight unit</name>
  <files>
    components/fitness/workout-logger/workout-finish-dialog.tsx
    components/fitness/workout-logger/workout-logger.tsx
  </files>
  <action>
    **In `workout-finish-dialog.tsx`:**
    1. Add import: `import { formatWeight } from "@/lib/fitness/units";`
    2. Add import: `import type { WeightUnit } from "@/lib/db/types";`
    3. Add `weightUnit: WeightUnit` to the `WorkoutFinishDialogProps` interface.
    4. Add `weightUnit` to the destructured props in the function signature.
    5. Replace the hardcoded volume display on line 99:
       - OLD: `{stats.totalVolume > 0 ? \`${stats.totalVolume.toLocaleString()} kg\` : "-"}`
       - NEW: `{stats.totalVolume > 0 ? formatWeight(stats.totalVolume, weightUnit) : "-"}`

    **In `workout-logger.tsx`:**
    6. Add `weightUnit={weightUnit}` prop to the `<WorkoutFinishDialog>` component (around line 140-149). The `weightUnit` variable is already in scope from line 23 (`const weightUnit = useWeightUnit()`).

    **Why formatWeight instead of manual template:** `formatWeight(kg, unit)` from `lib/fitness/units.ts` handles the kg-to-lbs conversion with 2-decimal rounding AND appends the unit suffix. This is the same function used in `WorkoutSetRow` for displaying previous set weights.

    **Volume is already computed in kg:** The `stats.totalVolume` useMemo sums `weight_kg * reps` in kg (the canonical storage unit). Converting at the display boundary avoids cumulative rounding errors.
  </action>
  <verify>
    Run `pnpm build` to confirm TypeScript compilation passes (TypeScript will catch any missing prop or import errors). Run `pnpm lint` to confirm no lint errors. Grep for hardcoded `kg` in the finish dialog to confirm it's gone: `grep -n "kg" components/fitness/workout-logger/workout-finish-dialog.tsx` should only show the import/type reference, not a hardcoded string.
  </verify>
  <done>
    WorkoutFinishDialog accepts a `weightUnit` prop and uses `formatWeight()` to display volume. WorkoutLogger passes `weightUnit` to the dialog. No hardcoded `'kg'` unit suffix remains in the volume display. Build and lint pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned WorkoutRestTimer, unused loadWorkoutFromStorage, and stale i18n key</name>
  <files>
    components/fitness/workout-logger/workout-rest-timer.tsx
    lib/fitness/workout-session.ts
    i18n/messages/en.json
    i18n/messages/zh.json
    i18n/messages/zh-TW.json
  </files>
  <action>
    **Delete orphaned component:**
    1. Delete `components/fitness/workout-logger/workout-rest-timer.tsx` entirely. This component was built in Phase 19-03 but superseded by the inline rest timer in `workout-header.tsx` (Phase 19-05). It has zero importers — confirmed by grep.

    **Remove unused export from workout-session.ts:**
    2. In `lib/fitness/workout-session.ts`, remove the entire `loadWorkoutFromStorage` function (the function starting with `export function loadWorkoutFromStorage(): WorkoutWithExercises | null {` and its body). Keep `saveWorkoutToStorage`, `clearWorkoutStorage`, and `STORAGE_KEY` — those ARE used by `useActiveWorkout`.

    **Clean i18n keys from all three locale files:**
    3. Remove the `"restTimerSkip": "Skip"` line from `i18n/messages/en.json` (line 938).
    4. Remove the `"restTimerSkip": "跳过"` line from `i18n/messages/zh.json` (line 938).
    5. Remove the `"restTimerSkip": "跳過"` line from `i18n/messages/zh-TW.json` (line 938).
    Note: When removing these lines, fix trailing comma issues if the removed line was the last property in its parent object.

    **Why these are safe to remove:**
    - `WorkoutRestTimer`: zero importers (grep confirmed). Rest timer display lives in `workout-header.tsx`.
    - `loadWorkoutFromStorage`: exported but never imported anywhere. SWR fetches from `/api/workouts/active` for session recovery.
    - `restTimerSkip`: only used by the orphaned `WorkoutRestTimer` component. The inline rest timer in the header uses `restTimerDismiss` instead.
  </action>
  <verify>
    Run `pnpm build` to confirm no broken imports (if any file imported the deleted component or function, build will fail). Run `pnpm lint` to confirm no lint errors. Run `pnpm test:run` to confirm no test regressions. Verify deletion: `ls components/fitness/workout-logger/workout-rest-timer.tsx` should return "No such file". Verify removal: `grep -n "loadWorkoutFromStorage" lib/fitness/workout-session.ts` should return no matches. Verify i18n: `grep -rn "restTimerSkip" i18n/` should return no matches.
  </verify>
  <done>
    `workout-rest-timer.tsx` file deleted. `loadWorkoutFromStorage` function removed from `workout-session.ts`. `restTimerSkip` key removed from all three locale files. Build, lint, and tests pass with no regressions.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes — no broken imports or type errors from the changes
2. `pnpm lint` passes — no lint errors introduced
3. `pnpm test:run` passes — no test regressions (no existing tests cover these files, so this confirms nothing else broke)
4. `grep -n "toLocaleString.*kg" components/fitness/workout-logger/workout-finish-dialog.tsx` returns no matches (hardcoded kg is gone)
5. `grep -n "formatWeight" components/fitness/workout-logger/workout-finish-dialog.tsx` returns a match (new pattern in use)
6. `grep -rn "workout-rest-timer\|WorkoutRestTimer" components/` returns no matches (orphaned component fully removed)
7. `grep -n "loadWorkoutFromStorage" lib/fitness/workout-session.ts` returns no matches (unused export removed)
8. `grep -rn "restTimerSkip" i18n/` returns no matches (stale i18n key removed from all 3 locales)
</verification>

<success_criteria>
- WorkoutFinishDialog shows volume in user's preferred unit (kg or lbs) using formatWeight
- Zero dead code: no orphaned files, no unused exports, no stale i18n keys
- Build, lint, and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-bug-fix-dead-code-cleanup/22-01-SUMMARY.md`
</output>
