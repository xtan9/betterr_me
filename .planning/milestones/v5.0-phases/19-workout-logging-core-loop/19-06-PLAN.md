---
phase: 19-workout-logging-core-loop
plan: 06
type: execute
wave: 4
depends_on: ["19-04", "19-05"]
files_modified:
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
autonomous: true
requirements: [WLOG-01, WLOG-02, WLOG-03, WLOG-06, WLOG-07, WLOG-08, WLOG-09, WLOG-10, WLOG-11, WLOG-12, REST-01, REST-02, REST-03, REST-04, REST-05]

must_haves:
  truths:
    - "All workout logger UI strings are translated in en, zh, and zh-TW"
    - "Rest timer strings are translated in all three locales"
    - "Workout lifecycle strings (start, finish, discard, resume) are translated"
    - "Set type labels (warmup, normal, drop set, failure) are translated"
  artifacts:
    - path: "i18n/messages/en.json"
      provides: "English workout logging translations"
      contains: "startWorkout"
    - path: "i18n/messages/zh.json"
      provides: "Simplified Chinese workout logging translations"
      contains: "startWorkout"
    - path: "i18n/messages/zh-TW.json"
      provides: "Traditional Chinese workout logging translations"
      contains: "startWorkout"
  key_links:
    - from: "components/fitness/workout-logger/*.tsx"
      to: "i18n/messages/en.json"
      via: "useTranslations('workouts')"
      pattern: "useTranslations.*workouts"
---

<objective>
Add all i18n translation strings for the workout logging UI in all three locales (en, zh, zh-TW). This covers workout session, rest timer, set types, exercise picker, finish/discard dialogs, and resume banner.

Purpose: The app supports three locales. All user-facing strings must be translated. This plan runs after the UI components exist so we know exactly which translation keys are needed.
Output: Updated locale files with complete workout logging translations
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-workout-logging-core-loop/19-04-SUMMARY.md
@.planning/phases/19-workout-logging-core-loop/19-05-SUMMARY.md

# Existing i18n structure
@i18n/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workout logging i18n strings to all three locales</name>
  <files>
    i18n/messages/en.json
    i18n/messages/zh.json
    i18n/messages/zh-TW.json
  </files>
  <action>
    Extend the existing `"workouts"` namespace in all three locale files. The existing keys are: `title`, `exerciseLibrary`, `exerciseLibraryDescription`, `browse`, `comingSoon`, `comingSoonDescription`. Add the following new keys under the `"workouts"` namespace:

    **Workout session:**
    - `startWorkout` — "Start Workout" / "开始训练" / "開始訓練"
    - `activeWorkout` — "Active Workout" / "进行中的训练" / "進行中的訓練"
    - `elapsed` — "Elapsed" / "已用时间" / "已用時間"
    - `finishWorkout` — "Finish Workout" / "完成训练" / "完成訓練"
    - `discardWorkout` — "Discard Workout" / "放弃训练" / "放棄訓練"
    - `noActiveWorkout` — "No active workout" / "没有进行中的训练" / "沒有進行中的訓練"
    - `workoutTitle` — "Workout Title" / "训练标题" / "訓練標題"
    - `untitledWorkout` — "Untitled Workout" / "未命名训练" / "未命名訓練"
    - `notes` — "Notes" / "备注" / "備註"
    - `addNotes` — "Add notes..." / "添加备注..." / "新增備註..."

    **Exercise management:**
    - `addExercise` — "Add Exercise" / "添加动作" / "新增動作"
    - `selectExercise` — "Select Exercise" / "选择动作" / "選擇動作"
    - `exerciseAdded` — "Added" / "已添加" / "已新增"
    - `removeExercise` — "Remove Exercise" / "移除动作" / "移除動作"
    - `exerciseNotes` — "Exercise notes..." / "动作备注..." / "動作備註..."

    **Set logging:**
    - `set` — "Set" / "组" / "組"
    - `previous` — "Previous" / "上次" / "上次"
    - `weight` — "Weight" / "重量" / "重量"
    - `reps` — "Reps" / "次数" / "次數"
    - `duration` — "Duration" / "时长" / "時長"
    - `addSet` — "+ Add Set" / "+ 添加组" / "+ 新增組"
    - `deleteSet` — "Delete Set" / "删除组" / "刪除組"
    - `noPrevious` — "—" / "—" / "—"

    **Set types:**
    - `setType` — "Set Type" / "组类型" / "組類型"
    - `warmup` — "Warmup" / "热身" / "熱身"
    - `normal` — "Normal" / "正式" / "正式"
    - `dropSet` — "Drop Set" / "递减组" / "遞減組"
    - `failure` — "Failure" / "力竭" / "力竭"

    **Rest timer:**
    - `restTimer` — "Rest Timer" / "休息计时" / "休息計時"
    - `restTime` — "Rest: {seconds}s" / "休息: {seconds}秒" / "休息: {seconds}秒"
    - `skip` — "Skip" / "跳过" / "跳過"

    **Finish dialog:**
    - `finishConfirm` — "Finish this workout?" / "完成此训练？" / "完成此訓練？"
    - `finishSummary` — "Workout Summary" / "训练总结" / "訓練總結"
    - `totalDuration` — "Duration" / "总时长" / "總時長"
    - `totalExercises` — "Exercises" / "动作数" / "動作數"
    - `totalSets` — "Sets Completed" / "完成组数" / "完成組數"
    - `totalVolume` — "Total Volume" / "总负荷" / "總負荷"
    - `confirm` — "Confirm" / "确认" / "確認"
    - `cancel` — "Cancel" / "取消" / "取消"

    **Discard dialog:**
    - `discardConfirm` — "Discard this workout?" / "放弃此训练？" / "放棄此訓練？"
    - `discardWarning` — "This will discard your current workout. Your data will be saved but marked as discarded." / "这将放弃你当前的训练。数据会被保存但标记为已放弃。" / "這將放棄你目前的訓練。資料會被儲存但標記為已放棄。"

    **Resume banner:**
    - `resumeBanner` — "You have an active workout" / "你有一个进行中的训练" / "你有一個進行中的訓練"
    - `resume` — "Resume" / "继续" / "繼續"

    **Also remove old coming-soon keys** that are now replaced by actual functionality:
    - Remove `comingSoon` and `comingSoonDescription` keys (no longer needed — workouts page now has real content)

    IMPORTANT: Add ALL strings to ALL THREE locale files. Verify each file has the exact same set of keys under the `"workouts"` namespace.
  </action>
  <verify>
    `pnpm build` succeeds. `pnpm lint` passes. Verify all three locale files have identical key sets under "workouts" namespace:
    ```bash
    # Check key count matches across locales
    node -e "const en = require('./i18n/messages/en.json'); const zh = require('./i18n/messages/zh.json'); const tw = require('./i18n/messages/zh-TW.json'); console.log('en:', Object.keys(en.workouts).length, 'zh:', Object.keys(zh.workouts).length, 'zh-TW:', Object.keys(tw.workouts).length)"
    ```
  </verify>
  <done>
    All workout logging i18n strings exist in en, zh, and zh-TW locales. Key counts match across all three files. Coming-soon placeholder keys removed.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `pnpm lint` passes
- `pnpm test:run` passes (no regressions)
- All three locale files have matching workouts namespace keys
- No leftover placeholder/coming-soon text
</verification>

<success_criteria>
Complete i18n coverage for all workout logging UI strings across en, zh, and zh-TW locales. All user-facing text in the workout logger, rest timer, dialogs, and resume banner has translations.
</success_criteria>

<output>
After completion, create `.planning/phases/19-workout-logging-core-loop/19-06-SUMMARY.md`
</output>
