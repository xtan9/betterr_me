---
phase: 19-workout-logging-core-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260224000003_add_workout_discarded_status.sql
  - lib/db/types.ts
  - lib/db/workouts.ts
  - lib/db/workout-exercises.ts
  - lib/db/index.ts
  - lib/validations/workout.ts
autonomous: true
requirements: [WLOG-01, WLOG-03, WLOG-04, WLOG-05, WLOG-06, WLOG-07, WLOG-10]

must_haves:
  truths:
    - "WorkoutsDB can start a workout, get active workout, list completed/discarded workouts, update status, and query nested exercises/sets"
    - "WorkoutExercisesDB can add/remove exercises, add/update/delete sets within a workout"
    - "Zod schemas validate all workout and set input at API boundaries"
    - "WorkoutStatus type includes 'discarded' for soft-delete support"
  artifacts:
    - path: "supabase/migrations/20260224000003_add_workout_discarded_status.sql"
      provides: "ALTER CHECK constraint to add discarded status"
      contains: "discarded"
    - path: "lib/db/workouts.ts"
      provides: "WorkoutsDB class with CRUD + active workout query"
      exports: ["WorkoutsDB"]
    - path: "lib/db/workout-exercises.ts"
      provides: "WorkoutExercisesDB class for exercises and sets within a workout"
      exports: ["WorkoutExercisesDB"]
    - path: "lib/validations/workout.ts"
      provides: "Zod schemas for workout create/update and set create/update"
      exports: ["workoutCreateSchema", "workoutUpdateSchema", "workoutSetCreateSchema", "workoutSetUpdateSchema", "addExerciseToWorkoutSchema"]
  key_links:
    - from: "lib/db/workouts.ts"
      to: "supabase workouts table"
      via: "supabase.from('workouts')"
      pattern: "from.*workouts"
    - from: "lib/db/workout-exercises.ts"
      to: "supabase workout_exercises and workout_sets tables"
      via: "supabase.from('workout_exercises') and supabase.from('workout_sets')"
      pattern: "from.*workout_(exercises|sets)"
---

<objective>
Create the data foundation for workout logging: schema migration to support discarded workouts, WorkoutsDB and WorkoutExercisesDB classes with full CRUD operations, and Zod validation schemas for all workout API inputs.

Purpose: All workout API routes (Plan 02) depend on these DB classes and validation schemas. This plan establishes the data layer that every other plan in Phase 19 builds on.
Output: 2 DB classes, 1 migration, 5+ Zod schemas, updated types
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-workout-logging-core-loop/19-RESEARCH.md

# Existing patterns to follow
@lib/db/exercises.ts
@lib/db/types.ts
@lib/validations/exercise.ts
@lib/validations/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration and WorkoutsDB class</name>
  <files>
    supabase/migrations/20260224000003_add_workout_discarded_status.sql
    lib/db/types.ts
    lib/db/workouts.ts
  </files>
  <action>
    1. **Migration** — Create migration that:
       - Drops existing `workouts_status_check` constraint
       - Adds new constraint: `CHECK (status IN ('in_progress', 'completed', 'discarded'))`
       - This is the ONLY schema change; all tables already exist from Phase 18

    2. **Update WorkoutStatus type** in `lib/db/types.ts`:
       - Change `WorkoutStatus = "in_progress" | "completed"` to include `| "discarded"`

    3. **WorkoutsDB class** in `lib/db/workouts.ts`:
       - Constructor takes `SupabaseClient` (same pattern as ExercisesDB)
       - `startWorkout(userId, data)`: INSERT into workouts with `status: 'in_progress'`, `started_at: new Date().toISOString()`. Catch error code `23505` (unique constraint from partial index `idx_workouts_active`) and throw a descriptive error.
       - `getActiveWorkout(userId)`: SELECT from workouts WHERE `user_id = userId` AND `status = 'in_progress'`, with nested select for `workout_exercises(*, exercise:exercises(*), sets:workout_sets(*))`. Order exercises by sort_order asc, sets by set_number asc. Return null if not found (PGRST116).
       - `getWorkoutWithExercises(workoutId)`: Same nested select but by workout ID. Used for both active and completed workouts.
       - `updateWorkout(workoutId, updates)`: PATCH workouts row. If status changing to 'completed', compute `completed_at = new Date().toISOString()` and `duration_seconds = Math.floor((completedAt - startedAt) / 1000)` and merge into updates. If status changing to 'discarded', just update status.
       - `getWorkouts(userId, options?: { limit?: number })`: SELECT from workouts WHERE `user_id = userId` AND `status != 'in_progress'`, order by `started_at DESC`, limit `options?.limit ?? 20`. Returns array of workout rows (no nested exercises). Used by GET /api/workouts for workout history listing.
       - `getPreviousSets(exerciseId)`: For WLOG-09. Query `workout_exercises` with inner join on `workouts` where `status = 'completed'` and matching `exercise_id`, limit 5, sort by workout `started_at` desc in app code, return sets from most recent match sorted by set_number. Return empty array if none found.
       - Use `import { log } from "@/lib/logger"` for error logging.
  </action>
  <verify>
    `pnpm build` succeeds (TypeScript compiles). Check that `WorkoutsDB` is importable and all methods have correct signatures.
  </verify>
  <done>
    WorkoutsDB class exists with startWorkout, getActiveWorkout, getWorkouts, getWorkoutWithExercises, updateWorkout, and getPreviousSets methods. WorkoutStatus includes 'discarded'. Migration file exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: WorkoutExercisesDB class and Zod validation schemas</name>
  <files>
    lib/db/workout-exercises.ts
    lib/db/index.ts
    lib/validations/workout.ts
  </files>
  <action>
    1. **WorkoutExercisesDB class** in `lib/db/workout-exercises.ts`:
       - Constructor takes `SupabaseClient`
       - `addExercise(workoutId, exerciseId, restTimerSeconds?)`: INSERT into workout_exercises. Compute sort_order: query max sort_order for this workout, add 65536.0 (same gap as task sort_order pattern). Return the created row.
       - `removeExercise(workoutExerciseId)`: DELETE from workout_exercises. CASCADE will delete related sets.
       - `updateExercise(workoutExerciseId, updates)`: PATCH workout_exercises (notes, rest_timer_seconds).
       - `addSet(workoutExerciseId, data)`: INSERT into workout_sets. Compute set_number: query max set_number for this workout_exercise, increment by 1. Default set_type to 'normal'.
       - `updateSet(setId, updates)`: PATCH workout_sets row. Accept weight_kg, reps, duration_seconds, distance_meters, is_completed, set_type.
       - `deleteSet(setId)`: DELETE from workout_sets.
       - Use `import { log } from "@/lib/logger"` for error logging.

    2. **Barrel export** — Add WorkoutsDB and WorkoutExercisesDB to `lib/db/index.ts`.

    3. **Zod validation schemas** in `lib/validations/workout.ts` (per RESEARCH.md code examples):
       - `workoutCreateSchema`: `{ title?: string (trim, 1-100), routine_id?: uuid | null }`
       - `workoutUpdateSchema`: `{ title?, notes? (max 2000, nullable), status?: enum('in_progress','completed','discarded') }` with `.refine()` non-empty check
       - `addExerciseToWorkoutSchema`: `{ exercise_id: uuid, rest_timer_seconds?: int 0-600 }`
       - `workoutExerciseUpdateSchema`: `{ notes?: string (max 2000, nullable), rest_timer_seconds?: int 0-600 }` with `.refine()` non-empty
       - `workoutSetCreateSchema`: `{ set_type?: enum default 'normal', weight_kg?: number 0-99999.99 nullable, reps?: int 0-9999 nullable, duration_seconds?: int 0-86400 nullable, distance_meters?: number 0-999999.99 nullable, is_completed?: boolean default false }`
       - `workoutSetUpdateSchema`: Same fields as create but all optional, with `.refine()` non-empty check
  </action>
  <verify>
    `pnpm build` succeeds. `pnpm lint` passes. Both DB classes and all Zod schemas are importable from their respective modules.
  </verify>
  <done>
    WorkoutExercisesDB exists with addExercise, removeExercise, updateExercise, addSet, updateSet, deleteSet methods. All 6 Zod schemas exported from lib/validations/workout.ts. Both DB classes exported from lib/db/index.ts.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero TypeScript errors
- `pnpm lint` passes
- `pnpm test:run` passes (no regressions)
- WorkoutsDB and WorkoutExercisesDB are importable from `@/lib/db`
- All 6 Zod schemas are importable from `@/lib/validations/workout`
- WorkoutStatus type includes "discarded"
</verification>

<success_criteria>
Data layer for workout logging is complete: 2 DB classes with full CRUD, 6 Zod validation schemas, migration for discarded status support. All existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/19-workout-logging-core-loop/19-01-SUMMARY.md`
</output>
