---
phase: 19-workout-logging-core-loop
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - app/api/workouts/route.ts
  - app/api/workouts/active/route.ts
  - app/api/workouts/[id]/route.ts
  - app/api/workouts/[id]/exercises/route.ts
  - app/api/workouts/[id]/exercises/[weId]/route.ts
  - app/api/workouts/[id]/exercises/[weId]/sets/route.ts
  - app/api/workouts/[id]/exercises/[weId]/sets/[setId]/route.ts
autonomous: true
requirements: [WLOG-01, WLOG-02, WLOG-03, WLOG-04, WLOG-05, WLOG-06, WLOG-07, WLOG-09, WLOG-10, WLOG-11, WLOG-12, REST-02]

must_haves:
  truths:
    - "POST /api/workouts starts a new workout and returns 409 if one is already active"
    - "GET /api/workouts/active returns the full active workout with nested exercises, sets, and previous values"
    - "PATCH /api/workouts/[id] can finish or discard a workout"
    - "POST/PATCH/DELETE on exercises and sets within a workout modify the correct nested entities"
  artifacts:
    - path: "app/api/workouts/route.ts"
      provides: "POST start workout, GET list workouts"
      exports: ["POST", "GET"]
    - path: "app/api/workouts/active/route.ts"
      provides: "GET active workout with nested data and previous values"
      exports: ["GET"]
    - path: "app/api/workouts/[id]/route.ts"
      provides: "GET workout detail, PATCH update workout (finish/discard/notes/title)"
      exports: ["GET", "PATCH"]
    - path: "app/api/workouts/[id]/exercises/route.ts"
      provides: "POST add exercise to workout"
      exports: ["POST"]
    - path: "app/api/workouts/[id]/exercises/[weId]/route.ts"
      provides: "PATCH update exercise (notes, rest timer), DELETE remove exercise"
      exports: ["PATCH", "DELETE"]
    - path: "app/api/workouts/[id]/exercises/[weId]/sets/route.ts"
      provides: "POST add set to exercise"
      exports: ["POST"]
    - path: "app/api/workouts/[id]/exercises/[weId]/sets/[setId]/route.ts"
      provides: "PATCH update set, DELETE remove set"
      exports: ["PATCH", "DELETE"]
  key_links:
    - from: "app/api/workouts/route.ts"
      to: "lib/db/workouts.ts"
      via: "WorkoutsDB.startWorkout()"
      pattern: "WorkoutsDB"
    - from: "app/api/workouts/active/route.ts"
      to: "lib/db/workouts.ts"
      via: "WorkoutsDB.getActiveWorkout() + getPreviousSets()"
      pattern: "getActiveWorkout|getPreviousSets"
    - from: "app/api/workouts/[id]/exercises/[weId]/sets/route.ts"
      to: "lib/db/workout-exercises.ts"
      via: "WorkoutExercisesDB.addSet()"
      pattern: "WorkoutExercisesDB"
---

<objective>
Create all 7 workout API routes covering the full workout lifecycle: start, get active, update (finish/discard), and nested CRUD for exercises and sets within a workout.

Purpose: These API routes are the REST interface that the useActiveWorkout hook and workout logger UI consume. Every mutation in the workout logger flows through these endpoints.
Output: 7 API route files with full CRUD coverage
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-workout-logging-core-loop/19-RESEARCH.md
@.planning/phases/19-workout-logging-core-loop/19-01-SUMMARY.md

# Existing API route patterns to follow
@app/api/exercises/route.ts
@app/api/exercises/[id]/route.ts
@lib/validations/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core workout routes (start, active, update)</name>
  <files>
    app/api/workouts/route.ts
    app/api/workouts/active/route.ts
    app/api/workouts/[id]/route.ts
  </files>
  <action>
    Follow existing API route pattern from `app/api/exercises/route.ts`: createClient, auth check, DB class instantiation, try/catch with log.error, Zod validation via `validateRequestBody`.

    1. **POST /api/workouts** (`app/api/workouts/route.ts`):
       - Auth check → validate body with `workoutCreateSchema` → `workoutsDB.startWorkout(user.id, data)` → return 201
       - Catch error code `23505` → return 409 `{ error: "You already have an active workout" }`
       - Also add GET handler: `workoutsDB.getWorkouts(userId)` listing completed/discarded workouts (needed later for history, simple SELECT with status != 'in_progress', order by started_at desc, limit 20)

    2. **GET /api/workouts/active** (`app/api/workouts/active/route.ts`):
       - Auth check → `workoutsDB.getActiveWorkout(user.id)` → if null return `{ workout: null }`
       - Enrich response with previous values: for each exercise in the workout, call `workoutsDB.getPreviousSets(exercise.exercise_id)` and attach as `previousSets` array on each exercise entry
       - Return `{ workout: enrichedWorkout }`

    3. **GET/PATCH /api/workouts/[id]** (`app/api/workouts/[id]/route.ts`):
       - GET: `workoutsDB.getWorkoutWithExercises(params.id)` → return workout or 404
       - PATCH: validate body with `workoutUpdateSchema` → `workoutsDB.updateWorkout(params.id, data)` → return updated workout
       - PATCH handles title, notes, status changes (finish = status:'completed', discard = status:'discarded')
  </action>
  <verify>
    `pnpm build` succeeds. Route files compile. Verify all 3 files export correct HTTP method handlers (POST, GET, PATCH).
  </verify>
  <done>
    POST /api/workouts starts a workout (201) or returns 409 on conflict. GET /api/workouts/active returns enriched active workout with previous values. GET/PATCH /api/workouts/[id] handles detail and updates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Nested exercise and set CRUD routes</name>
  <files>
    app/api/workouts/[id]/exercises/route.ts
    app/api/workouts/[id]/exercises/[weId]/route.ts
    app/api/workouts/[id]/exercises/[weId]/sets/route.ts
    app/api/workouts/[id]/exercises/[weId]/sets/[setId]/route.ts
  </files>
  <action>
    Follow same auth + validation + try/catch pattern. All routes instantiate `WorkoutExercisesDB` with a fresh server client.

    1. **POST /api/workouts/[id]/exercises** (`exercises/route.ts`):
       - Validate body with `addExerciseToWorkoutSchema` → `workoutExercisesDB.addExercise(params.id, data.exercise_id, data.rest_timer_seconds)` → return 201

    2. **PATCH/DELETE /api/workouts/[id]/exercises/[weId]** (`[weId]/route.ts`):
       - PATCH: validate body with `workoutExerciseUpdateSchema` → `workoutExercisesDB.updateExercise(params.weId, data)` → return updated
       - DELETE: `workoutExercisesDB.removeExercise(params.weId)` → return 204

    3. **POST /api/workouts/[id]/exercises/[weId]/sets** (`sets/route.ts`):
       - Validate body with `workoutSetCreateSchema` → `workoutExercisesDB.addSet(params.weId, data)` → return 201

    4. **PATCH/DELETE /api/workouts/[id]/exercises/[weId]/sets/[setId]** (`sets/[setId]/route.ts`):
       - PATCH: validate body with `workoutSetUpdateSchema` → `workoutExercisesDB.updateSet(params.setId, data)` → return updated
       - DELETE: `workoutExercisesDB.deleteSet(params.setId)` → return 204

    All route params accessed via `params` argument in Next.js 16 route handler signature: `(request: NextRequest, { params }: { params: Promise<{ id: string; weId?: string; setId?: string }> })`. Remember to `await params` in Next.js 16.
  </action>
  <verify>
    `pnpm build` succeeds. `pnpm lint` passes. All 4 route files compile with correct HTTP method exports.
  </verify>
  <done>
    All nested workout CRUD routes exist: add/remove exercises, add/update/delete sets. Each route follows the established auth + validation + error handling pattern.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `pnpm lint` passes
- `pnpm test:run` passes (no regressions)
- All 7 API route files exist and export correct HTTP methods
- Route nesting follows RESTful conventions: /api/workouts/[id]/exercises/[weId]/sets/[setId]
</verification>

<success_criteria>
Complete REST API for workout logging: start workout, get active workout with previous values, update workout (finish/discard/title/notes), add/remove exercises, add/update/delete sets. All routes use Zod validation and proper error handling.
</success_criteria>

<output>
After completion, create `.planning/phases/19-workout-logging-core-loop/19-02-SUMMARY.md`
</output>
