---
phase: 19-workout-logging-core-loop
plan: 05
type: execute
wave: 3
depends_on: ["19-02", "19-03"]
files_modified:
  - components/fitness/workout-logger/workout-add-exercise.tsx
  - components/fitness/workout-logger/workout-rest-timer.tsx
  - components/fitness/workout-logger/workout-finish-dialog.tsx
  - components/fitness/workout-logger/workout-discard-dialog.tsx
  - components/fitness/workout-resume-banner.tsx
  - app/workouts/page.tsx
autonomous: true
requirements: [WLOG-02, WLOG-07, WLOG-08, REST-01, REST-02, REST-03, REST-04, REST-05]

must_haves:
  truths:
    - "User can add exercises from a searchable/filterable picker sheet"
    - "User sees a rest timer countdown after marking a set complete with +15s/-15s buttons"
    - "Rest timer plays an audio beep on completion and auto-dismisses"
    - "User can finish a workout via a confirmation dialog showing summary stats"
    - "User can discard a workout via a confirmation dialog"
    - "User sees a resume banner on the workouts page when an active workout exists"
  artifacts:
    - path: "components/fitness/workout-logger/workout-add-exercise.tsx"
      provides: "Exercise picker sheet using existing ExerciseFilterBar and exercise list"
      min_lines: 30
    - path: "components/fitness/workout-logger/workout-rest-timer.tsx"
      provides: "Rest timer countdown display with +15s/-15s buttons and skip"
      min_lines: 40
    - path: "components/fitness/workout-logger/workout-finish-dialog.tsx"
      provides: "Finish workout confirmation dialog with summary stats"
      min_lines: 20
    - path: "components/fitness/workout-logger/workout-discard-dialog.tsx"
      provides: "Discard workout confirmation dialog"
      min_lines: 20
    - path: "components/fitness/workout-resume-banner.tsx"
      provides: "Resume active workout banner for workouts landing page"
      min_lines: 20
  key_links:
    - from: "components/fitness/workout-logger/workout-add-exercise.tsx"
      to: "lib/hooks/use-exercises.ts"
      via: "useExercises() for exercise list"
      pattern: "useExercises"
    - from: "components/fitness/workout-logger/workout-rest-timer.tsx"
      to: "lib/fitness/rest-timer.ts"
      via: "receives restTimer prop from useRestTimer()"
      pattern: "remaining|isActive|adjust|skip"
    - from: "components/fitness/workout-resume-banner.tsx"
      to: "/api/workouts/active"
      via: "fetch or SWR check for active workout"
      pattern: "api/workouts/active"
    - from: "app/workouts/page.tsx"
      to: "/api/workouts"
      via: "POST fetch in Start Workout button handler"
      pattern: "api/workouts"
---

<objective>
Build the exercise picker, rest timer display, workout lifecycle dialogs (finish/discard), and the resume banner for the workouts landing page.

Purpose: These components complete the workout logging experience: adding exercises, managing rest between sets, ending/discarding workouts, and resuming interrupted sessions. The exercise picker reuses existing Phase 18 exercise library components. The rest timer UI renders the countdown from the useRestTimer hook (Plan 03).
Output: 5 UI components + updated workouts landing page
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-workout-logging-core-loop/19-RESEARCH.md
@.planning/phases/19-workout-logging-core-loop/19-02-SUMMARY.md
@.planning/phases/19-workout-logging-core-loop/19-03-SUMMARY.md

# Existing components to reuse
@components/fitness/exercise-library/exercise-filter-bar.tsx
@components/fitness/exercise-library/exercise-card.tsx
@lib/hooks/use-exercises.ts
@app/workouts/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exercise picker sheet and rest timer display</name>
  <files>
    components/fitness/workout-logger/workout-add-exercise.tsx
    components/fitness/workout-logger/workout-rest-timer.tsx
  </files>
  <action>
    1. **`workout-add-exercise.tsx`** — Exercise picker in a Sheet (per CONTEXT.md: "Add Exercise button at bottom opens a sheet/modal"):
       - Uses shadcn `Sheet` component (opens from bottom on mobile, side on desktop)
       - Props: `open: boolean`, `onOpenChange`, `onSelectExercise(exerciseId: string)`, `workoutExerciseIds: string[]` (to show which are already added)
       - Content reuses existing Phase 18 components:
         - `useExercises()` hook for exercise data
         - `ExerciseFilterBar` for search input + muscle group + equipment dropdowns
         - Exercise list rendered as clickable items (tap to add). Each shows exercise name, muscle group, exercise type icon. Already-added exercises show a checkmark or "Added" badge.
       - On exercise click: call `onSelectExercise(exerciseId)` → parent calls `actions.addExercise()` → close sheet
       - Apply same client-side filtering logic as ExerciseLibrary (useMemo + Array.filter)
       - `"use client"` directive

    2. **`workout-rest-timer.tsx`** — Rest timer countdown display:
       - Props: receives rest timer state from parent (`remaining`, `isActive`, `adjust`, `skip` — from `useRestTimer()` hook)
       - Conditional render: only shows when `isActive` is true
       - Display placement: Claude's discretion per CONTEXT.md. Recommend a floating banner at the bottom of the screen (above any bottom UI) or a sticky element below the workout header. Use `fixed bottom-4 left-1/2 -translate-x-1/2` positioning for mobile-friendly floating banner.
       - Timer display: large countdown digits (text-3xl or text-4xl font), formatted as `M:SS`
       - Three buttons in a row: `-15s` (outline button), `Skip` (ghost button), `+15s` (outline button)
       - `-15s` calls `adjust(-15)`, `+15s` calls `adjust(15)`, `Skip` calls `skip()`
       - Visual flash when timer reaches 0: brief bg color change (e.g., animate-pulse for 1-2 seconds before auto-dismiss)
       - Smooth entrance/exit: use transition or CSS animation for appear/disappear
       - Background: `bg-card` with border and shadow for elevation above workout content
       - `"use client"` directive
  </action>
  <verify>
    `pnpm build` succeeds. Both components compile. Exercise picker reuses existing useExercises hook.
  </verify>
  <done>
    Exercise picker sheet shows searchable/filterable exercise list for adding to workout. Rest timer displays countdown with +15s/-15s adjustment and skip buttons, with visual flash on completion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finish/discard dialogs, resume banner, and workouts page update</name>
  <files>
    components/fitness/workout-logger/workout-finish-dialog.tsx
    components/fitness/workout-logger/workout-discard-dialog.tsx
    components/fitness/workout-resume-banner.tsx
    app/workouts/page.tsx
  </files>
  <action>
    1. **`workout-finish-dialog.tsx`** — Finish confirmation dialog (per CONTEXT.md: "confirmation dialog showing workout summary"):
       - Uses shadcn `AlertDialog` (same pattern as ProjectDeleteDialog)
       - Props: `open`, `onOpenChange`, `workout: WorkoutWithExercises`, `onConfirm: () => void`
       - Summary content: workout duration (formatted), number of exercises, total sets completed, total volume (weight x reps sum across all completed sets)
       - "Finish Workout" primary button, "Cancel" secondary button
       - On confirm: calls `onConfirm()` (parent calls `actions.finishWorkout()`)
       - `"use client"` directive

    2. **`workout-discard-dialog.tsx`** — Discard confirmation dialog (per CONTEXT.md: "confirmation dialog required"):
       - Uses shadcn `AlertDialog` with destructive styling
       - Props: `open`, `onOpenChange`, `onConfirm: () => void`
       - Warning text: "This will discard your current workout. Your data will be saved but marked as discarded."
       - "Discard Workout" destructive button, "Cancel" secondary button
       - On confirm: calls `onConfirm()` (parent calls `actions.discardWorkout()`)
       - `"use client"` directive

    3. **`workout-resume-banner.tsx`** — Resume active workout banner (per CONTEXT.md: "top banner on workouts page — not a blocking modal"):
       - Checks for active workout: use a lightweight SWR fetch to `/api/workouts/active` or accept `hasActiveWorkout` as prop
       - If active workout found: show a banner at the top with:
         - Text: "You have an active workout" with workout title and elapsed time
         - "Resume" button (primary) → navigate to /workouts/active
         - "Discard" button (ghost/destructive) → open WorkoutDiscardDialog
       - Non-blocking: rendered inside the page flow, not as a modal
       - `"use client"` directive

    4. **Update `app/workouts/page.tsx`** — Add resume banner and "Start Workout" button:
       - Import and render `WorkoutResumeBanner` at top of page content
       - Add "Start Workout" button in the PageHeader actions area (or as a prominent CTA card)
       - Start Workout button: calls POST /api/workouts to start, then navigates to /workouts/active
       - If an active workout exists (banner showing), the Start button should be hidden or disabled
       - Keep existing exercise library card link
  </action>
  <verify>
    `pnpm build` succeeds. `pnpm lint` passes. All 4 files exist. Workouts landing page shows resume banner conditionally and start workout button.
  </verify>
  <done>
    Finish dialog shows workout summary with confirm/cancel. Discard dialog warns about discarding with destructive confirm. Resume banner appears on workouts page when active workout exists. Workouts page has Start Workout button and exercise library link.
  </done>
</task>

</tasks>

<verification>
- `pnpm build` passes
- `pnpm lint` passes
- `pnpm test:run` passes (no regressions)
- All 6 files exist
- Exercise picker reuses useExercises hook and filtering
- Rest timer renders countdown with adjustment buttons
- Finish dialog shows workout summary
- Resume banner is non-blocking (not a modal)
</verification>

<success_criteria>
Supporting workout UI components complete: exercise picker sheet for adding exercises, rest timer floating display with +15s/-15s buttons and skip, finish/discard confirmation dialogs, and resume banner on workouts landing page with start workout button.
</success_criteria>

<output>
After completion, create `.planning/phases/19-workout-logging-core-loop/19-05-SUMMARY.md`
</output>
