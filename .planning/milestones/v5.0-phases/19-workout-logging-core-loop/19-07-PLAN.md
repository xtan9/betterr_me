---
phase: 19-workout-logging-core-loop
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - components/fitness/workout-logger/workout-logger.tsx
  - components/fitness/workout-logger/workout-exercise-card.tsx
  - lib/hooks/use-active-workout.ts
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
autonomous: true
gap_closure: true
requirements:
  - WLOG-02
  - WLOG-07
  - REST-02
  - WLOG-01
  - WLOG-03
  - WLOG-04
  - WLOG-05
  - WLOG-06
  - WLOG-08
  - WLOG-09
  - WLOG-10
  - WLOG-11
  - WLOG-12
  - REST-01
  - REST-03
  - REST-04
  - REST-05

must_haves:
  truths:
    - "Clicking 'Add Exercise' button in active workout opens the WorkoutAddExercise bottom sheet instead of navigating away"
    - "Selecting an exercise in the sheet adds it to the workout and closes the sheet"
    - "Clicking Finish button shows WorkoutFinishDialog with workout summary stats before completing"
    - "Clicking Discard (X) button shows WorkoutDiscardDialog confirmation before discarding"
    - "User can tap the rest timer duration label on an exercise card to edit it via a popover"
  artifacts:
    - path: "components/fitness/workout-logger/workout-logger.tsx"
      provides: "Wired WorkoutAddExercise sheet and WorkoutFinishDialog + WorkoutDiscardDialog"
      contains: "WorkoutAddExercise"
    - path: "components/fitness/workout-logger/workout-exercise-card.tsx"
      provides: "Editable rest timer duration per exercise"
      contains: "rest_timer_seconds"
    - path: "lib/hooks/use-active-workout.ts"
      provides: "updateExerciseRestTimer action"
      contains: "updateExerciseRestTimer"
  key_links:
    - from: "components/fitness/workout-logger/workout-logger.tsx"
      to: "components/fitness/workout-logger/workout-add-exercise.tsx"
      via: "import + useState open + onSelectExercise prop"
      pattern: "WorkoutAddExercise"
    - from: "components/fitness/workout-logger/workout-logger.tsx"
      to: "components/fitness/workout-logger/workout-finish-dialog.tsx"
      via: "import + useState open + onConfirm prop"
      pattern: "WorkoutFinishDialog"
    - from: "components/fitness/workout-logger/workout-logger.tsx"
      to: "components/fitness/workout-logger/workout-discard-dialog.tsx"
      via: "import + useState open + onConfirm prop"
      pattern: "WorkoutDiscardDialog"
    - from: "components/fitness/workout-logger/workout-exercise-card.tsx"
      to: "PATCH /api/workouts/[id]/exercises/[weId]"
      via: "onUpdateRestTimer callback from parent"
      pattern: "onUpdateRestTimer"
---

<objective>
Wire three orphaned UI components into the active workout flow and add a rest timer duration edit control.

Purpose: Close 3 verification gaps where components were built but never connected to the workout logger — the exercise picker sheet, finish/discard confirmation dialogs, and a REST-02 rest timer configuration control.

Output: Fully wired workout logger where Add Exercise opens a bottom sheet, Finish/Discard show confirmation dialogs, and rest timer duration is editable per-exercise.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-workout-logging-core-loop/19-VERIFICATION.md
@.planning/phases/19-workout-logging-core-loop/19-04-SUMMARY.md
@.planning/phases/19-workout-logging-core-loop/19-05-SUMMARY.md
@components/fitness/workout-logger/workout-logger.tsx
@components/fitness/workout-logger/workout-add-exercise.tsx
@components/fitness/workout-logger/workout-finish-dialog.tsx
@components/fitness/workout-logger/workout-discard-dialog.tsx
@components/fitness/workout-logger/workout-exercise-card.tsx
@components/fitness/workout-logger/workout-header.tsx
@lib/hooks/use-active-workout.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire exercise picker sheet, finish dialog, and discard dialog into workout-logger.tsx</name>
  <files>
    components/fitness/workout-logger/workout-logger.tsx
  </files>
  <action>
    Three components are orphaned — built in Plan 05 but never imported or used. Wire them all into workout-logger.tsx:

    **1. WorkoutAddExercise sheet (Gap 1 — WLOG-02):**
    - Import `WorkoutAddExercise` from `"./workout-add-exercise"`
    - Add `const [addExerciseOpen, setAddExerciseOpen] = useState(false)` state
    - Replace the `router.push("/workouts/exercises")` onClick on the "Add Exercise" button with `setAddExerciseOpen(true)`
    - Remove the `useRouter` import and `router` variable (no longer needed if only used for exercise navigation — check if router.push is used elsewhere in the component first; it IS used in the empty state `router.push("/workouts")` so keep useRouter)
    - Render `<WorkoutAddExercise open={addExerciseOpen} onOpenChange={setAddExerciseOpen} onSelectExercise={(id) => actions.addExercise(id)} workoutExerciseIds={workout.exercises.map(e => e.exercise_id)} />` inside the return, after the exercise cards div
    - IMPORTANT: `exercise_id` is the exercise's ID on the workout exercise object. Verify the field name from the `WorkoutExerciseWithPrevious` type (extends `WorkoutExerciseWithDetails` which has `exercise_id`).

    **2. WorkoutFinishDialog (Gap 2 — WLOG-07):**
    - Import `WorkoutFinishDialog` from `"./workout-finish-dialog"`
    - Add `const [showFinishDialog, setShowFinishDialog] = useState(false)` state
    - Add `const [finishDuration, setFinishDuration] = useState(0)` state (for duration prop per React compiler purity rule — see 19-05-SUMMARY)
    - Change `onFinish={actions.finishWorkout}` on WorkoutHeader to `onFinish={() => { setFinishDuration(Math.floor((Date.now() - new Date(workout.started_at).getTime()) / 1000)); setShowFinishDialog(true); }}`
    - Render `<WorkoutFinishDialog open={showFinishDialog} onOpenChange={setShowFinishDialog} workout={workout} durationSeconds={finishDuration} onConfirm={() => { void actions.finishWorkout(); setShowFinishDialog(false); }} />`

    **3. WorkoutDiscardDialog (Gap 2 supplement — WLOG-07):**
    - Import `WorkoutDiscardDialog` from `"./workout-discard-dialog"`
    - Add `const [showDiscardDialog, setShowDiscardDialog] = useState(false)` state
    - Change `onDiscard={actions.discardWorkout}` on WorkoutHeader to `onDiscard={() => setShowDiscardDialog(true)}`
    - Render `<WorkoutDiscardDialog open={showDiscardDialog} onOpenChange={setShowDiscardDialog} onConfirm={() => { void actions.discardWorkout(); setShowDiscardDialog(false); }} />`

    **Import cleanup:**
    - Add `useState` to the react import (it should already be there from the new state variables, but confirm)
    - Confirm `useState` is imported from "react"
  </action>
  <verify>
    - `pnpm lint` passes with 0 errors
    - `pnpm test:run` passes (no regressions)
    - Grep confirms: `WorkoutAddExercise` is imported in workout-logger.tsx
    - Grep confirms: `WorkoutFinishDialog` is imported in workout-logger.tsx
    - Grep confirms: `WorkoutDiscardDialog` is imported in workout-logger.tsx
    - Grep confirms: NO remaining `router.push('/workouts/exercises')` in workout-logger.tsx
    - Grep confirms: NO remaining direct `actions.finishWorkout` or `actions.discardWorkout` passed as prop to WorkoutHeader
  </verify>
  <done>
    Add Exercise button opens the bottom sheet picker instead of navigating away. Finish button shows confirmation dialog with summary stats. Discard button shows confirmation dialog. All three previously-orphaned components are wired into the workout flow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add rest timer duration edit control and updateExerciseRestTimer action</name>
  <files>
    components/fitness/workout-logger/workout-exercise-card.tsx
    components/fitness/workout-logger/workout-logger.tsx
    lib/hooks/use-active-workout.ts
    i18n/messages/en.json
    i18n/messages/zh.json
    i18n/messages/zh-TW.json
  </files>
  <action>
    **1. Add `updateExerciseRestTimer` action to `use-active-workout.ts` (REST-02):**
    - Add a new action method `updateExerciseRestTimer` to `UseActiveWorkoutActions` interface:
      ```
      updateExerciseRestTimer: (workoutExerciseId: string, seconds: number) => Promise<void>;
      ```
    - Implement it by PATCHing `/api/workouts/${workout.id}/exercises/${workoutExerciseId}` with `{ rest_timer_seconds: seconds }`. Use optimistic update pattern matching `updateExerciseNotes`. Update the exercise's `rest_timer_seconds` in the optimistic data.
    - Add it to the actions object.

    **2. Make rest timer duration editable in `workout-exercise-card.tsx` (REST-02):**
    - Add a new prop `onUpdateRestTimer: (seconds: number) => Promise<void>` to `WorkoutExerciseCardProps`
    - Replace the read-only rest timer display (the `<span>` showing `{exercise.rest_timer_seconds}s` with the Timer icon) with a `Popover` from `@/components/ui/popover`:
      - Trigger: The existing Timer icon + seconds label, styled as a clickable button (add `cursor-pointer hover:text-foreground` to the span)
      - Content: A small popover with a list of preset rest durations as buttons: 30s, 60s, 90s, 120s, 180s, 300s. Highlight the currently selected value. Clicking a preset calls `onUpdateRestTimer(seconds)` and closes the popover.
      - Use `useState` for popover open state
    - Import `Popover, PopoverContent, PopoverTrigger` from `@/components/ui/popover`

    **3. Wire the new prop in `workout-logger.tsx`:**
    - Add `onUpdateRestTimer={(seconds) => actions.updateExerciseRestTimer(exercise.id, seconds)}` to each `<WorkoutExerciseCard>` in the exercises map

    **4. Add i18n keys for rest timer edit UI:**
    - Add to all three locale files under the `workouts` namespace:
      - `"restTimerEdit"`: en="Rest Timer", zh="休息计时器", zh-TW="休息計時器"
      - `"restTimerSeconds"`: en="{seconds}s", zh="{seconds}秒", zh-TW="{seconds}秒"
    - Use `t("restTimerEdit")` as the popover heading/label
    - Use the seconds values as button labels (just show "30s", "60s" etc. — these are numeric and don't need translation)
  </action>
  <verify>
    - `pnpm lint` passes with 0 errors
    - `pnpm test:run` passes (no regressions)
    - Grep confirms: `updateExerciseRestTimer` exists in use-active-workout.ts
    - Grep confirms: `Popover` is imported in workout-exercise-card.tsx
    - Grep confirms: `onUpdateRestTimer` prop exists on WorkoutExerciseCard
    - Grep confirms: `restTimerEdit` key exists in all three locale files
  </verify>
  <done>
    Rest timer duration on each exercise card is editable via a popover with preset durations. The selected duration is saved to the server and reflected in the UI immediately via optimistic update.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Gap 1 closed (WLOG-02):** `WorkoutAddExercise` is imported and rendered in workout-logger.tsx with proper state management. The "Add Exercise" button opens the sheet.
2. **Gap 2 closed (WLOG-07):** `WorkoutFinishDialog` is imported and rendered with dialog gating. Finish button opens the dialog, not calls finishWorkout directly. `WorkoutDiscardDialog` is also wired for the discard button.
3. **Gap 3 closed (REST-02):** Rest timer duration is editable per exercise via a Popover with preset values. The `updateExerciseRestTimer` action PATCHes the server and updates SWR cache optimistically.
4. All 1311+ existing tests pass with zero regressions.
5. Lint passes with 0 errors.
6. No orphaned components remain in the workout-logger directory.
</verification>

<success_criteria>
- All three VERIFICATION.md gaps resolved (WLOG-02, WLOG-07/partial, REST-02)
- Zero orphaned components in the workout logger (WorkoutAddExercise, WorkoutFinishDialog, WorkoutDiscardDialog all wired)
- `pnpm lint` and `pnpm test:run` pass
- i18n strings added to all three locales
</success_criteria>

<output>
After completion, create `.planning/phases/19-workout-logging-core-loop/19-07-SUMMARY.md`
</output>
