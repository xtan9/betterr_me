---
phase: 18-database-foundation-exercise-library
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - lib/db/exercises.ts
  - lib/db/index.ts
  - app/api/exercises/route.ts
  - app/api/exercises/[id]/route.ts
  - lib/hooks/use-exercises.ts
  - components/fitness/exercise-library/exercise-library.tsx
  - components/fitness/exercise-library/exercise-card.tsx
  - components/fitness/exercise-library/exercise-filter-bar.tsx
  - components/fitness/exercise-library/exercise-form.tsx
autonomous: true
requirements:
  - EXER-01
  - EXER-02
  - EXER-03
  - EXER-04
  - EXER-05

must_haves:
  truths:
    - "User can see a list of ~100 preset exercises organized by muscle group"
    - "User can search exercises by name and filter by muscle group or equipment without server round-trips"
    - "User can create a custom exercise that appears only for them"
    - "User can edit and delete their own custom exercises but cannot modify preset exercises"
    - "Each exercise displays its type which determines what tracking fields are shown"
  artifacts:
    - path: "lib/db/exercises.ts"
      provides: "ExercisesDB class with CRUD methods"
      exports: ["ExercisesDB"]
    - path: "lib/db/index.ts"
      provides: "Barrel export including ExercisesDB"
      contains: "ExercisesDB"
    - path: "app/api/exercises/route.ts"
      provides: "GET list and POST create exercise endpoints"
      exports: ["GET", "POST"]
    - path: "app/api/exercises/[id]/route.ts"
      provides: "GET, PATCH, DELETE single exercise endpoints"
      exports: ["GET", "PATCH", "DELETE"]
    - path: "lib/hooks/use-exercises.ts"
      provides: "useExercises SWR hook for client-side data fetching"
      exports: ["useExercises"]
    - path: "components/fitness/exercise-library/exercise-library.tsx"
      provides: "Main exercise library browsing component with search and filters"
    - path: "components/fitness/exercise-library/exercise-card.tsx"
      provides: "Individual exercise display card"
    - path: "components/fitness/exercise-library/exercise-filter-bar.tsx"
      provides: "Muscle group and equipment filter controls"
    - path: "components/fitness/exercise-library/exercise-form.tsx"
      provides: "Create and edit custom exercise form"
  key_links:
    - from: "components/fitness/exercise-library/exercise-library.tsx"
      to: "lib/hooks/use-exercises.ts"
      via: "useExercises() hook call"
      pattern: "useExercises"
    - from: "lib/hooks/use-exercises.ts"
      to: "app/api/exercises/route.ts"
      via: "SWR fetch to /api/exercises"
      pattern: "api/exercises"
    - from: "app/api/exercises/route.ts"
      to: "lib/db/exercises.ts"
      via: "ExercisesDB instantiation"
      pattern: "new ExercisesDB"
    - from: "lib/db/exercises.ts"
      to: "lib/db/types.ts"
      via: "imports Exercise, ExerciseInsert, ExerciseUpdate types"
      pattern: "import.*Exercise.*from.*types"
    - from: "components/fitness/exercise-library/exercise-library.tsx"
      to: "components/fitness/exercise-library/exercise-filter-bar.tsx"
      via: "Client-side filtering state passed to filter bar"
      pattern: "ExerciseFilterBar"
---

<objective>
Build the complete exercise library feature: database class, API routes, SWR hook, and UI components for browsing, searching, filtering, and managing exercises.

Purpose: Users need to browse the exercise library to find exercises for their workouts (Phase 19). The exercise picker is a core dependency for the workout logger. This plan delivers the full CRUD cycle for exercises.

Output: ExercisesDB class, exercise API routes (list, create, read, update, delete), useExercises SWR hook, and 4 exercise library UI components.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-database-foundation-exercise-library/18-RESEARCH.md
@.planning/phases/18-database-foundation-exercise-library/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExercisesDB class, API routes, and SWR hook</name>
  <files>
lib/db/exercises.ts
lib/db/index.ts
app/api/exercises/route.ts
app/api/exercises/[id]/route.ts
lib/hooks/use-exercises.ts
  </files>
  <action>
**lib/db/exercises.ts — ExercisesDB class:**

Follow the existing DB class pattern from `lib/db/categories.ts` and `lib/db/habits.ts`:
- Constructor takes `SupabaseClient`
- `getAllExercises(): Promise<Exercise[]>` — SELECT * ordered by name. RLS handles visibility (presets + user's custom). No explicit filter needed.
- `getExercise(id: string): Promise<Exercise | null>` — SELECT single by id. Return null on PGRST116 error.
- `createExercise(userId: string, exercise: Omit<ExerciseInsert, 'user_id' | 'is_custom'>): Promise<Exercise>` — INSERT with user_id and is_custom=true. Use `.select().single()`.
- `updateExercise(id: string, updates: ExerciseUpdate): Promise<Exercise>` — UPDATE by id. RLS enforces ownership + is_custom. Use `.select().single()`.
- `deleteExercise(id: string): Promise<void>` — DELETE by id. RLS enforces ownership. Catch FK violation error (code '23503') and throw a user-friendly error: "This exercise has been used in workouts and cannot be deleted."

Import types from `@/lib/db/types`. Follow exact code from RESEARCH.md "ExercisesDB Class" section.

**lib/db/index.ts:**
Add `export { ExercisesDB } from './exercises';` to the barrel file.

**app/api/exercises/route.ts — GET and POST:**

Follow the existing pattern from `app/api/categories/route.ts`:
- GET: auth check -> instantiate ExercisesDB(supabase) -> getAllExercises() -> return { exercises }
- POST: auth check -> validate body with exerciseFormSchema via validateRequestBody -> createExercise(user.id, data) -> return { exercise } with 201

Import `{ log }` from `@/lib/logger` for error logging (not console.error).
Import `{ validateRequestBody }` from `@/lib/validations/api`.
Import `{ exerciseFormSchema }` from `@/lib/validations/exercise`.

**app/api/exercises/[id]/route.ts — GET, PATCH, DELETE:**

Follow the existing pattern from `app/api/habits/[id]/route.ts`:
- GET: auth check -> getExercise(id) -> return 404 if null -> return { exercise }
- PATCH: auth check -> validate body with exerciseUpdateSchema -> updateExercise(id, data) -> return { exercise }. Catch RLS error and return 403 "Cannot modify preset exercises".
- DELETE: auth check -> deleteExercise(id) -> return 204. Catch FK violation and return 409 "This exercise has been used in workouts and cannot be deleted." Catch RLS error and return 403 "Cannot delete preset exercises."

Extract `id` from `params` using `const { id } = await params;` (Next.js 16 async params pattern — check existing [id] routes for the exact pattern used in this codebase).

**lib/hooks/use-exercises.ts — useExercises SWR hook:**

```typescript
import useSWR from "swr";
import { fetcher } from "@/lib/fetcher";
import type { Exercise } from "@/lib/db/types";

export function useExercises() {
  const { data, error, isLoading, mutate } = useSWR<{ exercises: Exercise[] }>(
    "/api/exercises",
    fetcher,
    { dedupingInterval: 600000 } // 10 min cache — exercises rarely change
  );
  return { exercises: data?.exercises ?? [], error, isLoading, mutate };
}
```

Note: No filters in the SWR key — load ALL exercises once, filter client-side. This matches the architecture decision from RESEARCH.md (80-120 exercises fit in memory).

Run `pnpm lint` after creating all files.
  </action>
  <verify>
Run `pnpm build` to confirm compilation. Run `pnpm lint` to confirm no lint errors. Run `pnpm test:run` to confirm no existing tests broken. Verify:
- ExercisesDB has 5 methods (getAllExercises, getExercise, createExercise, updateExercise, deleteExercise)
- GET /api/exercises returns { exercises: Exercise[] }
- POST /api/exercises validates body and returns { exercise } with 201
- GET/PATCH/DELETE /api/exercises/[id] handle not-found, auth errors
- useExercises returns { exercises, error, isLoading, mutate }
  </verify>
  <done>ExercisesDB class with 5 CRUD methods. Exercise API routes (GET list, POST create, GET/PATCH/DELETE single). useExercises SWR hook with 10-min dedup. All following existing codebase patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Build exercise library UI components</name>
  <files>
components/fitness/exercise-library/exercise-library.tsx
components/fitness/exercise-library/exercise-card.tsx
components/fitness/exercise-library/exercise-filter-bar.tsx
components/fitness/exercise-library/exercise-form.tsx
  </files>
  <action>
**All components are "use client" components.**

**exercise-card.tsx — ExerciseCard component:**
- Props: `exercise: Exercise`, `onEdit?: (exercise: Exercise) => void`, `onDelete?: (exercise: Exercise) => void`
- Display: Exercise name as title, muscle_group_primary as badge, equipment as secondary badge, exercise_type as small label
- If exercise.is_custom: show edit/delete action buttons (3-dot menu or icon buttons). If preset: no action buttons.
- Use shadcn Card, Badge components
- Use `useTranslations("exercises")` for i18n strings
- Show `muscle_groups_secondary` as smaller secondary badges if non-empty
- Use `EXERCISE_FIELD_MAP` from `@/lib/fitness/exercise-fields` to show what fields this exercise tracks (e.g., "Weight + Reps" for weight_reps)

**exercise-filter-bar.tsx — ExerciseFilterBar component:**
- Props: `search: string`, `onSearchChange: (v: string) => void`, `muscleGroup: MuscleGroup | null`, `onMuscleGroupChange: (v: MuscleGroup | null) => void`, `equipment: Equipment | null`, `onEquipmentChange: (v: Equipment | null) => void`
- Display: Text input for search (with Search icon from lucide-react), Select dropdown for muscle group filter (with "All" option), Select dropdown for equipment filter (with "All" option)
- Use shadcn Input and Select components
- Use `useTranslations("exercises")` for filter labels
- MuscleGroup options: all 16 values from the type. Equipment options: all 9 values.

**exercise-library.tsx — ExerciseLibrary component:**
- Main exercise browsing component. Uses `useExercises()` hook to load all exercises.
- Local state: `search` (string), `muscleGroup` (MuscleGroup | null), `equipment` (Equipment | null), `isFormOpen` (boolean), `editingExercise` (Exercise | null)
- Client-side filtering: `exercises.filter()` matching search (case-insensitive name includes), muscleGroup (exact match on muscle_group_primary), equipment (exact match). This is the key EXER-02 implementation — no server round-trips.
- Group filtered exercises by muscle_group_primary for organized display. Show group headers.
- Render `ExerciseFilterBar` at top, `ExerciseCard` for each exercise.
- "Create Custom Exercise" button that opens the exercise form dialog.
- Handle edit: set editingExercise and open form. Handle delete: confirm dialog, then `fetch DELETE /api/exercises/${id}`, then `mutate()` to refresh.
- Show loading skeleton while isLoading. Show empty state if no exercises match filters.
- Use `useTranslations("exercises")` for all UI strings.

**exercise-form.tsx — ExerciseForm component:**
- Props: `open: boolean`, `onOpenChange: (v: boolean) => void`, `exercise?: Exercise` (if editing), `onSaved: () => void` (callback to refresh list)
- Use react-hook-form with zodResolver(exerciseFormSchema)
- Fields: name (Input), muscle_group_primary (Select), muscle_groups_secondary (multi-select or checkboxes), equipment (Select), exercise_type (Select)
- On submit: if exercise prop exists, PATCH `/api/exercises/${exercise.id}`; else POST `/api/exercises`
- Use shadcn Dialog, Form, Input, Select, Button components
- Use `useTranslations("exercises")` for form labels, buttons, validation messages
- Close dialog and call onSaved on success. Show toast on error via sonner.
- Pre-fill form when editing an existing exercise.

Run `pnpm lint` after creating all files. Fix any lint issues.
  </action>
  <verify>
Run `pnpm build` to confirm all components compile. Run `pnpm lint` to confirm no lint errors. Verify:
- ExerciseLibrary renders with search input, filter dropdowns, and exercise cards
- Client-side filtering works: searching by name filters instantly, selecting muscle group/equipment narrows results
- Create form opens in dialog, submits to POST /api/exercises
- Edit form opens with pre-filled data, submits to PATCH /api/exercises/[id]
- Delete triggers confirmation, calls DELETE /api/exercises/[id]
- Preset exercises show no edit/delete buttons
- Custom exercises show edit/delete buttons
  </verify>
  <done>Exercise library UI with: browsable list grouped by muscle group, instant client-side search and filter (no server round-trips), create/edit custom exercise dialog form, delete with confirmation. Preset exercises are read-only. Custom exercises are fully editable.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds
2. `pnpm lint` passes
3. `pnpm test:run` passes — no regressions
4. ExercisesDB has getAllExercises, getExercise, createExercise, updateExercise, deleteExercise
5. GET /api/exercises returns exercise list
6. POST /api/exercises creates custom exercise with Zod validation
7. PATCH/DELETE /api/exercises/[id] work for custom, reject for preset
8. useExercises() returns exercises array with 10-min SWR dedup
9. Exercise library renders ~100 exercises grouped by muscle group
10. Search filters exercises client-side without API calls
11. Muscle group and equipment dropdowns narrow exercise list
12. Create/edit form uses react-hook-form + exerciseFormSchema
13. Delete shows confirmation and calls API
</verification>

<success_criteria>
- User can browse ~100 preset exercises organized by muscle group
- User can search by name and filter by muscle group/equipment — all client-side, instant
- User can create custom exercises with validation
- User can edit and delete their own custom exercises
- Preset exercises cannot be modified or deleted
- Each exercise shows its type and what it tracks
</success_criteria>

<output>
After completion, create `.planning/phases/18-database-foundation-exercise-library/18-02-SUMMARY.md`
</output>
