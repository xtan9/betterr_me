---
phase: 18-database-foundation-exercise-library
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/workouts/exercises/page.tsx
  - app/workouts/page.tsx
autonomous: true
gap_closure: true
requirements:
  - EXER-01
  - EXER-02
  - EXER-03
  - EXER-04
  - EXER-05
  - SETT-01
  - SETT-02
  - SETT-03

must_haves:
  truths:
    - "User can navigate to /workouts/exercises and see the exercise library"
    - "User can browse ~80-120 preset exercises organized by muscle group"
    - "User can search exercises by name and filter by muscle group or equipment"
    - "User can create a custom exercise via the Create button"
    - "User can edit and delete their own custom exercises"
  artifacts:
    - path: "app/workouts/exercises/page.tsx"
      provides: "Exercise library page route that mounts ExerciseLibrary component"
      min_lines: 8
  key_links:
    - from: "app/workouts/exercises/page.tsx"
      to: "components/fitness/exercise-library/exercise-library.tsx"
      via: "import and render ExerciseLibrary"
      pattern: "ExerciseLibrary"
    - from: "app/workouts/page.tsx"
      to: "app/workouts/exercises/page.tsx"
      via: "link to exercises sub-page"
      pattern: "/workouts/exercises"
---

<objective>
Mount the fully-implemented ExerciseLibrary component on a page route so users can browse, search, and manage exercises.

Purpose: Close all 4 verification gaps from 18-VERIFICATION.md. The exercise library backend (DB class, API routes, SWR hook) and frontend components (ExerciseLibrary, ExerciseCard, ExerciseFilterBar, ExerciseForm) are fully built but orphaned — no page route renders them.

Output: `app/workouts/exercises/page.tsx` page route + updated `/workouts` landing page with link to exercise library.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-database-foundation-exercise-library/18-VERIFICATION.md
@.planning/phases/18-database-foundation-exercise-library/18-02-SUMMARY.md

Key existing files:
@components/fitness/exercise-library/exercise-library.tsx — Fully implemented client component with browse/search/filter/create/edit/delete
@app/workouts/page.tsx — Current placeholder "coming soon" page
@app/workouts/layout.tsx — SidebarShell wrapper (covers all /workouts/* routes)
@i18n/messages/en.json — Already has exercises.title ("Exercises") and workouts.* keys
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exercise library page route and update workouts landing</name>
  <files>
    app/workouts/exercises/page.tsx
    app/workouts/page.tsx
  </files>
  <action>
    **1. Create `app/workouts/exercises/page.tsx`:**

    This is a server component page that renders the client-side ExerciseLibrary component within the standard page layout.

    ```tsx
    import { getTranslations } from "next-intl/server";
    import { PageHeader } from "@/components/layouts/page-header";
    import { ExerciseLibrary } from "@/components/fitness/exercise-library/exercise-library";

    export default async function ExercisesPage() {
      const t = await getTranslations("exercises");

      return (
        <div className="space-y-6">
          <PageHeader title={t("title")} />
          <ExerciseLibrary />
        </div>
      );
    }
    ```

    Key details:
    - Server component wrapper (no "use client" directive) that renders the client ExerciseLibrary
    - Uses `getTranslations("exercises")` for the page title (key `exercises.title` = "Exercises")
    - Follows the same `space-y-6` + `PageHeader` pattern as `/workouts/page.tsx`
    - The parent `app/workouts/layout.tsx` already provides `SidebarShell`, so no layout needed

    **2. Update `app/workouts/page.tsx`:**

    Replace the "coming soon" placeholder with a card that links to the exercise library. This makes the exercise library discoverable from the workouts landing page.

    Replace the existing Card content with a link to `/workouts/exercises`:
    - Keep the `PageHeader` with `workouts.title`
    - Add a card or link section for "Exercise Library" that navigates to `/workouts/exercises`
    - Use the i18n key `workouts.comingSoon` for the workout logging section (still coming in Phase 19)
    - Import `Link` from `next/link` for client-side navigation

    The workouts page should show:
    1. PageHeader with workouts title
    2. An "Exercise Library" card with a brief description and a link/button to `/workouts/exercises`
    3. Keep a "coming soon" section for workout logging (Phase 19)

    Use i18n keys that already exist: `exercises.title`, `workouts.comingSoon`, `workouts.comingSoonDescription`. If a key for "Browse and manage exercises" is needed, use `exercises.searchPlaceholder` or hardcode a brief English string wrapped in a translation call. Check `i18n/messages/en.json` for available keys under `exercises.*` and `workouts.*`.
  </action>
  <verify>
    1. `pnpm build` succeeds without errors
    2. `pnpm lint` passes
    3. `pnpm test:run` — all tests pass (no regressions)
    4. Verify `app/workouts/exercises/page.tsx` exists and imports ExerciseLibrary
    5. Verify `grep -rn "ExerciseLibrary" app/` shows the new page file importing the component
  </verify>
  <done>
    - `app/workouts/exercises/page.tsx` exists and renders `<ExerciseLibrary />`
    - `/workouts` landing page links to `/workouts/exercises`
    - Build and lint pass
    - All existing tests pass with no regressions
    - The ExerciseLibrary component is no longer orphaned — it has a page route
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds — confirms the new page route compiles and all imports resolve
2. `pnpm lint` passes — no lint errors in new or modified files
3. `pnpm test:run` — all existing tests pass (no regressions from the page addition)
4. `grep -rn "ExerciseLibrary" app/` — confirms the component is imported in the new page
5. `ls app/workouts/exercises/page.tsx` — confirms the page file exists on disk
</verification>

<success_criteria>
- The ExerciseLibrary component is mounted on the `/workouts/exercises` route
- Users navigating to `/workouts/exercises` see the exercise library with browse, search, filter, create, edit, and delete functionality
- The `/workouts` landing page provides navigation to the exercise library
- All 4 verification gaps (truths 1-4 from 18-VERIFICATION.md) are closed by this single page route addition
- Build, lint, and tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-database-foundation-exercise-library/18-04-SUMMARY.md`
</output>
