---
phase: 18-database-foundation-exercise-library
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - lib/validations/preferences.ts
  - components/settings/weight-unit-selector.tsx
  - components/settings/settings-content.tsx
  - components/layouts/app-sidebar.tsx
  - app/workouts/page.tsx
  - app/workouts/layout.tsx
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03

must_haves:
  truths:
    - "User can select kg or lbs in Settings and the preference is saved to their profile"
    - "Weight unit selector shows the user's current preference and updates persist across page refresh"
    - "Workouts appears as a top-level sidebar nav item with Dumbbell icon"
    - "Clicking Workouts nav item navigates to /workouts placeholder page"
    - "All new UI strings are translated in en, zh, and zh-TW"
  artifacts:
    - path: "components/settings/weight-unit-selector.tsx"
      provides: "kg/lbs toggle component for settings page"
    - path: "components/settings/settings-content.tsx"
      provides: "Settings page with weight unit card added"
      contains: "WeightUnitSelector"
    - path: "components/layouts/app-sidebar.tsx"
      provides: "Sidebar with Workouts nav item using Dumbbell icon"
      contains: "Dumbbell"
    - path: "app/workouts/page.tsx"
      provides: "Placeholder workouts landing page"
    - path: "app/workouts/layout.tsx"
      provides: "Layout wrapper for workouts section"
    - path: "i18n/messages/en.json"
      provides: "English translations for workouts, exercises, settings.weightUnit"
      contains: "workouts"
    - path: "i18n/messages/zh.json"
      provides: "Chinese translations"
      contains: "workouts"
    - path: "i18n/messages/zh-TW.json"
      provides: "Traditional Chinese translations"
      contains: "workouts"
  key_links:
    - from: "components/settings/settings-content.tsx"
      to: "components/settings/weight-unit-selector.tsx"
      via: "imports and renders WeightUnitSelector"
      pattern: "WeightUnitSelector"
    - from: "components/settings/weight-unit-selector.tsx"
      to: "/api/profile/preferences"
      via: "PATCH request to save weight_unit preference"
      pattern: "api/profile/preferences"
    - from: "components/layouts/app-sidebar.tsx"
      to: "app/workouts/page.tsx"
      via: "href='/workouts' in mainNavItems"
      pattern: "href.*workouts"
---

<objective>
Add weight unit preference to Settings, add Workouts to sidebar navigation, create placeholder workouts page, and add i18n strings for all Phase 18 features.

Purpose: SETT-01/02 ensure users can choose kg/lbs before any workout data exists. SETT-03 makes the Workouts section discoverable in navigation. i18n strings enable all Phase 18 UI to render in all 3 locales.

Output: Weight unit selector in settings, Workouts sidebar nav item with Dumbbell icon, placeholder /workouts page, and i18n strings in all 3 locale files.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-database-foundation-exercise-library/18-RESEARCH.md
@.planning/phases/18-database-foundation-exercise-library/18-01-SUMMARY.md
@components/settings/settings-content.tsx
@components/layouts/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add weight unit preference to Settings</name>
  <files>
components/settings/weight-unit-selector.tsx
components/settings/settings-content.tsx
  </files>
  <action>
**components/settings/weight-unit-selector.tsx — WeightUnitSelector component:**

"use client" component. Follow the exact pattern of the existing WeekStartSelector in settings-content.tsx:

- Props: `value: "kg" | "lbs"`, `onChange: (unit: "kg" | "lbs") => void`, `disabled?: boolean`
- Render two buttons (or a RadioGroup or SegmentedControl) for "kg" and "lbs"
- Active selection uses primary styling (bg-primary text-primary-foreground), inactive uses outline
- Use `useTranslations("settings")` for labels: `t("weightUnit.kg")` and `t("weightUnit.lbs")`
- The component is a controlled input — parent manages state and save logic
- Use shadcn Button components with variant="outline" for inactive and variant="default" for active
- Layout: horizontal row of 2 buttons, similar to how week start day selector works

**components/settings/settings-content.tsx — Add weight unit card:**

Read the existing file first to understand its structure. Then:

1. Import `WeightUnitSelector` from `@/components/settings/weight-unit-selector`
2. Add state: `const [weightUnit, setWeightUnit] = useState<"kg" | "lbs">("kg");`
3. Initialize from profile data: in the existing useEffect that reads profile preferences, add `setWeightUnit(data.profile.preferences.weight_unit ?? "kg")` — use nullish coalescing per Pitfall 3 in RESEARCH.md (existing users won't have this field)
4. Add a new Card section after the existing "Week Start Day" card (or within the same Preferences card if it's a single card). The card should have:
   - Title: `t("weightUnit.title")` — "Weight Unit"
   - Description: `t("weightUnit.description")` — "Choose your preferred weight unit for workout tracking"
   - The WeightUnitSelector component
5. Include `weight_unit: weightUnit` in the save payload alongside existing preferences (week_start_day, etc.)

IMPORTANT: The existing preferences save logic already calls `PATCH /api/profile/preferences`. The preferencesSchema was already extended with weight_unit in Plan 01. So just add weight_unit to the save body — no API changes needed.

IMPORTANT: Use `?? "kg"` when reading weight_unit from preferences to handle existing users who don't have the field yet (Pitfall 3).
  </action>
  <verify>
Run `pnpm build` and `pnpm lint`. Run `pnpm test:run` to ensure existing settings tests still pass. Verify:
- WeightUnitSelector renders kg and lbs buttons
- Active button has primary styling
- Settings page shows weight unit card
- Weight unit preference saves via existing preferences PATCH endpoint
- Default is "kg" when existing user has no weight_unit in preferences
  </verify>
  <done>Settings page has a weight unit selector card. Users can choose kg or lbs. Preference saves to profile via existing PATCH /api/profile/preferences. Defaults to kg for existing users.</done>
</task>

<task type="auto">
  <name>Task 2: Add Workouts sidebar nav item and placeholder page</name>
  <files>
components/layouts/app-sidebar.tsx
app/workouts/page.tsx
app/workouts/layout.tsx
  </files>
  <action>
**components/layouts/app-sidebar.tsx — Add Workouts nav item:**

1. Add import: `import { Dumbbell } from "lucide-react";` (lucide-react 0.511.0 is already installed)
2. Add to `mainNavItems` array as the 4th item (after tasks):
```typescript
{ href: "/workouts", icon: Dumbbell, labelKey: "workouts", match: (p: string) => p.startsWith("/workouts") },
```

The existing nav rendering code iterates over mainNavItems generically — no other changes needed. The badge counts system is keyed by labelKey, and "workouts" will simply not have a badge (returns undefined, which is handled gracefully).

**app/workouts/layout.tsx — Layout for workouts section:**

Follow the existing pattern from `app/habits/layout.tsx` or `app/tasks/layout.tsx`:
- Server component (no "use client")
- Import `SidebarShell` from `@/components/layouts/sidebar-shell`
- Wrap children in SidebarShell

```typescript
import { SidebarShell } from "@/components/layouts/sidebar-shell";

export default function WorkoutsLayout({ children }: { children: React.ReactNode }) {
  return <SidebarShell>{children}</SidebarShell>;
}
```

Check the exact pattern used by existing layout files (habits/tasks) and follow it precisely.

**app/workouts/page.tsx — Placeholder workouts page:**

Server component. Minimal placeholder for Phase 19:
- Import `PageHeader` from `@/components/layouts/page-header`
- Import `getTranslations` from `next-intl/server`
- Show PageHeader with title from `t("workouts.title")` — "Workouts"
- Show a Card with centered message: "Workout tracking coming soon" (translated)
- This page will be replaced with the full workout history + "Start Workout" button in Phase 19

Do NOT add workout counts to the sidebar counts endpoint (per Pitfall 5 in RESEARCH.md).
  </action>
  <verify>
Run `pnpm build` and `pnpm lint`. Run `pnpm test:run` to ensure sidebar tests still pass (they test for 3 nav items; update test assertions if needed to expect 4). Verify:
- Sidebar shows 4 nav items: Dashboard, Habits, Tasks, Workouts
- Workouts nav item has Dumbbell icon
- Clicking Workouts navigates to /workouts
- /workouts page renders with PageHeader and placeholder content
- Active state works when pathname starts with /workouts
  </verify>
  <done>Sidebar shows Workouts as 4th nav item with Dumbbell icon. /workouts page renders with placeholder content. Layout wraps content in SidebarShell.</done>
</task>

<task type="auto">
  <name>Task 3: Add i18n strings for all Phase 18 features</name>
  <files>
i18n/messages/en.json
i18n/messages/zh.json
i18n/messages/zh-TW.json
  </files>
  <action>
Add translation keys to ALL THREE locale files. These strings support the exercise library, settings weight unit, and workouts navigation.

**New keys to add (organized by namespace):**

1. **nav namespace** (or wherever existing nav labels live — check the file):
   - `"workouts": "Workouts"` / `"workouts": "训练"` / `"workouts": "訓練"`

2. **settings namespace** (extend existing):
   - `"weightUnit.title": "Weight Unit"` / `"weightUnit.title": "重量单位"` / `"weightUnit.title": "重量單位"`
   - `"weightUnit.description": "Choose your preferred weight unit for workout tracking"` / Chinese equivalents
   - `"weightUnit.kg": "Kilograms (kg)"` / `"weightUnit.kg": "千克 (kg)"` / `"weightUnit.kg": "千克 (kg)"`
   - `"weightUnit.lbs": "Pounds (lbs)"` / `"weightUnit.lbs": "磅 (lbs)"` / `"weightUnit.lbs": "磅 (lbs)"`

3. **exercises namespace** (new):
   - `"title": "Exercises"` / `"title": "动作库"` / `"title": "動作庫"`
   - `"searchPlaceholder": "Search exercises..."` / Chinese equivalents
   - `"filterMuscleGroup": "Muscle Group"` / Chinese equivalents
   - `"filterEquipment": "Equipment"` / Chinese equivalents
   - `"allMuscleGroups": "All Muscle Groups"` / Chinese equivalents
   - `"allEquipment": "All Equipment"` / Chinese equivalents
   - `"createCustom": "Create Custom Exercise"` / Chinese equivalents
   - `"editExercise": "Edit Exercise"` / Chinese equivalents
   - `"deleteExercise": "Delete Exercise"` / Chinese equivalents
   - `"deleteConfirmTitle": "Delete Exercise"` / Chinese equivalents
   - `"deleteConfirmDescription": "Are you sure you want to delete this exercise? This action cannot be undone."` / Chinese equivalents
   - `"deleteSuccess": "Exercise deleted"` / Chinese equivalents
   - `"saveSuccess": "Exercise saved"` / Chinese equivalents
   - `"createSuccess": "Exercise created"` / Chinese equivalents
   - `"name": "Name"` / Chinese equivalents
   - `"muscleGroup": "Primary Muscle Group"` / Chinese equivalents
   - `"secondaryMuscles": "Secondary Muscles"` / Chinese equivalents
   - `"equipment": "Equipment"` / Chinese equivalents
   - `"exerciseType": "Exercise Type"` / Chinese equivalents
   - `"custom": "Custom"` / Chinese equivalents
   - `"noResults": "No exercises found"` / Chinese equivalents
   - `"noResultsDescription": "Try adjusting your search or filters"` / Chinese equivalents
   - Muscle group labels (16): "Chest", "Back", "Shoulders", "Biceps", "Triceps", "Forearms", "Core", "Quadriceps", "Hamstrings", "Glutes", "Calves", "Traps", "Lats", "Full Body", "Cardio", "Other"
   - Equipment labels (9): "Barbell", "Dumbbell", "Machine", "Bodyweight", "Kettlebell", "Cable", "Band", "Other", "None"
   - Exercise type labels (8): "Weight + Reps", "Bodyweight Reps", "Weighted Bodyweight", "Assisted Bodyweight", "Duration", "Duration + Weight", "Distance + Duration", "Weight + Distance"

4. **workouts namespace** (new, minimal for placeholder):
   - `"title": "Workouts"` / `"title": "训练"` / `"title": "訓練"`
   - `"comingSoon": "Workout tracking coming soon"` / `"comingSoon": "训练追踪功能即将推出"` / `"comingSoon": "訓練追蹤功能即將推出"`
   - `"comingSoonDescription": "Track your workouts, sets, and personal records."` / Chinese equivalents

IMPORTANT: Read each locale file first to understand the existing structure and key organization. Add new namespaces at the appropriate location (alphabetical or after existing feature namespaces). Do NOT modify existing keys. The Chinese translations should be accurate — use standard fitness terminology (e.g., 杠铃 for barbell, 哑铃 for dumbbell, 卧推 for bench press pattern).

Run `pnpm lint` after modifying all 3 files.
  </action>
  <verify>
Run `pnpm build` and `pnpm lint`. Run `pnpm test:run`. Verify:
- All 3 locale files parse as valid JSON
- The "workouts" nav key exists in all 3 files
- The "exercises" namespace exists in all 3 files with all required keys
- The "settings.weightUnit" keys exist in all 3 files
- No existing keys were modified
- Build succeeds (next-intl validates keys at build time)
  </verify>
  <done>All Phase 18 i18n strings added to en.json, zh.json, and zh-TW.json. Covers: workouts nav label, exercise library (search, filter, form, muscle groups, equipment, exercise types), settings weight unit, and workouts placeholder page.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds
2. `pnpm lint` passes
3. `pnpm test:run` passes — sidebar tests may need assertion update for 4th nav item
4. Settings page shows weight unit selector (kg/lbs)
5. Weight unit preference saves to /api/profile/preferences
6. Sidebar shows 4 nav items with Workouts using Dumbbell icon
7. /workouts page renders with PageHeader and placeholder
8. All 3 locale files have workouts, exercises, settings.weightUnit keys
9. No existing i18n keys modified
</verification>

<success_criteria>
- User can select kg or lbs in Settings — preference persists after page refresh
- Workouts appears in sidebar with Dumbbell icon, navigates to /workouts
- /workouts placeholder page renders correctly
- All UI strings are translated in en, zh, zh-TW
</success_criteria>

<output>
After completion, create `.planning/phases/18-database-foundation-exercise-library/18-03-SUMMARY.md`
</output>
