---
phase: 35.1-reminder-cron-preferences-polish
verified: 2026-04-03T11:25:00Z
status: passed
score: 14/14 must-haves verified
re_verification: false
---

# Phase 35.1: Reminder Cron, Preferences & Polish Verification Report

**Phase Goal:** Cron-based reminder dispatch, smart defaults, quiet hours, user preferences, responsive mobile layout, and i18n.
**Verified:** 2026-04-03T11:25:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Cron job dispatches pending reminders via push and email channels | VERIFIED | `app/api/cron/dispatch-reminders/route.ts` calls `sendPushNotification` and `sendReminderEmail`; `vercel.json` schedules `* * * * *` |
| 2 | Quiet hours prevent push notifications between user-configured start/end times | VERIFIED | `isInQuietHours()` in `lib/push/quiet-hours.ts` correctly handles overnight wrap; cron skips push when in quiet hours |
| 3 | Failed deliveries are logged with status='failed' | VERIFIED | Cron route calls `updateReminderStatus(…, 'failed')` when all channels fail |
| 4 | Push during quiet hours remains status='pending' for later dispatch | VERIFIED | Cron skips reminder (no status update) when `channelsToSend.length === 0` |
| 5 | Expired push subscriptions (410) are cleaned up automatically | VERIFIED | `send.ts` catches 410, calls `deleteSubscription(userId, sub.endpoint)` |
| 6 | User can create/read/update/delete reminders via API | VERIFIED | `app/api/reminders/route.ts` (GET, POST), `app/api/reminders/[id]/route.ts` (PATCH, DELETE); 21 tests passing |
| 7 | fire_at is pre-computed on create based on reminder type and event start | VERIFIED | `computeFireAt()` in `lib/reminders/fire-at.ts` called in POST /api/reminders |
| 8 | When a calendar event is rescheduled, pending relative reminders have fire_at recomputed | VERIFIED | `app/api/calendar-events/[id]/route.ts` imports `RemindersDB` and `computeFireAt`, recomputes on start_date/start_time change |
| 9 | User sees reminder rows within event dialog with smart defaults for new events | VERIFIED | `event-dialog.tsx` imports `ReminderRows`, `SMART_DEFAULTS`; populates 15min/push on new event creation |
| 10 | User can configure quiet hours in Settings | VERIFIED | `QuietHoursSettings` in `notification-settings.tsx`; PATCH /api/profile with quiet_hours_start/end |
| 11 | User can customize default reminders per source type in Settings | VERIFIED | `ReminderDefaultsSettings` in `notification-settings.tsx`; PUT /api/reminder-defaults with dirty tracking |
| 12 | Calendar sidebar collapses on mobile, accessible via Sheet drawer | VERIFIED | `calendar-header.tsx` has `SheetTrigger` with `md:hidden` Menu button; sidebar hidden via `hidden lg:block` in page content |
| 13 | User can swipe left/right on day view to navigate between days | VERIFIED | `day-view.tsx` imports `useSwipe`; spread swipe handlers on outer wrapper |
| 14 | All reminder and mobile calendar UI strings exist in en, zh, zh-TW | VERIFIED | `calendar.reminders.*`, `settings.notifications.quietHours.*`, `settings.notifications.reminderDefaults.*` present in all three locales |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/push/send.ts` | sendPushNotification() utility | VERIFIED | 88 lines, exports `sendPushNotification` and `PushPayload` |
| `lib/push/quiet-hours.ts` | isInQuietHours() check | VERIFIED | 39 lines, exports `isInQuietHours`, handles overnight wrap |
| `app/api/cron/dispatch-reminders/route.ts` | Vercel Cron dispatch endpoint | VERIFIED | 129 lines, exports `GET`, CRON_SECRET auth, per-reminder try/catch |
| `vercel.json` | Cron schedule config | VERIFIED | Contains `"path": "/api/cron/dispatch-reminders"`, `"schedule": "* * * * *"` |
| `lib/reminders/fire-at.ts` | computeFireAt utility | VERIFIED | 45 lines, exports `computeFireAt` and `FireAtInput` |
| `app/api/reminders/route.ts` | GET and POST for reminders | VERIFIED | Exports `GET` and `POST`, uses `computeFireAt` and `reminderCreateSchema` |
| `app/api/reminders/[id]/route.ts` | PATCH and DELETE for individual reminders | VERIFIED | Exports `PATCH` and `DELETE`, uses `reminderUpdateSchema` |
| `app/api/reminder-defaults/route.ts` | GET and PUT for reminder defaults | VERIFIED | Exports `GET` and `PUT`, uses `ReminderDefaultsDB` |
| `lib/db/reminders.ts` | updateReminder and deleteReminder methods | VERIFIED | Lines 55 (`updateReminder`) and 67 (`deleteReminder`) present |
| `components/calendar/reminder-rows.tsx` | Reminder row management component | VERIFIED | 247 lines, exports `ReminderRows`, `ReminderRowData`, `SMART_DEFAULTS`, `RELATIVE_PRESETS` |
| `components/settings/quiet-hours-settings.tsx` | Quiet hours time picker UI | VERIFIED | Exports `QuietHoursSettings`, uses SWR, type="time" inputs |
| `components/settings/reminder-defaults-settings.tsx` | Per-source-type default config UI | VERIFIED | Exports `ReminderDefaultsSettings`, fetches `/api/reminder-defaults`, dirty tracking |
| `hooks/use-swipe.ts` | useSwipe hook for touch navigation | VERIFIED | 52 lines, exports `useSwipe`, returns `onTouchStart`/`onTouchEnd` |
| `components/calendar/calendar-page-content.tsx` | FAB and mobile default view logic | VERIFIED | `fixed bottom-6 right-6` FAB with `md:hidden`, `window.matchMedia("(min-width: 768px)")` for mobile default |
| `i18n/messages/en.json` | English translations for reminders/quiet hours/mobile | VERIFIED | `calendar.reminders.*` (15 keys), `quietHours.*` (10 keys), `reminderDefaults.*` (13 keys) |
| `i18n/messages/zh.json` | Chinese Simplified translations | VERIFIED | All same key paths present; `quietHours.title = '免打扰时段'` |
| `i18n/messages/zh-TW.json` | Chinese Traditional translations | VERIFIED | All same key paths present; `quietHours.title = '勿擾時段'` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app/api/cron/dispatch-reminders/route.ts` | `lib/push/send.ts` | import sendPushNotification | WIRED | Line 5: `import { sendPushNotification }`, called at line 68 |
| `app/api/cron/dispatch-reminders/route.ts` | `lib/email/send.ts` | import sendReminderEmail | WIRED | Line 6: `import { sendReminderEmail }`, called at line 81 |
| `app/api/cron/dispatch-reminders/route.ts` | `lib/push/quiet-hours.ts` | import isInQuietHours | WIRED | Line 7: `import { isInQuietHours }`, called at line 45 |
| `app/api/reminders/route.ts` | `lib/db/reminders.ts` | RemindersDB | WIRED | `import { RemindersDB } from "@/lib/db"`, `new RemindersDB(supabase)` |
| `app/api/reminders/route.ts` | `lib/reminders/fire-at.ts` | computeFireAt | WIRED | `import { computeFireAt }`, called in POST handler |
| `app/api/reminder-defaults/route.ts` | `lib/db/reminder-defaults.ts` | ReminderDefaultsDB | WIRED | `import { ReminderDefaultsDB } from "@/lib/db"`, `new ReminderDefaultsDB(supabase)` |
| `app/api/calendar-events/[id]/route.ts` | `lib/db/reminders.ts` | RemindersDB for reschedule | WIRED | Line 3: imports `RemindersDB`, line 131: `new RemindersDB(supabase)`, uses `getRemindersBySource` and `computeFireAt` |
| `components/calendar/event-dialog.tsx` | `components/calendar/reminder-rows.tsx` | import ReminderRows | WIRED | Line 6: imports `ReminderRows`, `ReminderRowData`, `SMART_DEFAULTS`; rendered at line 401 |
| `components/calendar/event-dialog.tsx` | `/api/reminders` | fetch on save | WIRED | Lines 190-213: POST for new reminders, PATCH for existing, DELETE for removed |
| `components/settings/notification-settings.tsx` | `quiet-hours-settings.tsx` | import QuietHoursSettings | WIRED | Lines 21 and 145/247 |
| `components/settings/notification-settings.tsx` | `reminder-defaults-settings.tsx` | import ReminderDefaultsSettings | WIRED | Lines 22 and 146/248 |
| `components/calendar/day-view.tsx` | `hooks/use-swipe.ts` | import useSwipe | WIRED | Line 7: `import { useSwipe }`, spread handlers on outer wrapper |
| `components/calendar/calendar-header.tsx` | `components/calendar/calendar-sidebar.tsx` | Sheet trigger opens sidebar content | WIRED | `SheetContent` renders `mobileSidebar` ReactNode prop passed from `calendar-page-content.tsx` |
| `components/calendar/reminder-rows.tsx` | `i18n/messages/en.json` | useTranslations('calendar') | WIRED | All `t("reminders.*")` keys confirmed in en.json |
| `components/settings/quiet-hours-settings.tsx` | `i18n/messages/en.json` | useTranslations('settings.notifications') | WIRED | All `t("quietHours.*")` keys confirmed in en.json |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `QuietHoursSettings` | `profileData.profile.preferences` | SWR `/api/profile` | Yes — API reads from Supabase profiles table | FLOWING |
| `ReminderDefaultsSettings` | `data.defaults` | SWR `/api/reminder-defaults` | Yes — `ReminderDefaultsDB.getDefaults()` queries DB | FLOWING |
| `event-dialog.tsx` reminder rows | `reminderRows` state | `GET /api/reminders?source_type=calendar_event&source_id=` on dialog open for edit | Yes — `RemindersDB.getRemindersBySource()` queries DB | FLOWING |
| `app/api/cron/dispatch-reminders/route.ts` | `pending` | `RemindersDB.getPendingReminders(now)` | Yes — queries reminders table where status='pending' and fire_at <= now | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| sendPushNotification exports function | `node -e "require check"` | PASS | PASS |
| isInQuietHours exports function | file content check | PASS | PASS |
| computeFireAt exports function | file content check | PASS | PASS |
| vercel.json has cron entry | file content check | `"path": "/api/cron/dispatch-reminders"` found | PASS |
| locale JSON files parse valid | `node -e "require()"` | All 3 parse without errors | PASS |
| Plan 01 unit tests (29 tests) | `pnpm test:run` | 29/29 passing | PASS |
| Plan 02 unit tests (21 tests) | `pnpm test:run` | 21/21 passing | PASS |
| Plan 03 component tests (31 tests) | `pnpm test:run` | 31/31 passing | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REMN-01 | Plans 02, 03 | User can add multiple reminders per calendar event (relative or absolute) | SATISFIED | POST /api/reminders accepts relative and absolute types; ReminderRows allows adding multiple rows |
| REMN-02 | Plans 02, 03 | Relative reminders support 5/15/30/60/1440 min and custom | SATISFIED | `RELATIVE_PRESETS` in reminder-rows.tsx; custom input shown when value=-1 |
| REMN-03 | Plans 02, 03 | Absolute reminders support specific date+time | SATISFIED | `datetime-local` input in ReminderRows; `absolute_time` field in schema |
| REMN-04 | Plans 02, 03 | Each reminder can target push, email, or both channels | SATISFIED | Channel checkboxes in ReminderRows; `channels` array in schema |
| REMN-05 | Plans 02, 03 | Smart defaults auto-apply based on source type | SATISFIED | `SMART_DEFAULTS` in reminder-rows.tsx; auto-populated on new event in event-dialog.tsx |
| REMN-06 | Plans 02, 03 | User can customize default reminders per source type in settings | SATISFIED | `ReminderDefaultsSettings` component wired to PUT /api/reminder-defaults |
| REMN-07 | Plan 01, 03 | User can set quiet hours (no push between configurable start/end times) | SATISFIED | `QuietHoursSettings` + PATCH /api/profile; `isInQuietHours()` in cron route |
| REMN-08 | Plan 01 | Vercel Cron job runs every minute to dispatch pending reminders | SATISFIED | `vercel.json` with `"schedule": "* * * * *"`, cron endpoint `/api/cron/dispatch-reminders` |
| REMN-09 | Plan 02 | Reminders have fire_at pre-computed and recomputed on event reschedule | SATISFIED | `computeFireAt()` in POST /api/reminders; recomputed in calendar-events/[id] PATCH |
| REMN-10 | Plan 01 | Failed reminder deliveries logged with status='failed' | SATISFIED | Cron route calls `updateReminderStatus(…, 'failed')` when all channels fail |
| I18N-01 | Plan 05 | All calendar/reminder UI strings translated in en, zh, zh-TW | SATISFIED | 15+10+13 keys across `calendar.reminders`, `quietHours`, `reminderDefaults` namespaces in all 3 locales |
| RESP-01 | Plan 04 | On mobile, sidebar collapses; accessible via header drawer | SATISFIED | `hidden lg:block` sidebar; `SheetTrigger` with `md:hidden` Menu button in header. Note: uses `lg` breakpoint (1024px) not `sm` — deliberate decision per plan summary for better tablet UX |
| RESP-02 | Plan 04 | On mobile, default view is Day with swipe left/right navigation | SATISFIED | `window.matchMedia("(min-width: 768px)")` in page content sets day default on mobile; `useSwipe` in day-view.tsx |
| RESP-03 | Plan 04 | "+ New Event" becomes a floating action button on mobile | SATISFIED | `fixed bottom-6 right-6 z-50 md:hidden` FAB in calendar-page-content.tsx |

**Note on REQUIREMENTS.md status table:** REMN-07, REMN-08, REMN-10, RESP-01, RESP-02, RESP-03 show as "Pending" in `.planning/REQUIREMENTS.md` checkboxes and status table. The implementations are fully present and tested. The REQUIREMENTS.md file was not updated to reflect completion — this is a documentation gap, not an implementation gap.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `components/calendar/reminder-rows.tsx` | 166 | `placeholder={t("reminders.customMinutes")}` | Info | HTML input placeholder attribute — correct usage, not a stub |

No stub anti-patterns found. The single match is a legitimate HTML input placeholder attribute.

### Human Verification Required

#### 1. Mobile Sidebar Sheet Drawer

**Test:** On a mobile viewport (< 768px), open the calendar page. Verify the sidebar is hidden and a Menu icon button appears in the header. Tap the Menu button and verify the sidebar slides in from the left as a Sheet drawer.
**Expected:** Sidebar hidden on mobile, Sheet drawer opens with sidebar content on button tap.
**Why human:** Responsive layout behavior and Sheet animation require a real browser viewport.

#### 2. Day View Swipe Navigation

**Test:** On a touch device or browser simulator, open the calendar Day view. Swipe left and right on the time grid.
**Expected:** Swipe left navigates to next day; swipe right navigates to previous day. Vertical scrolling does not accidentally trigger navigation.
**Why human:** Touch event simulation with real threshold/direction behavior requires a device or browser dev tools.

#### 3. Floating Action Button on Mobile

**Test:** On a mobile viewport (< 768px), navigate to the calendar page.
**Expected:** A circular FAB button appears fixed at bottom-right of screen. Tapping it opens the event creation dialog.
**Why human:** Fixed positioning and mobile overlay behavior require visual inspection.

#### 4. Smart Defaults on New Event Creation

**Test:** Open the event dialog for a new event (no existing event).
**Expected:** A reminder row is pre-populated with "15 minutes before" and "Push" channel selected.
**Why human:** Requires browser interaction to trigger the useEffect that sets the initial reminder row.

#### 5. Quiet Hours End-to-End Behavior

**Test:** In Settings, enable quiet hours with start=22:00 end=07:00, save. Wait for cron dispatch during those hours.
**Expected:** Push-only reminders are not dispatched; email-only reminders still go through. After quiet hours end, push reminders should fire on the next cron run.
**Why human:** Requires real cron execution and cannot be tested with grep/static analysis alone.

### Gaps Summary

No gaps found. All 14 observable truths are verified. All 17 required artifacts exist, are substantive, and are wired. All 14 requirements (REMN-01 through REMN-10, I18N-01, RESP-01 through RESP-03) have implementation evidence.

One minor documentation note: the REQUIREMENTS.md status table has REMN-07, REMN-08, REMN-10, RESP-01, RESP-02, RESP-03 marked as "Pending" but the implementations are complete and tested. Consider updating REQUIREMENTS.md checkboxes to reflect completion.

---

_Verified: 2026-04-03T11:25:00Z_
_Verifier: Claude (gsd-verifier)_
