---
phase: 20-transaction-management-categorization
plan: 05
type: execute
wave: 4
depends_on: ["20-04"]
files_modified:
  - tests/components/money/transaction-list.test.tsx
  - tests/components/money/category-badge.test.tsx
  - tests/lib/validations/transactions.test.ts
autonomous: false
requirements: [TXNS-01, TXNS-02, TXNS-03, TXNS-04, TXNS-05, TXNS-06, TXNS-07, TXNS-08, CATG-01, CATG-02, CATG-03]

must_haves:
  truths:
    - "Component tests verify transaction list renders with date grouping and amounts"
    - "Validation schema tests cover transaction filter edge cases"
    - "All existing tests still pass (no regressions)"
    - "Human has verified the full transaction management flow visually"
  artifacts:
    - path: "tests/components/money/transaction-list.test.tsx"
      provides: "TransactionList component tests"
    - path: "tests/components/money/category-badge.test.tsx"
      provides: "CategoryBadge component tests"
    - path: "tests/lib/validations/transactions.test.ts"
      provides: "Transaction filter validation tests"
  key_links:
    - from: "tests/components/money/transaction-list.test.tsx"
      to: "components/money/transaction-list.tsx"
      via: "render and assert"
      pattern: "render.*TransactionList"
---

<objective>
Add component tests and validation tests for the Phase 20 transaction management features, then perform human verification of the complete flow.

Purpose: Ensure the transaction list, category badge, and filter validation work correctly via automated tests, and verify the full user experience visually.

Output: 3 test files, human-verified transaction management flow.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-transaction-management-categorization/20-03-SUMMARY.md
@.planning/phases/20-transaction-management-categorization/20-04-SUMMARY.md
@components/money/transaction-list.tsx
@components/money/category-badge.tsx
@lib/validations/transactions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create component tests and validation tests</name>
  <files>
    tests/components/money/transaction-list.test.tsx
    tests/components/money/category-badge.test.tsx
    tests/lib/validations/transactions.test.ts
  </files>
  <action>
**1. `tests/components/money/transaction-list.test.tsx`** — TransactionList component tests:

Mock pattern (same as existing money component tests like AccountsList):
```typescript
// Mock SWR hooks
const mockTransactions = vi.fn();
vi.mock("@/lib/hooks/use-transactions", () => ({
  useTransactions: (...args: unknown[]) => mockTransactions(...args),
}));

const mockFilters = vi.fn();
vi.mock("@/lib/hooks/use-transaction-filters", () => ({
  useTransactionFilters: () => mockFilters(),
}));

const mockCategories = vi.fn();
vi.mock("@/lib/hooks/use-categories", () => ({
  useCategories: () => mockCategories(),
}));

// Mock next-intl
vi.mock("next-intl", () => ({
  useTranslations: () => (key: string) => key,
}));
```

Tests (8-10):
- Renders loading skeleton when isLoading=true
- Renders empty state when transactions is empty and no filters active
- Renders "no results" message when transactions is empty and filters are active
- Renders transaction rows grouped by date
- Shows "Today" header for today's transactions
- Shows correct number of transaction rows
- Shows Load More button when hasMore=true
- Hides Load More button when hasMore=false
- Displays total count: "Showing X of Y transactions"
- Accessibility: no violations (`expect(await axe(container)).toHaveNoViolations()`)

**2. `tests/components/money/category-badge.test.tsx`** — CategoryBadge tests:

Tests (5):
- Renders category icon and display_name
- Renders category name when display_name is null
- Renders "Uncategorized" when category is null
- Shows color dot with correct color
- Accessibility: no violations

**3. `tests/lib/validations/transactions.test.ts`** — Transaction filter validation:

Tests (12+):
- Valid filter with all fields passes
- Valid filter with no fields passes (all optional)
- search accepts string
- date_from rejects invalid date format
- date_from accepts YYYY-MM-DD format
- amount_min coerces string to number
- amount_min rejects negative number (if enforced)
- limit defaults to 50 when not provided
- limit rejects values > 100
- offset defaults to 0 when not provided
- categoryCreateSchema: name required, max 50 chars
- merchantRuleCreateSchema: merchant_name required, category_id must be UUID
- transactionUpdateSchema: requires at least one field
- transactionSplitSchema: requires minimum 2 splits

Follow the established test patterns from `tests/lib/validations/plaid.test.ts`.
  </action>
  <verify>
- `pnpm test:run` — all tests pass including new ones
- `pnpm lint` — no lint errors in test files
- Test count increased by 25-30 tests
  </verify>
  <done>25+ tests across 3 test files covering TransactionList rendering (loading, empty, data, grouping, pagination), CategoryBadge display, and all Zod validation schemas for transaction filters, categories, merchant rules, and splits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of transaction management flow</name>
  <files></files>
  <action>
Present the following verification checklist to the user. This is a visual/functional check of the complete Phase 20 feature set.

What was built:
- Transaction list page at /money/transactions with date-grouped rows, search, and filters
- Inline transaction detail with category override, notes, and split support
- Merchant rule creation prompted on category change
- Category management in money settings (hide/show defaults, custom categories, merchant rules)
- Sync pipeline applies merchant rules to incoming Plaid transactions
- All i18n strings in 3 locales

How to verify:
1. Navigate to `/money/transactions` — verify the page loads with a transaction list (or empty state if no transactions)
2. If transactions exist:
   a. Verify date grouping with sticky headers (Today, Yesterday, dates)
   b. Verify amounts show green +$X.XX for income, red -$X.XX for spending
   c. Type in the search box — results should filter after ~300ms
   d. Set a date range filter — verify URL updates and results filter
   e. Set a category filter — verify chip appears below filter bar
   f. Click "Clear all" — verify all filters removed
   g. If many transactions, verify "Load More" button and "Showing X of Y"
3. Click a transaction row — verify inline detail expands:
   a. Change the category via dropdown
   b. If transaction has merchant name, verify toast asking to create merchant rule
   c. Add a note and save
   d. Click "Split" and add 2 splits — verify remaining amount updates
4. Navigate to `/money/settings`:
   a. Verify system categories listed with hide/show toggles
   b. Create a custom category with name and color
   c. Verify merchant rules section shows any created rules
5. Verify `/money` page shell links to transactions
6. Switch locale (zh or zh-TW) and verify money UI strings are translated
  </action>
  <verify>User responds with "approved" or describes issues to fix</verify>
  <done>User has visually verified the complete transaction management and categorization flow works correctly</done>
</task>

</tasks>

<verification>
- All new tests pass: `pnpm test:run`
- All existing tests pass (no regressions)
- Lint clean: `pnpm lint`
- Build succeeds: `pnpm build`
- Human has verified the transaction management flow visually
</verification>

<success_criteria>
- 25+ new tests covering transaction list, category badge, and validation schemas
- No test regressions
- Human-verified: transaction list renders correctly with all features
- Human-verified: category override and merchant rules work
- Human-verified: transaction splitting works
- Human-verified: i18n strings display in all 3 locales
</success_criteria>

<output>
After completion, create `.planning/phases/20-transaction-management-categorization/20-05-SUMMARY.md`
</output>
