---
phase: 37.1-v6-gap-closure-calendar-reminder-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/calendar/event-dialog.tsx
  - lib/calendar/feed-aggregation.ts
  - tests/lib/calendar/feed-aggregation.test.ts
  - tests/components/calendar/event-dialog.test.tsx
autonomous: true
gap_closure: true
requirements:
  - REMN-01
  - REMN-04
  - AGGR-04
  - AGGR-06

must_haves:
  truths:
    - "Editing reminders in event dialog persists the user's actual edits, not a stale snapshot from first render"
    - "Workouts appear at their started_at time on the calendar, not as all-day events"
    - "All 5 domain layers (events, tasks, habits, bills, workouts) enabled by default on first load"
  artifacts:
    - path: "components/calendar/event-dialog.tsx"
      provides: "Fixed handleSubmit useCallback with correct dependency array"
      contains: "reminderRows, reminderLoadFailed"
    - path: "lib/calendar/feed-aggregation.ts"
      provides: "normalizeWorkouts extracts time from started_at"
      contains: "allDay: !timePart"
    - path: "components/calendar/calendar-page-content.tsx"
      provides: "All layers enabled by default"
      contains: 'new Set(["events", "tasks", "habits", "bills", "workouts"])'
  key_links:
    - from: "components/calendar/event-dialog.tsx"
      to: "reminderRows state"
      via: "useCallback dependency array"
      pattern: "reminderRows.*reminderLoadFailed"
---

<objective>
Fix three bugs found by v6.0 milestone audit: stale closure in event dialog reminder save, workouts displaying as all-day instead of at their start time, and calendar layers defaulting to only events.

Purpose: These are runtime bugs that cause incorrect behavior — reminders silently lose user edits, workouts show in wrong position, and users must manually enable 4/5 layers every page load.
Output: Three targeted fixes with updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/v6.0-MILESTONE-AUDIT.md
@components/calendar/event-dialog.tsx
@lib/calendar/feed-aggregation.ts
@components/calendar/calendar-page-content.tsx
@tests/lib/calendar/feed-aggregation.test.ts
@tests/components/calendar/event-dialog.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix stale closure in event dialog handleSubmit</name>
  <files>components/calendar/event-dialog.tsx, tests/components/calendar/event-dialog.test.tsx</files>
  <read_first>components/calendar/event-dialog.tsx, tests/components/calendar/event-dialog.test.tsx, .planning/v6.0-MILESTONE-AUDIT.md</read_first>
  <behavior>
    - Test: After modifying reminderRows state (simulating user adding/editing reminders), handleSubmit should use the CURRENT reminderRows value, not the initial value from first render
    - Test: When reminderLoadFailed is true, handleSubmit should skip reminder persistence (this already works but the dep array fix must not break it)
  </behavior>
  <action>
In `components/calendar/event-dialog.tsx`, line 265, the `handleSubmit` useCallback dependency array is:

```
[isEditing, event, onSaved, onClose, form, t]
```

Add `reminderRows` and `reminderLoadFailed` to the dependency array so it becomes:

```
[isEditing, event, onSaved, onClose, form, t, reminderRows, reminderLoadFailed]
```

This fixes REMN-01 and REMN-04 — the stale closure bug where reminder edits made after dialog open were captured at first render time, not at submit time.

Then add or update a test in `tests/components/calendar/event-dialog.test.tsx` that verifies the dependency array includes `reminderRows` and `reminderLoadFailed`. The simplest approach: add a test that simulates opening the dialog for a new event, modifying the reminder rows, submitting, and verifying the fetch call includes the modified reminder data (not the smart default data).
  </action>
  <verify>
    <automated>cd /home/xingdi/code/betterr_me && pnpm test:run -- tests/components/calendar/event-dialog.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "reminderRows, reminderLoadFailed" components/calendar/event-dialog.tsx returns a match on the useCallback dependency line
    - grep -n "reminderRows" components/calendar/event-dialog.tsx shows it appears in the dependency array (line ~265)
    - All existing event-dialog tests pass plus new test for stale closure fix
  </acceptance_criteria>
  <done>handleSubmit useCallback dependency array includes reminderRows and reminderLoadFailed. Test confirms reminder edits are not lost on submit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix workout time extraction and layer defaults</name>
  <files>lib/calendar/feed-aggregation.ts, tests/lib/calendar/feed-aggregation.test.ts, components/calendar/calendar-page-content.tsx</files>
  <read_first>lib/calendar/feed-aggregation.ts, tests/lib/calendar/feed-aggregation.test.ts, components/calendar/calendar-page-content.tsx</read_first>
  <behavior>
    - Test: normalizeWorkouts with started_at="2025-04-05T14:30:00Z" and no timezone should produce startTime="14:30" and allDay=false
    - Test: normalizeWorkouts with started_at="2025-04-05T14:30:00Z" and timezone="America/New_York" should produce correct local time and allDay=false
    - Test: normalizeWorkouts with started_at="2025-04-05" (no time component) should produce startTime=null and allDay=true
  </behavior>
  <action>
**Workout time (AGGR-04):** The `normalizeWorkouts` function in `lib/calendar/feed-aggregation.ts` already has the fix in the current codebase (it extracts time from `started_at` and sets `allDay: !timePart`). Verify the existing code is correct by reading it. If it already handles time extraction properly (which it appears to from the read), ensure the tests cover both timezone and non-timezone paths.

Add or update tests in `tests/lib/calendar/feed-aggregation.test.ts`:
1. Test `normalizeWorkouts` with a workout that has a full ISO timestamp — verify `startTime` is extracted (e.g., "14:30") and `allDay` is false
2. Test `normalizeWorkouts` with timezone param — verify timezone conversion works
3. Test `normalizeWorkouts` with a date-only `started_at` — verify `allDay` is true

**Layer defaults (AGGR-06):** The `calendar-page-content.tsx` already has `new Set(["events", "tasks", "habits", "bills", "workouts"])` at line 85. Verify this is correct by reading the file. If the fix is already in place, just ensure the test covers it. If not, change `new Set(["events"])` to `new Set(["events", "tasks", "habits", "bills", "workouts"])`.

Note: Based on the file reads, BOTH fixes appear to already be in place in the current codebase. The task should verify they are correct and add test coverage if missing.
  </action>
  <verify>
    <automated>cd /home/xingdi/code/betterr_me && pnpm test:run -- tests/lib/calendar/feed-aggregation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "allDay.*!timePart" lib/calendar/feed-aggregation.ts returns a match in normalizeWorkouts
    - grep -n 'new Set.*events.*tasks.*habits.*bills.*workouts' components/calendar/calendar-page-content.tsx returns a match
    - Tests for normalizeWorkouts cover: timestamp with time -> startTime extracted + allDay false; timestamp with timezone -> correct local time; date-only -> allDay true
    - All feed-aggregation tests pass
  </acceptance_criteria>
  <done>Workouts display at their started_at time (not all-day). All 5 layers enabled by default. Tests confirm both behaviors.</done>
</task>

</tasks>

<verification>
1. `pnpm test:run -- tests/components/calendar/event-dialog.test.tsx` — all pass
2. `pnpm test:run -- tests/lib/calendar/feed-aggregation.test.ts` — all pass
3. `grep -n "reminderRows, reminderLoadFailed" components/calendar/event-dialog.tsx` — match found
4. `grep -n 'new Set.*events.*tasks.*habits.*bills.*workouts' components/calendar/calendar-page-content.tsx` — match found
</verification>

<success_criteria>
- handleSubmit useCallback in event-dialog.tsx has reminderRows and reminderLoadFailed in its dependency array
- normalizeWorkouts extracts time from started_at and sets allDay accordingly
- enabledLayers defaults to all 5 domain layers
- All related tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/37.1-v6-gap-closure-calendar-reminder-fixes/37.1-01-SUMMARY.md`
</output>
