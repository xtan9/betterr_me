---
phase: journal-v4
task: UAT fixes implemented, ready for user testing
total_tasks: 6 gaps fixed
status: fixes_complete
last_updated: 2026-02-28T10:00:00.000Z
---

<current_state>
All 6 UAT gaps from journal-UAT.md have been fixed. 1600 tests pass, build clean, lint clean. Ready for user verification.
</current_state>

<completed_work>

### Fix 1: SWR cache invalidation (blocker — fixed 4/7 issues: tests 9, 10, 11, 15)
- `use-journal-autosave.ts`: Added `onSaved` callback (via ref) called after each successful save
- `journal-entry-modal.tsx`: Passes `() => mutate()` as `onSaved` so SWR revalidates after every autosave
- `journal-entry-modal.tsx`: `handleOpenChange` now async — awaits `flushNow()` + `mutate()` before closing modal, ensuring save completes before `refreshKey` increments

### Fix 2: Calendar mood indicators (test 2)
- Root cause was SWR timing: save hadn't completed before calendar re-fetched
- Fixed by Fix 1 (await flushNow before refreshKey increment)

### Fix 3: Word count for Chinese (test 3)
- `journal-editor.tsx`: Added `countWords()` function that handles CJK characters (each counted as 1 word) + Latin words (split on whitespace)
- Replaced `editor.storage.characterCount.words()` with `countWords(editor.state.doc.textContent)`
- 7 new tests covering English, Chinese, Japanese, Traditional Chinese, and mixed content

### Fix 4: Prompt persistence (test 7)
- Root cause: `setPromptKey(null)` in the modal-open useEffect cleared the prompt, and the entry-sync effect didn't re-run (cached entry unchanged)
- Fix: Removed `setPromptKey(null)` from open effect; combined sync logic into `[open, entry]` effect that always syncs mood+prompt when modal opens or entry loads

### Fix 5: Permanent toolbar (test 4)
- Created `journal-toolbar.tsx` — same buttons as bubble menu, rendered as static toolbar above editor
- `journal-editor.tsx` now imports `JournalToolbar` instead of `JournalBubbleMenu`

</completed_work>

<remaining_work>
None — all 6 UAT gaps addressed. User verification needed.
</remaining_work>

<files_changed>
- lib/hooks/use-journal-autosave.ts (added onSaved callback)
- components/journal/journal-entry-modal.tsx (async close, SWR mutate, prompt fix)
- components/journal/journal-editor.tsx (custom countWords, toolbar import)
- components/journal/journal-toolbar.tsx (NEW — permanent toolbar)
- tests/components/journal/journal-entry-modal.test.tsx (async close test)
- tests/components/journal/count-words.test.ts (NEW — 7 word count tests)
</files_changed>
