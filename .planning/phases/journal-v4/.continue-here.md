---
phase: journal-v4
task: UAT complete, diagnosis needed
total_tasks: 6 gaps to fix
status: uat_complete
last_updated: 2026-02-28T06:17:38.458Z
---

<current_state>
Journal v4.0 UAT testing is complete. 17 tests run: 9 passed, 7 issues found (1 blocker, 4 major, 2 cosmetic). The UAT file is at `.planning/phases/journal-v4/journal-UAT.md`. No diagnosis or fix planning has been done yet.

Two CI fix PRs were also completed this session:
- PR #298 (merged): visual regression masks, bundle thresholds, optimizePackageImports
- PR #299 (merged): removed networkidle wait causing 45min e2e timeout
</current_state>

<completed_work>

- CI Fix PR #298: Masked journal widget in visual regression screenshots, added @tiptap/* to optimizePackageImports, raised bundle thresholds (600→750KB total, 100→130KB per-chunk), added networkidle wait to waitForRedirect — MERGED
- CI Fix PR #299: Removed networkidle wait (caused SWR polling to hang e2e for 45min), replaced with targeted habit-card element waitFor — MERGED
- Visual regression snapshots: Updated via "Update Visual Snapshots" workflow on PR branch
- Journal v4.0 UAT: All 17 tests completed with user
</completed_work>

<remaining_work>

### Blocker (fix first)
1. **SWR cache invalidation** — Saved/edited/deleted journal entries don't update UI without page refresh. Affects calendar view, timeline view, and entry modal. Root cause: SWR cache keys not being mutated after write operations (save, edit, delete). Tests 9, 10, 11.

### Major Issues
2. **Calendar mood indicators missing** — Calendar shows no dots/indicators on days with journal entries. Test 2.
3. **Word count broken for Chinese** — Word count in editor footer only works for English text, not Chinese characters. Test 3.
4. **Prompt not persisted** — Selected writing prompt disappears when reopening an entry. Test 7.
5. **Timeline stale after delete** — Timeline doesn't refresh after deleting an entry (related to SWR issue). Test 15.

### Cosmetic
6. **Floating bubble menu → permanent toolbar** — User prefers a permanent toolbar instead of the floating bubble menu for text formatting. Test 4.
</remaining_work>

<decisions_made>

- Raised bundle thresholds rather than trying to reduce Tiptap chunk size — Tiptap is already lazy-loaded via next/dynamic, the 119KB chunk is irreducible
- Removed networkidle from waitForRedirect entirely — SWR polling prevents networkidle from ever resolving; targeted element waitFor is more reliable
- Grouped SWR cache issues (tests 9, 10, 11, 15) as one root cause — all stem from missing SWR mutate() calls after journal API writes
</decisions_made>

<blockers>
- None — all issues are known and diagnosable
</blockers>

<context>
The SWR cache invalidation blocker is likely the highest-impact fix — it will resolve 4 of the 7 issues (tests 9, 10, 11, and 15). The fix pattern is to call `mutate()` on relevant SWR keys after journal entry save/edit/delete operations. Look at how habits handle SWR cache invalidation for the pattern.

Calendar mood indicators (test 2) may be a separate issue — the calendar component might not be fetching entry metadata for the visible month, or the data is there but not rendering dots.

Word count for Chinese (test 3) needs a different counting strategy — Chinese doesn't use spaces between words, so a simple split-on-whitespace approach won't work. Consider using Intl.Segmenter or character count.
</context>

<next_action>
Start with: Diagnose the 6 gaps from the UAT file. The SWR cache invalidation blocker should be investigated first — find all journal save/edit/delete handlers and check if they call SWR mutate(). Then plan fixes for all gaps and execute.

To resume: `/gsd:resume-work` or read `.planning/phases/journal-v4/journal-UAT.md` for full gap details.
</next_action>
