---
phase: 22-bills-goals-net-worth
plan: 04
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - components/money/bill-summary-header.tsx
  - components/money/bill-row.tsx
  - components/money/bills-list.tsx
  - components/money/bill-form.tsx
  - components/money/bill-calendar.tsx
  - components/money/bill-calendar-day.tsx
  - app/money/bills/page.tsx
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03, BILL-04]

must_haves:
  truths:
    - "User can see all detected and manual bills in a list grouped by frequency (Monthly, Weekly, Annual)"
    - "User can confirm or dismiss auto-detected bills inline without a modal"
    - "User can view a bill calendar showing upcoming charges per day with month navigation"
    - "User can create manual bills and edit confirmed bills"
    - "Bills show paid-this-month status and price change badges"
    - "Summary header shows total monthly cost and bill counts"
  artifacts:
    - path: "components/money/bills-list.tsx"
      provides: "Bill list with frequency sections and dismissed section"
      min_lines: 80
    - path: "components/money/bill-row.tsx"
      provides: "Individual bill row with confirm/dismiss toggle and status badges"
      min_lines: 40
    - path: "components/money/bill-calendar.tsx"
      provides: "Month grid calendar with bill markers and navigation"
      min_lines: 60
    - path: "components/money/bill-form.tsx"
      provides: "Create/edit bill dialog with react-hook-form"
      min_lines: 50
    - path: "app/money/bills/page.tsx"
      provides: "Bills page route with server header and client list"
      min_lines: 15
  key_links:
    - from: "components/money/bills-list.tsx"
      to: "lib/hooks/use-bills.ts"
      via: "useBills hook"
      pattern: "useBills"
    - from: "components/money/bill-row.tsx"
      to: "/api/money/bills/[id]"
      via: "PATCH for status toggle"
      pattern: "fetch.*api/money/bills"
    - from: "components/money/bill-calendar.tsx"
      to: "date-fns"
      via: "Calendar grid generation"
      pattern: "eachDayOfInterval|startOfMonth|endOfMonth"
---

<objective>
Build the complete bills UI: list with frequency sections, inline confirm/dismiss toggles, bill calendar with month navigation, create/edit forms, and bills page route.

Purpose: Users need to see, manage, and plan around their recurring charges. This plan implements all bill-related UI components per the locked user decisions.

Output: 6 component files, 1 page route. Complete bills feature visible at /money/bills.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bills-goals-net-worth/22-CONTEXT.md
@.planning/phases/22-bills-goals-net-worth/22-RESEARCH.md
@.planning/phases/22-bills-goals-net-worth/22-02-SUMMARY.md
@components/money/budget-overview.tsx
@components/money/budget-ring.tsx
@components/money/transaction-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bill list components with frequency grouping, confirm/dismiss toggle, and form</name>
  <files>
    components/money/bill-summary-header.tsx
    components/money/bill-row.tsx
    components/money/bills-list.tsx
    components/money/bill-form.tsx
  </files>
  <action>
    All components use Calm Finance design tokens (money-sage, money-amber, money-surface, money-border, money-caution). Add `"use client"` directive.

    **BillSummaryHeader** (`components/money/bill-summary-header.tsx`):
    - Displays summary stats from bills API: "12 bills . 3 pending . $487/mo" per user decision
    - Props: `{ totalMonthlyCents: number, billCount: number, pendingCount: number }`
    - Use formatMoney from `@/lib/money/arithmetic` for the monthly total
    - Use useTranslations('money') for labels
    - Calm Finance card styling: border-money-border bg-money-surface rounded-lg p-4

    **BillRow** (`components/money/bill-row.tsx`):
    - Props: `{ bill: RecurringBill, onStatusChange: (id, status) => void, onEdit: (bill) => void }`
    - Display: bill name, amount (formatMoney), frequency label, next due date
    - **Inline confirm/dismiss toggle** (per locked decision -- NO modal):
      - For auto-detected bills: Show small "Confirm" button (money-sage) and "Dismiss" button (muted)
      - For confirmed bills: Show small "Dismiss" link
      - For dismissed bills: Show "Re-confirm" button
      - Status change calls onStatusChange which PATCHes /api/money/bills/[id] with { user_status }
    - **Paid-this-month indicator**: If bill has a recent transaction match (from Plaid stream data), show a small checkmark badge. For now, derive from bill.is_active and last_date -- if last_date is within current month, show "Paid" badge in money-sage color.
    - **Price change badge** (per locked decision): If bill.previous_amount_cents exists and differs from bill.amount_cents, show a small badge: "Was {old} -> {new}" with money-amber color.
    - Click on bill name or edit icon opens edit form
    - Use lucide-react icons: Check for paid, AlertTriangle for price change, MoreHorizontal for actions

    **BillsList** (`components/money/bills-list.tsx`):
    - Uses `useBills()` hook to fetch data
    - Shows BillSummaryHeader at top
    - Groups bills by frequency per locked decision: Monthly, Weekly, Annual sections
    - Within each section, sort by next_due_date (ascending)
    - Separate section for dismissed bills at bottom per locked decision: collapsible "Dismissed" section user can browse and re-confirm
    - Filter out dismissed from main sections, show them in the dismissed section
    - "Add Bill" button at top opens BillForm for manual creation
    - "Sync Bills" button triggers POST /api/money/bills/sync (with loading state)
    - Handle empty state: if no bills detected, show welcoming message encouraging bank connection or manual entry
    - Loading state: skeleton cards

    **BillForm** (`components/money/bill-form.tsx`):
    - Dialog component for create/edit (per locked decision: confirmed bills are fully editable)
    - Uses react-hook-form + zodResolver with a local form schema derived from billCreateSchema
    - Fields: name (text), amount (number input, dollars), frequency (Select from WEEKLY/BIWEEKLY/SEMI_MONTHLY/MONTHLY/ANNUALLY), next_due_date (date input)
    - For edit mode: pre-populate fields from existing bill
    - On submit: POST /api/money/bills (create) or PATCH /api/money/bills/[id] (edit)
    - After success: mutate bills SWR cache and close dialog
    - Dialog uses modal={false} if it contains Select components (per Phase 21 decision for Radix Select compatibility)
  </action>
  <verify>pnpm lint passes. TypeScript compiles. Components render without errors.</verify>
  <done>Bill list shows frequency-grouped sections with inline confirm/dismiss, price change badges, paid status, and create/edit forms. Summary header shows monthly cost totals.</done>
</task>

<task type="auto">
  <name>Task 2: Create bill calendar and bills page route</name>
  <files>
    components/money/bill-calendar.tsx
    components/money/bill-calendar-day.tsx
    app/money/bills/page.tsx
  </files>
  <action>
    **BillCalendar** (`components/money/bill-calendar.tsx`):
    Claude's discretion: use month grid style (fits Calm Finance better than timeline -- grids are calmer, more structured).
    - Props: `{ bills: RecurringBill[] }`
    - Generate calendar grid using date-fns: `startOfWeek(startOfMonth(currentMonth))` to `endOfWeek(endOfMonth(currentMonth))`
    - Use `eachDayOfInterval()` to get all days in the grid
    - Month navigation with left/right arrows per locked decision: `addMonths(currentMonth, -1)` and `addMonths(currentMonth, 1)`
    - Display month/year header: `format(currentMonth, 'MMMM yyyy')`
    - Day of week header row (Sun, Mon, Tue, etc.)
    - For each day: render BillCalendarDay component
    - Map bills to days: for confirmed/auto bills, check if next_due_date falls on that day. For future months, project forward based on frequency (MONTHLY: same day each month, WEEKLY: every 7 days, etc.)
    - Use `isSameDay()` for date comparison
    - Days outside current month shown in muted style (text-muted-foreground, opacity-50)
    - Show individual bill names/icons per day per locked decision (NO daily aggregate totals)
    - Responsive: use CSS grid with 7 columns

    **BillCalendarDay** (`components/money/bill-calendar-day.tsx`):
    - Props: `{ date: Date, bills: RecurringBill[], isCurrentMonth: boolean, isToday: boolean }`
    - Display day number and bill markers
    - Claude's discretion on day-click: use inline expand (simpler, fits calm aesthetic -- clicking a day expands to show bill details below the calendar row, similar to how transaction detail expands inline)
    - Bill markers: small colored dots or truncated bill names (if 1-2 bills show name, if 3+ show count badge)
    - Today's date gets a subtle ring highlight (border-money-sage)
    - Bills on this day shown with amount next to name

    **Bills page route** (`app/money/bills/page.tsx`):
    Follow server page + client list pattern (same as accounts page):
    - Server component with i18n PageHeader: `await getTranslations('money')`
    - Import SidebarShell for layout
    - Render: SidebarShell > PageHeader > div with two sections:
      - BillsList component (the list view)
      - BillCalendar component (the calendar view) -- could use Tabs to switch between list and calendar views, or show both. Claude's discretion: use Tabs (List | Calendar) for clean navigation between the two views.
    - Both BillsList and BillCalendar are client components using useBills hook
    - Wrap in Suspense for SSR compatibility (useSearchParams if needed)
  </action>
  <verify>pnpm lint passes. TypeScript compiles. Bills page renders at /money/bills.</verify>
  <done>Bill calendar shows month grid with bill markers, month navigation, and inline day expansion. Bills page provides tabbed List/Calendar views.</done>
</task>

</tasks>

<verification>
- `pnpm lint` passes with 0 errors
- `pnpm exec tsc --noEmit` compiles without errors
- `pnpm test:run` -- existing tests still pass
- Bills page renders at /money/bills with list and calendar views
- Bills grouped by frequency sections (Monthly, Weekly, Annual)
- Confirm/dismiss toggles work inline (no modal)
- Calendar shows bills per day with month navigation
- Manual bill creation works via form dialog
</verification>

<success_criteria>
- Bills list shows frequency sections sorted by due date
- Inline confirm/dismiss toggle per locked decision (no modal)
- Price change badge shows old vs new amount
- Paid-this-month status derived from bill data
- Calendar uses month grid with arrows for navigation
- Individual bill names shown per day (no aggregate totals)
- Bill form supports both create and edit modes
- Summary header: "N bills . N pending . $X/mo"
</success_criteria>

<output>
After completion, create `.planning/phases/22-bills-goals-net-worth/22-04-SUMMARY.md`
</output>
