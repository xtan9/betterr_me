---
phase: 22-bills-goals-net-worth
plan: 05
type: execute
wave: 3
depends_on: [22-03]
files_modified:
  - components/money/goal-card.tsx
  - components/money/goal-grid.tsx
  - components/money/goal-form.tsx
  - components/money/net-worth-chart.tsx
  - components/money/net-worth-summary.tsx
  - components/money/net-worth-accounts.tsx
  - components/money/manual-asset-form.tsx
  - app/money/goals/page.tsx
  - app/money/net-worth/page.tsx
autonomous: true
requirements: [GOAL-01, GOAL-02, GOAL-03, GOAL-04, GOAL-05, NTWT-01, NTWT-02, NTWT-03]

must_haves:
  truths:
    - "User can see all savings goals as cards with ring/donut progress visualization"
    - "User can create goals with target amount, optional deadline, and funding type"
    - "User can see projected completion date with color-coded status"
    - "User can see total net worth with assets vs liabilities breakdown"
    - "User can see net worth line chart with timeframe toggle (1M/3M/6M/1Y/All)"
    - "User can add manual assets with name, value, and type"
  artifacts:
    - path: "components/money/goal-card.tsx"
      provides: "Goal card with progress ring and projection"
      min_lines: 50
    - path: "components/money/goal-grid.tsx"
      provides: "Multi-goal card grid layout"
      min_lines: 30
    - path: "components/money/net-worth-chart.tsx"
      provides: "Recharts LineChart for net worth over time"
      min_lines: 60
    - path: "components/money/net-worth-summary.tsx"
      provides: "Assets vs liabilities summary with change indicator"
      min_lines: 40
    - path: "app/money/goals/page.tsx"
      provides: "Goals page route"
      min_lines: 15
    - path: "app/money/net-worth/page.tsx"
      provides: "Net worth page route"
      min_lines: 15
  key_links:
    - from: "components/money/goal-grid.tsx"
      to: "lib/hooks/use-goals.ts"
      via: "useGoals hook"
      pattern: "useGoals"
    - from: "components/money/net-worth-chart.tsx"
      to: "lib/hooks/use-net-worth.ts"
      via: "useNetWorthHistory hook"
      pattern: "useNetWorthHistory"
    - from: "components/money/net-worth-chart.tsx"
      to: "recharts"
      via: "LineChart component"
      pattern: "LineChart|ResponsiveContainer"
---

<objective>
Build the complete goals UI (card grid with progress rings and projections) and net worth UI (line chart, assets/liabilities summary, account breakdown, manual assets).

Purpose: Users need visual progress tracking for savings goals and a comprehensive net worth dashboard. This plan completes the remaining two feature areas of Phase 22.

Output: 7 component files, 2 page routes. Complete goals at /money/goals and net worth at /money/net-worth.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bills-goals-net-worth/22-CONTEXT.md
@.planning/phases/22-bills-goals-net-worth/22-RESEARCH.md
@.planning/phases/22-bills-goals-net-worth/22-03-SUMMARY.md
@components/money/budget-ring.tsx
@components/money/spending-trend-bar.tsx
@components/money/budget-overview.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create savings goals UI components and page route</name>
  <files>
    components/money/goal-card.tsx
    components/money/goal-grid.tsx
    components/money/goal-form.tsx
    app/money/goals/page.tsx
  </files>
  <action>
    **GoalCard** (`components/money/goal-card.tsx`):
    - Props: `{ goal: GoalWithProjection, onEdit: (goal) => void, onContribute: (goalId) => void }`
    - Display: goal name, icon/emoji (if set), progress ring, amounts, projection
    - **Progress ring** per locked decision (ring/donut): Reuse the BudgetRing SVG pattern from `components/money/budget-ring.tsx` (custom SVG, ~60 lines). Calculate percent = (current_cents / target_cents) * 100, cap at 100.
    - Ring color based on goal.status_color from API:
      - green: `hsl(var(--money-sage))` (on track or no deadline)
      - yellow: `hsl(var(--money-amber))` (slightly behind)
      - red: `hsl(var(--money-caution))` (significantly behind)
    - Display current/target amounts: "${current} / ${target}" using formatMoney
    - Display projected completion: "On track to complete by {date}" or "Behind schedule" or "No progress yet"
    - For linked goals: show linked account name, "Auto-tracking" badge
    - For manual goals: show "Add Funds" button that calls onContribute
    - Deadline display: if set, show "Due {date}" with color-coded text matching status_color
    - Card styling: Calm Finance tokens, subtle hover effect (transition-all duration-200 hover:shadow-md hover:-translate-y-0.5)

    **GoalGrid** (`components/money/goal-grid.tsx`):
    Claude's discretion: use card grid layout (better for visual progress rings, more scannable than stacked list).
    - Uses `useGoals()` hook to fetch data
    - Responsive grid: 1 col on mobile, 2 on md, 3 on lg
    - Render GoalCard for each goal
    - "Create Goal" button at top opens GoalForm
    - Empty state: welcoming message with CTA to create first goal
    - Loading state: skeleton cards (3 skeleton cards in grid)
    - Sort goals: active first (by deadline if set, then by created_at), completed at bottom

    **GoalForm** (`components/money/goal-form.tsx`):
    - Dialog component for create/edit goals + contribution dialog
    - Two modes controlled by props: `mode: 'create' | 'edit' | 'contribute'`
    - **Create/Edit mode**: react-hook-form + zodResolver
      - Fields: name, target_amount (dollars), deadline (date picker, optional), funding_type (radio: Manual Contributions / Link to Account), linked_account_id (Select, shown only when funding_type='linked' -- fetch accounts from useAccounts hook), icon (emoji picker or text input, optional), color (color selector, optional)
      - On submit: POST /api/money/goals or PATCH /api/money/goals/[id]
    - **Contribute mode**: simpler form
      - Fields: amount (dollars), note (optional text)
      - On submit: POST /api/money/goals/[id]/contributions
      - After success: mutate goals cache
    - Dialog uses modal={false} if it contains Select components
    - useTranslations('money') for labels

    **Goals page** (`app/money/goals/page.tsx`):
    - Server component with i18n PageHeader
    - SidebarShell layout
    - Render GoalGrid client component
    - Wrap in Suspense if needed
  </action>
  <verify>pnpm lint passes. TypeScript compiles. Goals page renders at /money/goals.</verify>
  <done>Goals page shows card grid with progress rings, projected completion dates, color-coded status, and create/edit/contribute forms.</done>
</task>

<task type="auto">
  <name>Task 2: Create net worth UI components and page route</name>
  <files>
    components/money/net-worth-chart.tsx
    components/money/net-worth-summary.tsx
    components/money/net-worth-accounts.tsx
    components/money/manual-asset-form.tsx
    app/money/net-worth/page.tsx
  </files>
  <action>
    **NetWorthChart** (`components/money/net-worth-chart.tsx`):
    - Props: none (fetches its own data via useNetWorthHistory hook)
    - **Timeframe toggle** per locked decision: preset buttons 1M, 3M, 6M, 1Y, All. Use a row of small buttons with active state highlighting (bg-money-sage text-white for active, bg-muted for inactive).
    - useState for selected period, default '6M'
    - `useNetWorthHistory(period)` fetches data when period changes (keepPreviousData for smooth transition)
    - **Recharts LineChart** following the pattern from 22-RESEARCH.md and existing spending-trend-bar.tsx:
      - ResponsiveContainer width="100%" height={300}
      - CartesianGrid with strokeDasharray="3 3" and money-border color
      - XAxis with date labels, YAxis with money formatting (formatMoney or abbreviated: $10K, $100K)
      - Custom Tooltip showing date + formatted net worth + assets + liabilities
      - Single Line for total net worth: stroke=money-sage, strokeWidth=2, dot={false}, type="monotone"
      - Optionally add dashed lines for assets and liabilities if data exists
    - Handle empty state: "No net worth data yet. Connect a bank account to start tracking."
    - Use Calm Finance styling for card container

    **NetWorthSummary** (`components/money/net-worth-summary.tsx`):
    - Props: none (fetches via useNetWorth hook)
    - **Layout** per locked decision: line chart on top -> this component -> account breakdown below
    - Display:
      - Total net worth in large text (formatMoney, text-2xl font-semibold)
      - **Change indicator** per locked decision: amount and percentage change vs last period
        - Green (money-sage) for positive change: "+$1,234 (+5.2%)"
        - Red (money-caution) for negative change: "-$567 (-2.1%)"
        - Use ArrowUpRight/ArrowDownRight icons from lucide-react
      - Assets total and Liabilities total side by side:
        - Assets: positive amount in money-sage
        - Liabilities: amount in money-caution/muted
      - Manual assets subtotal (if any): "Includes $X in manual assets"
    - Loading skeleton with pulsing placeholders

    **NetWorthAccounts** (`components/money/net-worth-accounts.tsx`):
    - Props: none (fetches via useNetWorth hook -- reuses same data as summary)
    - **Account breakdown by type** per locked decision: checking, savings, credit cards, loans
    - Display as sections with account type headers
    - Each section shows: type icon (lucide-react), type label, subtotal for that type
    - Under each type: list individual accounts with name, mask (last 4 digits), balance
    - "Manual Assets" section at bottom with each manual asset name and value
    - "Add Asset" button opens ManualAssetForm

    **ManualAssetForm** (`components/money/manual-asset-form.tsx`):
    - Dialog for create/edit manual assets per locked decision (property, vehicle, etc.)
    - react-hook-form + zodResolver with manualAssetCreateSchema/manualAssetUpdateSchema
    - Fields: name (text), value (number, dollars), asset_type (Select: Property, Vehicle, Investment, Other), notes (textarea, optional)
    - Clarifying text per research pitfall: "Add assets not tracked by connected accounts (home value, vehicle, etc.)"
    - On submit: POST /api/money/manual-assets or PATCH /api/money/manual-assets/[id]
    - After success: mutate net worth cache

    **Net worth page** (`app/money/net-worth/page.tsx`):
    - Server component with i18n PageHeader
    - SidebarShell layout
    - Layout per locked decision: chart on top -> summary -> account breakdown
    - Render in order: NetWorthChart, NetWorthSummary, NetWorthAccounts
    - All three are client components with their own SWR hooks
    - Wrap in Suspense if needed
  </action>
  <verify>pnpm lint passes. TypeScript compiles. Net worth page renders at /money/net-worth.</verify>
  <done>Net worth page shows line chart with timeframe toggle, assets/liabilities summary with change indicators, account breakdown by type, and manual asset management.</done>
</task>

</tasks>

<verification>
- `pnpm lint` passes with 0 errors
- `pnpm exec tsc --noEmit` compiles without errors
- `pnpm test:run` -- existing tests still pass
- Goals page at /money/goals shows card grid with progress rings
- Net worth page at /money/net-worth shows chart + summary + accounts
- Timeframe toggle switches chart data (1M/3M/6M/1Y/All)
- Manual asset form creates assets that appear in net worth
- Change indicator shows correct direction (green up, red down)
</verification>

<success_criteria>
- Goals use ring/donut progress visualization per locked decision
- Goals show color-coded deadline status (green/yellow/red)
- Multiple goals displayed simultaneously in responsive card grid
- Net worth shows line chart on top per locked decision
- Timeframe toggle uses preset buttons (1M/3M/6M/1Y/All) per locked decision
- Change indicator uses green for up, red for down per locked decision
- Manual assets clearly separated from connected accounts
- Manual asset form clarifies "for items not tracked by connected accounts"
</success_criteria>

<output>
After completion, create `.planning/phases/22-bills-goals-net-worth/22-05-SUMMARY.md`
</output>
