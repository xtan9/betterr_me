---
phase: 22-bills-goals-net-worth
plan: 06
type: execute
wave: 4
depends_on: [22-04, 22-05]
files_modified:
  - components/money/money-page-shell.tsx
  - app/money/layout.tsx
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
  - tests/components/money/bills-list.test.tsx
  - tests/components/money/goal-grid.test.tsx
  - tests/components/money/net-worth-chart.test.tsx
  - tests/validations/bills-goals.test.tsx
autonomous: false
requirements: [BILL-01, BILL-02, BILL-03, BILL-04, GOAL-01, GOAL-02, GOAL-03, GOAL-05, NTWT-01, NTWT-02]

must_haves:
  truths:
    - "Sidebar and money shell navigation includes Bills, Goals, and Net Worth links"
    - "All UI strings have translations in en, zh, and zh-TW"
    - "Component tests cover key rendering paths for bills, goals, and net worth"
    - "Validation tests cover all new Zod schemas"
    - "Human has verified the complete bills, goals, and net worth features"
  artifacts:
    - path: "tests/components/money/bills-list.test.tsx"
      provides: "BillsList component tests"
      min_lines: 40
    - path: "tests/components/money/goal-grid.test.tsx"
      provides: "GoalGrid component tests"
      min_lines: 40
    - path: "tests/components/money/net-worth-chart.test.tsx"
      provides: "NetWorthChart component tests"
      min_lines: 30
    - path: "tests/validations/bills-goals.test.tsx"
      provides: "Validation tests for bills, goals, contributions, manual assets schemas"
      min_lines: 60
  key_links:
    - from: "components/money/money-page-shell.tsx"
      to: "/money/bills, /money/goals, /money/net-worth"
      via: "Navigation links"
      pattern: "money/bills|money/goals|money/net-worth"
    - from: "i18n/messages/en.json"
      to: "components/money/*"
      via: "useTranslations('money')"
      pattern: "bills|goals|netWorth"
---

<objective>
Add navigation links, i18n translations for all 3 features, component and validation tests, and human verification of the complete Phase 22 feature set.

Purpose: Complete the phase by wiring navigation, adding translations for all locales, ensuring test coverage, and getting human sign-off on the implemented features.

Output: Updated navigation, 3 locale files updated, 4 test files, human verification checkpoint.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-bills-goals-net-worth/22-04-SUMMARY.md
@.planning/phases/22-bills-goals-net-worth/22-05-SUMMARY.md
@components/money/money-page-shell.tsx
@i18n/messages/en.json
@tests/components/money/money-page-shell.test.tsx
@tests/validations/transactions.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation links, i18n translations, and tests</name>
  <files>
    components/money/money-page-shell.tsx
    app/money/layout.tsx
    i18n/messages/en.json
    i18n/messages/zh.json
    i18n/messages/zh-TW.json
    tests/components/money/bills-list.test.tsx
    tests/components/money/goal-grid.test.tsx
    tests/components/money/net-worth-chart.test.tsx
    tests/validations/bills-goals.test.tsx
  </files>
  <action>
    **Navigation updates:**

    Update `components/money/money-page-shell.tsx` to add Bills, Goals, and Net Worth links alongside existing Accounts, Transactions, Budgets, and Settings links. Use the same nav pattern already established in the file. Bills link goes to `/money/bills`, Goals to `/money/goals`, Net Worth to `/money/net-worth`.

    Update `app/money/layout.tsx` if needed to ensure the new page routes are properly served within the money layout.

    **i18n translations** (ALL THREE locale files):

    Add money.bills, money.goals, money.netWorth namespace keys to all three locale files (en.json, zh.json, zh-TW.json). Include:

    Bills strings:
    - title, subtitle, addBill, syncBills, syncing
    - frequency labels: weekly, biweekly, semiMonthly, monthly, annually
    - status labels: confirmed, dismissed, autoDetected, paid, pending
    - summary: totalMonthly, billCount, pendingCount
    - form: name, amount, frequency, nextDueDate, create, edit, save, cancel
    - actions: confirm, dismiss, reConfirm, delete
    - priceChange: was, now, changed
    - calendar: title, today, noBills
    - empty: title, description
    - list/calendar tab labels

    Goals strings:
    - title, subtitle, createGoal, editGoal, addFunds
    - form: name, targetAmount, deadline, fundingType, manual, linked, linkedAccount, icon, color, save, cancel
    - contribute: amount, note, addContribution
    - status: onTrack, slightlyBehind, behind, noProgress, completed, noDeadline
    - projection: projectedDate, monthlyRate, noDataYet
    - empty: title, description

    Net Worth strings:
    - title, subtitle
    - summary: totalNetWorth, assets, liabilities, manualAssets, change, vsLastMonth
    - chart: timeframes (1M, 3M, 6M, 1Y, All)
    - accounts: checking, savings, creditCards, loans, other, manualAssets
    - assets: addAsset, editAsset, name, value, type, notes, property, vehicle, investment, other
    - empty: title, description

    For zh and zh-TW: provide natural Chinese translations (not just Pinyin).

    **Component tests:**

    `tests/components/money/bills-list.test.tsx`:
    - Mock useBills hook (same pattern as TransactionList tests: mock sub-components to isolate)
    - Test: renders summary header with correct stats
    - Test: groups bills by frequency sections
    - Test: shows dismissed section when dismissed bills exist
    - Test: renders empty state when no bills
    - Test: confirm/dismiss buttons trigger correct API call
    - Test: loading skeleton shown when data is loading
    - Test: accessibility (axe)

    `tests/components/money/goal-grid.test.tsx`:
    - Mock useGoals hook
    - Test: renders goal cards in grid layout
    - Test: progress ring shows correct percentage
    - Test: color-coded status (green/yellow/red) based on projection
    - Test: empty state when no goals
    - Test: create goal button opens form
    - Test: accessibility (axe)

    `tests/components/money/net-worth-chart.test.tsx`:
    - Mock useNetWorthHistory hook
    - Test: renders chart container
    - Test: timeframe toggle buttons present (1M, 3M, 6M, 1Y, All)
    - Test: clicking timeframe button changes selected period
    - Test: empty state when no snapshot data
    - Test: accessibility (axe)

    `tests/validations/bills-goals.test.tsx`:
    Following pattern from `tests/validations/transactions.test.tsx` -- test all schemas in one file:
    - billCreateSchema: valid input passes, missing name fails, invalid frequency fails, negative amount fails
    - billUpdateSchema: partial updates pass, invalid user_status fails
    - goalCreateSchema: valid input passes, missing name fails, linked without account_id fails (refine), zero target fails
    - goalUpdateSchema: partial updates pass, invalid status fails
    - contributionCreateSchema: valid passes, zero amount fails, note too long fails
    - manualAssetCreateSchema: valid passes, invalid asset_type fails, zero value fails
    - manualAssetUpdateSchema: partial updates pass

    Run `pnpm test:run` and `pnpm lint` to verify.
  </action>
  <verify>`pnpm test:run` passes (all new tests + existing tests). `pnpm lint` passes. All 3 locale files have the new keys.</verify>
  <done>Navigation includes Bills, Goals, Net Worth links. All UI strings translated in en/zh/zh-TW. Component tests cover key rendering paths. Validation tests cover all new schemas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete bills, goals, and net worth features</name>
  <files>none</files>
  <action>
    Human verifies the complete Phase 22 feature set:
    1. Visit `/money/bills` -- verify bill list shows (may need to sync bills first via "Sync Bills" button if bank connected, or add a manual bill)
    2. Add a manual bill (name, amount, frequency, due date) -- verify it appears in the correct frequency section
    3. Switch to Calendar tab -- verify the bill appears on its due date
    4. Navigate months in calendar -- verify bills project forward correctly
    5. Visit `/money/goals` -- create a savings goal with target amount and deadline
    6. Add a contribution to the goal -- verify progress ring updates
    7. Check that projected completion date and color-coded status display correctly
    8. Visit `/money/net-worth` -- verify net worth total, chart, and account breakdown
    9. Toggle chart timeframes (1M/3M/6M/1Y/All) -- verify chart updates
    10. Add a manual asset (e.g., property) -- verify it appears in the accounts section and affects net worth total
    11. Verify navigation: MoneyPageShell shows links to Bills, Goals, Net Worth
    12. Check dark mode on all three pages
  </action>
  <verify>Human types "approved" or describes issues to fix.</verify>
  <done>Human has verified bills, goals, and net worth features work correctly in both light and dark modes.</done>
</task>

</tasks>

<verification>
- `pnpm test:run` -- all tests pass including new ones
- `pnpm lint` -- 0 errors
- Navigation links work for bills, goals, net worth
- i18n keys present in all 3 locale files
- Human has verified the complete feature set
</verification>

<success_criteria>
- Money page shell navigation includes Bills, Goals, Net Worth
- All UI strings translated in en, zh, zh-TW
- Component tests cover bills list, goal grid, net worth chart
- Validation tests cover all 6 new Zod schemas
- Human approves the complete Phase 22 feature set
</success_criteria>

<output>
After completion, create `.planning/phases/22-bills-goals-net-worth/22-06-SUMMARY.md`
</output>
