---
phase: 23-household-couples
plan: 04
type: execute
wave: 4
depends_on: [23-03]
files_modified:
  - tests/components/money/household-view-tabs.test.tsx
  - tests/components/money/household-settings.test.tsx
  - tests/lib/validations/household.test.ts
  - tests/app/api/money/household/invite-route.test.ts
  - tests/app/api/money/household/accept-route.test.ts
autonomous: false
requirements: [HOUS-01, HOUS-02, HOUS-03, HOUS-04, HOUS-05, HOUS-06, HOUS-07]

must_haves:
  truths:
    - "Component tests verify HouseholdViewTabs shows/hides based on member count"
    - "Component tests verify household settings shows invite for owner, leave for member"
    - "Validation tests cover all 3 Zod schemas (invite, visibility, transaction visibility)"
    - "API route tests verify invite/accept endpoint behavior"
    - "Human verifies end-to-end household flow works in the browser"
  artifacts:
    - path: "tests/components/money/household-view-tabs.test.tsx"
      provides: "Component tests for HouseholdViewTabs"
      contains: "HouseholdViewTabs"
    - path: "tests/lib/validations/household.test.ts"
      provides: "Validation schema tests"
      contains: "inviteSchema"
  key_links:
    - from: "tests/components/money/household-view-tabs.test.tsx"
      to: "components/money/household-view-tabs.tsx"
      via: "render and assert"
      pattern: "render.*HouseholdViewTabs"
---

<objective>
Write component tests, validation tests, and API route tests for the household/couples feature, then verify end-to-end functionality via human testing.

Purpose: Ensure all household features work correctly with automated tests and manual verification before marking Phase 23 complete.
Output: Test files, passing test suite, human-verified UI.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-household-couples/23-CONTEXT.md
@.planning/phases/23-household-couples/23-01-SUMMARY.md
@.planning/phases/23-household-couples/23-02-SUMMARY.md
@.planning/phases/23-household-couples/23-03-SUMMARY.md

Key test patterns:
@tests/setup.ts
@tests/components/money/bills-list.test.tsx (example component test)
@tests/lib/validations/bills-goals.test.ts (example validation test)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write component tests and validation tests</name>
  <files>
    tests/components/money/household-view-tabs.test.tsx
    tests/components/money/household-settings.test.tsx
    tests/lib/validations/household.test.ts
    tests/app/api/money/household/invite-route.test.ts
    tests/app/api/money/household/accept-route.test.ts
  </files>
  <action>
Follow existing test patterns from the project. Mock sub-components to isolate tests (same pattern as 20-05, 22-06).

**1. tests/components/money/household-view-tabs.test.tsx:**

Mock `next-intl` with `useTranslations: () => (key: string) => key`.

Test cases:
- "renders nothing when isMultiMember is false" -- render with isMultiMember=false, expect null output
- "renders Mine and Household tabs when isMultiMember is true" -- render with isMultiMember=true, expect both tab triggers
- "calls onValueChange when tab is clicked" -- click Household tab, verify callback called with 'household'
- "shows Mine tab as active by default" -- render with value='mine', verify active state
- Accessibility: run `axe` on rendered component, expect no violations

**2. tests/components/money/household-settings.test.tsx:**

Mock `useHousehold` and `useTranslations`. Mock sub-components (HouseholdMembersList, HouseholdInviteDialog).

Test cases:
- "shows invite button for owner" -- mock role=owner, verify invite trigger visible
- "hides invite button for non-owner" -- mock role=member, verify invite trigger not visible
- "shows member list" -- verify members rendered
- "shows leave button for non-owner member" -- mock role=member in multi-member household
- "hides leave button for owner" -- mock role=owner, verify no leave button

**3. tests/lib/validations/household.test.ts:**

Test all 3 Zod schemas in a single file (same pattern as bills-goals.test.ts):

inviteSchema:
- "accepts valid email" -- `{ email: "partner@example.com" }` passes
- "rejects invalid email" -- `{ email: "not-an-email" }` fails
- "rejects empty email" -- `{ email: "" }` fails

visibilityChangeSchema:
- "accepts 'mine'" -- passes
- "accepts 'ours'" -- passes
- "accepts 'hidden'" -- passes
- "rejects invalid visibility" -- `{ visibility: "public" }` fails

transactionVisibilitySchema:
- "accepts is_hidden_from_household boolean" -- passes
- "accepts is_shared_to_household boolean" -- passes
- "accepts both fields" -- passes
- "rejects non-boolean" -- fails

**4. tests/app/api/money/household/invite-route.test.ts:**

Mock pattern: vi.hoisted for mock functions, vi.mock for HouseholdsDB methods and supabase.

Test cases:
- "returns 401 when not authenticated"
- "returns 400 for invalid email"
- "returns 403 when non-owner tries to invite"
- "returns 400 when household is full (5 members)"
- "returns 201 with invitation data on success"
- "returns 409 when email already invited"

**5. tests/app/api/money/household/accept-route.test.ts:**

Test cases:
- "returns 401 when not authenticated"
- "returns 400 for missing token"
- "returns 400 for expired token"
- "returns 200 on successful acceptance"

Run all tests with `pnpm test:run` after writing. Fix any failures.
  </action>
  <verify>
Run `pnpm test:run` -- all tests pass including new ones.
Run `pnpm lint` -- 0 errors.
New test count: at least 20 new tests across the 5 files.
  </verify>
  <done>
All household component tests, validation tests, and API route tests pass. No regressions in existing test suite.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of household feature</name>
  <files>none</files>
  <action>
Human verifies the complete household/couples feature (Phase 23):
- Household invitation system with shareable invite links
- Account visibility controls (mine/ours/hidden)
- Transaction-level sharing and hiding
- Mine/Household view tabs on all money pages
- Household settings with member management
- Individual vs shared budgets and goals
- Transaction detail redaction in household view
- i18n in all 3 locales
  </action>
  <verify>
**Solo user behavior (no changes):**
1. Visit `/money` -- should see current view with NO tabs
2. Visit `/money/accounts` -- no visibility selectors, no household UI
3. Visit `/money/settings` -- should see Household section with "Invite Partner" button

**Invitation flow (if able to test with two accounts):**
4. As owner: Go to `/money/settings` -> click "Invite Partner" -> enter email -> verify invite link appears
5. Copy the invite link -> open in different browser/incognito (as the invitee)
6. Verify invite accept page processes correctly

**Household views (after partner joins):**
7. Verify Mine/Household tabs now appear on money pages
8. Switch to "Household" tab on accounts page -- verify shared accounts visible
9. Switch to "Household" tab on transactions page -- verify only category + amount shown (no merchant/description)
10. On "Mine" tab on accounts page -- verify visibility selector appears on each account

**Settings and membership:**
11. Verify owner sees member list with remove button
12. Verify non-owner sees "Leave Household" button

**Privacy:**
13. Set an account to "hidden" -- verify partner sees name + balance but no transactions in household view
14. Set a transaction as "hidden from household" on an "ours" account -- verify it disappears from household view
  </verify>
  <done>
Type "approved" if the household feature works correctly, or describe issues found.
  </done>
</task>

</tasks>

<verification>
- All new tests pass: component tests, validation tests, API route tests
- No regressions in existing test suite
- Human verified: solo user unaffected, invitation flow works, view switching works, privacy controls enforce correctly, transaction redaction working
- `pnpm lint` passes
- `pnpm test:run` passes
</verification>

<success_criteria>
Phase 23 is complete when all tests pass and human verification confirms the household/couples feature works end-to-end: invitation, membership, view switching, privacy controls, and data isolation.
</success_criteria>

<output>
After completion, create `.planning/phases/23-household-couples/23-04-SUMMARY.md`
</output>
