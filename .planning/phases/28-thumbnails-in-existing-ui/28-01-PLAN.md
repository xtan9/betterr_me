---
phase: 28-thumbnails-in-existing-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/fitness/exercise-thumbnail.tsx
  - tests/components/fitness/exercise-thumbnail.test.tsx
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
autonomous: true
requirements: [THUMB-04, I18N-01]

must_haves:
  truths:
    - "ExerciseThumbnail renders a circular GIF when gif_url is provided"
    - "ExerciseThumbnail shows a Dumbbell fallback icon when exercise_media is null or gif_url is null"
    - "ExerciseThumbnail shows a loading skeleton while image loads"
    - "Thumbnail alt text is translatable in all 3 locales"
  artifacts:
    - path: "components/fitness/exercise-thumbnail.tsx"
      provides: "Reusable ExerciseThumbnail component"
      exports: ["ExerciseThumbnail"]
    - path: "tests/components/fitness/exercise-thumbnail.test.tsx"
      provides: "Unit tests for thumbnail rendering and fallback"
    - path: "i18n/messages/en.json"
      provides: "exerciseThumbnailAlt key under exercises namespace"
      contains: "exerciseThumbnailAlt"
    - path: "i18n/messages/zh.json"
      provides: "Chinese translation for exerciseThumbnailAlt"
      contains: "exerciseThumbnailAlt"
    - path: "i18n/messages/zh-TW.json"
      provides: "Traditional Chinese translation for exerciseThumbnailAlt"
      contains: "exerciseThumbnailAlt"
  key_links:
    - from: "components/fitness/exercise-thumbnail.tsx"
      to: "lib/db/types.ts"
      via: "ExerciseMedia type import"
      pattern: "import.*ExerciseMedia.*from.*lib/db/types"
---

<objective>
Create the reusable ExerciseThumbnail component and add i18n alt text strings.

Purpose: This component is the building block used by all 3 integration points (exercise picker, workout logger, exercise library). Building it first with tests ensures consistent behavior across all consumers.

Output: ExerciseThumbnail component, unit tests, i18n strings in all 3 locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-data-layer-sync/27-01-SUMMARY.md

<interfaces>
<!-- Key types the executor needs -->

From lib/db/types.ts:
```typescript
export interface ExerciseMedia {
  gif_url: string | null;
  thumbnail_url: string | null;
  instructions: string[] | null;
  alternative_names: string[] | null;
  exercisedb_id: string | null;
  media_status: "active" | "broken" | "fallback";
}

export type MuscleGroup = (typeof MUSCLE_GROUPS)[number];
// MUSCLE_GROUPS includes: chest, back, shoulders, biceps, triceps, forearms, core, quads, hamstrings, glutes, calves, cardio, full_body, other
```

Existing fallback pattern in codebase:
```typescript
import { Dumbbell } from "lucide-react";
// Used in workout-logger.tsx and exercise-library.tsx as empty-state icon
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ExerciseThumbnail component with tests</name>
  <files>components/fitness/exercise-thumbnail.tsx, tests/components/fitness/exercise-thumbnail.test.tsx</files>
  <read_first>
    - lib/db/types.ts (ExerciseMedia interface, MuscleGroup type)
    - components/fitness/workout-logger/workout-add-exercise.tsx (see how exercises are rendered for sizing context)
    - components/fitness/exercise-library/exercise-card.tsx (see card layout for sizing context)
    - next.config.ts (confirm v2.exercisedb.io is in remotePatterns)
  </read_first>
  <behavior>
    - Test 1: Renders img element with gif_url when exercise_media.gif_url is provided
    - Test 2: Renders Dumbbell icon fallback when exercise_media is null
    - Test 3: Renders Dumbbell icon fallback when exercise_media.gif_url is null
    - Test 4: Renders Dumbbell icon fallback when media_status is "broken"
    - Test 5: Applies circular crop (rounded-full class) to the container
    - Test 6: Accepts size prop ("sm" | "md" | "lg") with sm=32px, md=40px, lg=64px defaults
    - Test 7: Renders alt text from exerciseName prop
  </behavior>
  <action>
    Create `components/fitness/exercise-thumbnail.tsx`:

    Props interface:
    ```typescript
    interface ExerciseThumbnailProps {
      exerciseMedia: ExerciseMedia | null;
      exerciseName: string;
      size?: "sm" | "md" | "lg";
      className?: string;
    }
    ```

    Component behavior:
    - "use client" directive (uses useState for loading state)
    - Size map: sm=32px (8 tailwind), md=40px (10 tailwind), lg=64px (16 tailwind)
    - Container: div with `rounded-full overflow-hidden bg-muted flex items-center justify-center shrink-0` and size classes
    - When gif_url exists AND media_status is not "broken":
      - Render `<img>` tag (NOT next/image — these are small thumbnails from CDN, unoptimized GIFs)
      - Set src={gif_url}, alt={exerciseName}, className="h-full w-full object-cover"
      - Use useState for loading state — show Skeleton while loading, show img on load
      - On error: show Dumbbell fallback icon
    - When gif_url is null OR exercise_media is null OR media_status is "broken":
      - Render `<Dumbbell>` from lucide-react with `text-muted-foreground` and proportional sizing (h-4 w-4 for sm, h-5 w-5 for md, h-8 w-8 for lg)
    - Export as named export: `export function ExerciseThumbnail`

    Write tests first (RED), then implement (GREEN). Use vitest + testing-library. Mock next-intl with `vi.mock("next-intl", () => ({ useTranslations: () => (key: string) => key }))`.
  </action>
  <verify>
    <automated>cd /Users/xingditan/Code/betterr_me && npx vitest run tests/components/fitness/exercise-thumbnail.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function ExerciseThumbnail" components/fitness/exercise-thumbnail.tsx
    - grep -q "rounded-full" components/fitness/exercise-thumbnail.tsx
    - grep -q "Dumbbell" components/fitness/exercise-thumbnail.tsx
    - grep -q "exercise-thumbnail" tests/components/fitness/exercise-thumbnail.test.tsx
    - grep -q "broken" components/fitness/exercise-thumbnail.tsx
  </acceptance_criteria>
  <done>ExerciseThumbnail component renders circular GIF thumbnails with loading state, error handling, and Dumbbell fallback. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n thumbnail alt text strings to all 3 locale files</name>
  <files>i18n/messages/en.json, i18n/messages/zh.json, i18n/messages/zh-TW.json</files>
  <read_first>
    - i18n/messages/en.json (find "exercises" namespace to add key)
    - i18n/messages/zh.json (find "exercises" namespace)
    - i18n/messages/zh-TW.json (find "exercises" namespace)
  </read_first>
  <action>
    Add the following key inside the "exercises" object in each locale file:

    **en.json** — inside "exercises" object:
    ```json
    "exerciseThumbnailAlt": "{name} exercise demonstration"
    ```

    **zh.json** — inside "exercises" object:
    ```json
    "exerciseThumbnailAlt": "{name} 动作演示"
    ```

    **zh-TW.json** — inside "exercises" object:
    ```json
    "exerciseThumbnailAlt": "{name} 動作演示"
    ```

    Place the new key near the bottom of the "exercises" object, before the closing brace. The `{name}` placeholder is an ICU message format interpolation that next-intl handles automatically.

    Per I18N-01: All thumbnail UI strings must exist in all 3 locale files.
  </action>
  <verify>
    <automated>cd /Users/xingditan/Code/betterr_me && grep -c "exerciseThumbnailAlt" i18n/messages/en.json i18n/messages/zh.json i18n/messages/zh-TW.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "exerciseThumbnailAlt" i18n/messages/en.json
    - grep -q "exerciseThumbnailAlt" i18n/messages/zh.json
    - grep -q "exerciseThumbnailAlt" i18n/messages/zh-TW.json
    - grep -q "{name}" i18n/messages/en.json (interpolation placeholder)
  </acceptance_criteria>
  <done>All 3 locale files contain exerciseThumbnailAlt key with proper translations and {name} interpolation.</done>
</task>

</tasks>

<verification>
- ExerciseThumbnail component exists and exports correctly
- All tests pass for the component
- All 3 locale files have exerciseThumbnailAlt key
- Component handles: gif_url present, gif_url null, exercise_media null, media_status broken
</verification>

<success_criteria>
- ExerciseThumbnail renders circular GIF when media available, Dumbbell fallback when not
- Component accepts size prop (sm/md/lg) for different integration contexts
- Loading skeleton shown while GIF loads
- Alt text strings present in en, zh, zh-TW
</success_criteria>

<output>
After completion, create `.planning/phases/28-thumbnails-in-existing-ui/28-01-SUMMARY.md`
</output>
