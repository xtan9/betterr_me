---
phase: 32-calendar-ui-week-day-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/calendar/date-utils.ts
  - hooks/use-keyboard-shortcuts.ts
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
autonomous: true
requirements: [VIEW-12]

must_haves:
  truths:
    - "getWeekDateRange, getDayDateRange, and getWeekDates functions exist in date-utils.ts"
    - "useKeyboardShortcuts hook handles D/W/M/T/arrows/C/N/Esc keys"
    - "Keyboard shortcuts are suppressed when focus is in text inputs or textareas"
    - "All 3 locale files have Phase 32 i18n keys for time grid, quick-create, event dialog, shortcuts, and time labels"
  artifacts:
    - path: "lib/calendar/date-utils.ts"
      provides: "getWeekDateRange, getDayDateRange, getWeekDates date utility functions"
      exports: ["getWeekDateRange", "getDayDateRange", "getWeekDates"]
    - path: "hooks/use-keyboard-shortcuts.ts"
      provides: "useKeyboardShortcuts hook for calendar page keyboard shortcuts"
      exports: ["useKeyboardShortcuts"]
    - path: "i18n/messages/en.json"
      provides: "Phase 32 calendar i18n keys: timeGrid, quickCreate, eventDialog, shortcuts, timeLabels"
      contains: "timeGrid"
  key_links:
    - from: "hooks/use-keyboard-shortcuts.ts"
      to: "components/calendar/calendar-page-content.tsx"
      via: "imported and called in CalendarPageContent"
      pattern: "useKeyboardShortcuts"
    - from: "lib/calendar/date-utils.ts"
      to: "components/calendar/calendar-page-content.tsx"
      via: "getWeekDateRange and getDayDateRange used for SWR key computation"
      pattern: "getWeekDateRange"
---

<objective>
Add date utility functions for week/day views, keyboard shortcuts hook, and i18n strings for all Phase 32 UI.

Purpose: Foundation layer with no UI dependencies that all subsequent plans build on.
Output: Three new date-utils exports, a reusable keyboard shortcuts hook, and ~30 new i18n keys in all 3 locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-calendar-ui-week-day-views/32-CONTEXT.md
@.planning/phases/32-calendar-ui-week-day-views/32-RESEARCH.md

<interfaces>
From lib/calendar/date-utils.ts (existing exports):
```ts
export function getMonthGridDates(year: number, month: number, weekStartDay: number): Date[]
export function getMonthDateRange(year: number, month: number, weekStartDay: number): { startDate: string; endDate: string }
export function groupEventsByDate(events: ExpandedCalendarEvent[]): Map<string, ExpandedCalendarEvent[]>
```

From lib/utils.ts:
```ts
export function getLocalDateString(date?: Date): string  // returns YYYY-MM-DD
```

Existing i18n calendar namespace keys (en.json):
```json
{
  "calendar": {
    "title": "Calendar",
    "views": { "day": "Day", "week": "Week", "month": "Month" },
    "navigation": { "today": "Today", "previous": "Previous", "next": "Next" },
    "layers": { "events": "Events", "tasks": "Tasks", "habits": "Habits", "bills": "Bills", "workouts": "Workouts" },
    "overflow": { "more": "+{count} more" },
    "sidebar": { "newEvent": "+ New Event", "layers": "Layers", "comingSoonPhase": "Coming in a future phase" },
    "comingSoon": "Coming soon",
    "noEvents": "No events",
    "error": "Failed to load calendar. Please try refreshing."
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add week/day date utility functions to date-utils.ts</name>
  <files>lib/calendar/date-utils.ts</files>
  <read_first>lib/calendar/date-utils.ts, lib/utils.ts</read_first>
  <action>
Add three new exported functions to `lib/calendar/date-utils.ts` (below the existing `groupEventsByDate` function):

1. **`getWeekDates(date: Date, weekStartDay: number): Date[]`**
   - Returns an array of 7 Date objects for the week containing `date`, starting from `weekStartDay`
   - Calculate the start of the week:
     ```ts
     const day = date.getDay();
     const daysBack = (day - weekStartDay + 7) % 7;
     const weekStart = new Date(date.getFullYear(), date.getMonth(), date.getDate() - daysBack);
     ```
   - Push 7 consecutive dates starting from weekStart
   - Return the array

2. **`getWeekDateRange(date: Date, weekStartDay: number): { startDate: string; endDate: string }`**
   - Uses `getWeekDates(date, weekStartDay)` to get the 7 dates
   - Returns `{ startDate: getLocalDateString(dates[0]), endDate: getLocalDateString(dates[6]) }`

3. **`getDayDateRange(date: Date): { startDate: string; endDate: string }`**
   - Returns `{ startDate: getLocalDateString(date), endDate: getLocalDateString(date) }`
   - Simplest utility — same date for both start and end
  </action>
  <verify>
    <automated>grep -q "getWeekDates" lib/calendar/date-utils.ts && grep -q "getWeekDateRange" lib/calendar/date-utils.ts && grep -q "getDayDateRange" lib/calendar/date-utils.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- lib/calendar/date-utils.ts exports `getWeekDates` that returns a 7-element Date array
- lib/calendar/date-utils.ts exports `getWeekDateRange` that returns `{ startDate: string; endDate: string }` using getWeekDates
- lib/calendar/date-utils.ts exports `getDayDateRange` that returns `{ startDate: string; endDate: string }` with same date for both
- All 3 functions use `getLocalDateString` from `@/lib/utils` for date string formatting (consistent with existing code)
  </acceptance_criteria>
  <done>Three new date utility functions exported from date-utils.ts for week and day view date range computation.</done>
</task>

<task type="auto">
  <name>Task 2: Create useKeyboardShortcuts hook</name>
  <files>hooks/use-keyboard-shortcuts.ts</files>
  <read_first>components/calendar/calendar-page-content.tsx</read_first>
  <action>
Create `hooks/use-keyboard-shortcuts.ts` with the following implementation:

```ts
"use client";

import { useEffect } from "react";

interface KeyboardShortcutHandlers {
  onDayView: () => void;
  onWeekView: () => void;
  onMonthView: () => void;
  onToday: () => void;
  onPrev: () => void;
  onNext: () => void;
  onQuickCreate: () => void;
  onNewEvent: () => void;
  onSearch: () => void;
  onEscape: () => void;
  /** When true, only Esc is active (popover/dialog is open) */
  isOverlayOpen?: boolean;
}

export function useKeyboardShortcuts(handlers: KeyboardShortcutHandlers) {
  useEffect(() => {
    function handleKeyDown(e: KeyboardEvent) {
      // Suppress shortcuts when focus is in text inputs, textareas, or contenteditable
      const target = e.target as HTMLElement;
      const tagName = target.tagName.toLowerCase();
      if (
        tagName === "input" ||
        tagName === "textarea" ||
        target.isContentEditable
      ) {
        // Allow Esc even in inputs
        if (e.key === "Escape") {
          handlers.onEscape();
          return;
        }
        return;
      }

      // When overlay is open, only Esc works
      if (handlers.isOverlayOpen) {
        if (e.key === "Escape") {
          handlers.onEscape();
        }
        return;
      }

      switch (e.key) {
        case "d":
        case "D":
          e.preventDefault();
          handlers.onDayView();
          break;
        case "w":
        case "W":
          e.preventDefault();
          handlers.onWeekView();
          break;
        case "m":
        case "M":
          e.preventDefault();
          handlers.onMonthView();
          break;
        case "t":
        case "T":
          e.preventDefault();
          handlers.onToday();
          break;
        case "ArrowLeft":
          e.preventDefault();
          handlers.onPrev();
          break;
        case "ArrowRight":
          e.preventDefault();
          handlers.onNext();
          break;
        case "c":
        case "C":
          e.preventDefault();
          handlers.onQuickCreate();
          break;
        case "n":
        case "N":
          e.preventDefault();
          handlers.onNewEvent();
          break;
        case "/":
          e.preventDefault();
          handlers.onSearch();
          break;
        case "Escape":
          handlers.onEscape();
          break;
      }
    }

    document.addEventListener("keydown", handleKeyDown);
    return () => document.removeEventListener("keydown", handleKeyDown);
  }, [handlers]);
}
```

Key implementation details:
- Global `keydown` listener on `document` (per D-21)
- Suppressed when focus is in `input`, `textarea`, or `contenteditable` elements (per D-22)
- Only `Esc` works when `isOverlayOpen` is true (per D-22)
- Case-insensitive for letter keys (matches both `d` and `D`)
- `e.preventDefault()` on all handled keys to prevent default browser behavior
- Cleanup on unmount via return function in useEffect
  </action>
  <verify>
    <automated>test -f hooks/use-keyboard-shortcuts.ts && grep -q "useKeyboardShortcuts" hooks/use-keyboard-shortcuts.ts && grep -q "isOverlayOpen" hooks/use-keyboard-shortcuts.ts && grep -q "isContentEditable" hooks/use-keyboard-shortcuts.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- hooks/use-keyboard-shortcuts.ts exists and exports `useKeyboardShortcuts`
- Hook accepts `KeyboardShortcutHandlers` interface with handlers for D/W/M/T/arrows/C/N/slash/Esc
- Hook checks `target.tagName` for "input"/"textarea" and `target.isContentEditable` before firing shortcuts
- Hook has `isOverlayOpen` prop that restricts to only Esc when true
- Hook adds listener on mount and removes on unmount
- Each key handler calls `e.preventDefault()` before invoking the callback
  </acceptance_criteria>
  <done>Keyboard shortcuts hook created with all 10 shortcut bindings, input suppression, and overlay-open restriction.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 32 i18n strings to all 3 locale files</name>
  <files>i18n/messages/en.json, i18n/messages/zh.json, i18n/messages/zh-TW.json</files>
  <read_first>i18n/messages/en.json, i18n/messages/zh.json, i18n/messages/zh-TW.json</read_first>
  <action>
Add the following new keys INSIDE the existing `"calendar"` namespace in all 3 locale files. Add them after the existing `"error"` key, before the closing `}` of the calendar object.

**English (en.json) — add these keys inside the `"calendar"` block:**
```json
"timeGrid": {
  "allDay": "All day",
  "allDayMore": "+{count} more",
  "collapseAllDay": "Show less"
},
"quickCreate": {
  "titlePlaceholder": "Add title",
  "moreOptions": "More options",
  "saving": "Saving..."
},
"eventDialog": {
  "newEvent": "New Event",
  "editEvent": "Edit Event",
  "title": "Title",
  "titlePlaceholder": "Add title",
  "date": "Date",
  "startTime": "Start time",
  "endTime": "End time",
  "allDay": "All day",
  "location": "Location",
  "locationPlaceholder": "Add location",
  "description": "Description",
  "descriptionPlaceholder": "Add description",
  "category": "Category",
  "color": "Color",
  "recurrence": "Repeat",
  "reminders": "Reminders",
  "save": "Save",
  "cancel": "Cancel",
  "delete": "Delete",
  "deleteConfirm": "Are you sure you want to delete this event?"
},
"shortcuts": {
  "dayView": "Day view (D)",
  "weekView": "Week view (W)",
  "monthView": "Month view (M)",
  "today": "Today (T)",
  "navigate": "Navigate (← →)",
  "quickCreate": "Quick create (C)",
  "newEvent": "New event (N)",
  "search": "Search (/)",
  "close": "Close (Esc)"
},
"timeLabels": {
  "am": "AM",
  "pm": "PM"
},
"weekHeader": {
  "rangeTitle": "{start} – {end}, {year}"
},
"dayHeader": {
  "dateTitle": "{weekday}, {date}"
}
```

**Simplified Chinese (zh.json) — add these keys inside the `"calendar"` block:**
```json
"timeGrid": {
  "allDay": "全天",
  "allDayMore": "+{count} 更多",
  "collapseAllDay": "收起"
},
"quickCreate": {
  "titlePlaceholder": "添加标题",
  "moreOptions": "更多选项",
  "saving": "保存中..."
},
"eventDialog": {
  "newEvent": "新事件",
  "editEvent": "编辑事件",
  "title": "标题",
  "titlePlaceholder": "添加标题",
  "date": "日期",
  "startTime": "开始时间",
  "endTime": "结束时间",
  "allDay": "全天",
  "location": "地点",
  "locationPlaceholder": "添加地点",
  "description": "描述",
  "descriptionPlaceholder": "添加描述",
  "category": "分类",
  "color": "颜色",
  "recurrence": "重复",
  "reminders": "提醒",
  "save": "保存",
  "cancel": "取消",
  "delete": "删除",
  "deleteConfirm": "确定要删除这个事件吗？"
},
"shortcuts": {
  "dayView": "日视图 (D)",
  "weekView": "周视图 (W)",
  "monthView": "月视图 (M)",
  "today": "今天 (T)",
  "navigate": "导航 (← →)",
  "quickCreate": "快速创建 (C)",
  "newEvent": "新事件 (N)",
  "search": "搜索 (/)",
  "close": "关闭 (Esc)"
},
"timeLabels": {
  "am": "上午",
  "pm": "下午"
},
"weekHeader": {
  "rangeTitle": "{start} – {end}, {year}"
},
"dayHeader": {
  "dateTitle": "{weekday}, {date}"
}
```

**Traditional Chinese (zh-TW.json) — add these keys inside the `"calendar"` block:**
```json
"timeGrid": {
  "allDay": "全天",
  "allDayMore": "+{count} 更多",
  "collapseAllDay": "收起"
},
"quickCreate": {
  "titlePlaceholder": "新增標題",
  "moreOptions": "更多選項",
  "saving": "儲存中..."
},
"eventDialog": {
  "newEvent": "新事件",
  "editEvent": "編輯事件",
  "title": "標題",
  "titlePlaceholder": "新增標題",
  "date": "日期",
  "startTime": "開始時間",
  "endTime": "結束時間",
  "allDay": "全天",
  "location": "地點",
  "locationPlaceholder": "新增地點",
  "description": "描述",
  "descriptionPlaceholder": "新增描述",
  "category": "分類",
  "color": "顏色",
  "recurrence": "重複",
  "reminders": "提醒",
  "save": "儲存",
  "cancel": "取消",
  "delete": "刪除",
  "deleteConfirm": "確定要刪除這個事件嗎？"
},
"shortcuts": {
  "dayView": "日檢視 (D)",
  "weekView": "週檢視 (W)",
  "monthView": "月檢視 (M)",
  "today": "今天 (T)",
  "navigate": "導航 (← →)",
  "quickCreate": "快速建立 (C)",
  "newEvent": "新事件 (N)",
  "search": "搜尋 (/)",
  "close": "關閉 (Esc)"
},
"timeLabels": {
  "am": "上午",
  "pm": "下午"
},
"weekHeader": {
  "rangeTitle": "{start} – {end}, {year}"
},
"dayHeader": {
  "dateTitle": "{weekday}, {date}"
}
```
  </action>
  <verify>
    <automated>node -e "const en=require('./i18n/messages/en.json'); const zh=require('./i18n/messages/zh.json'); const tw=require('./i18n/messages/zh-TW.json'); if(en.calendar.timeGrid && en.calendar.quickCreate && en.calendar.eventDialog && en.calendar.shortcuts && en.calendar.timeLabels && zh.calendar.timeGrid && tw.calendar.timeGrid) console.log('PASS'); else console.log('FAIL');"</automated>
  </verify>
  <acceptance_criteria>
- en.json calendar namespace contains keys: timeGrid.allDay, quickCreate.titlePlaceholder, eventDialog.newEvent, eventDialog.save, eventDialog.deleteConfirm, shortcuts.dayView, timeLabels.am, weekHeader.rangeTitle, dayHeader.dateTitle
- zh.json calendar namespace contains same key structure with Simplified Chinese values (e.g., timeGrid.allDay = "全天", eventDialog.save = "保存")
- zh-TW.json calendar namespace contains same key structure with Traditional Chinese values (e.g., eventDialog.save = "儲存", quickCreate.titlePlaceholder = "新增標題")
- All 3 files remain valid JSON after editing
  </acceptance_criteria>
  <done>Phase 32 i18n strings added to all 3 locale files covering time grid, quick-create, event dialog, keyboard shortcuts, and time labels.</done>
</task>

</tasks>

<verification>
1. `grep -q "getWeekDates\|getWeekDateRange\|getDayDateRange" lib/calendar/date-utils.ts` — all 3 new functions exist
2. `test -f hooks/use-keyboard-shortcuts.ts && grep -q "useKeyboardShortcuts" hooks/use-keyboard-shortcuts.ts` — hook exists
3. `node -e "const en=require('./i18n/messages/en.json'); console.log(!!en.calendar.timeGrid && !!en.calendar.eventDialog)"` — i18n keys exist
4. `node -e "JSON.parse(require('fs').readFileSync('i18n/messages/en.json','utf8')); console.log('valid JSON')"` — en.json is valid JSON
</verification>

<success_criteria>
- date-utils.ts has 3 new exported functions for week/day date range computation
- useKeyboardShortcuts hook handles all 10 keyboard shortcuts with input suppression and overlay-open restriction
- All 3 locale files have ~30 new i18n keys under the calendar namespace for Phase 32 UI
</success_criteria>

<output>
After completion, create `.planning/phases/32-calendar-ui-week-day-views/32-01-SUMMARY.md`
</output>
