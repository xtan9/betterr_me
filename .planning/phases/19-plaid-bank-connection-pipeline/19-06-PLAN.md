---
phase: 19-plaid-bank-connection-pipeline
plan: 06
type: execute
wave: 3
depends_on: [19-03, 19-05]
files_modified:
  - components/money/manual-transaction-dialog.tsx
  - components/money/accounts-list.tsx
  - lib/validations/plaid.ts
  - tests/components/money/manual-transaction-dialog.test.tsx
  - tests/lib/validations/plaid.test.ts
autonomous: false
requirements: [PLAD-01, PLAD-05, PLAD-06, PLAD-07, PLAD-08]

must_haves:
  truths:
    - "User can open a manual transaction entry dialog from the accounts page 'Other options' dropdown"
    - "Manual entry requires amount, description, date, and category"
    - "User can pick which account the transaction belongs to including Cash"
    - "All existing tests still pass after all Phase 19 changes"
    - "Lint passes across all new and modified files"
  artifacts:
    - path: "components/money/manual-transaction-dialog.tsx"
      provides: "Manual transaction entry modal"
      exports: ["ManualTransactionDialog"]
    - path: "tests/components/money/manual-transaction-dialog.test.tsx"
      provides: "Manual entry dialog tests"
      min_lines: 40
    - path: "tests/lib/validations/plaid.test.ts"
      provides: "Validation schema tests"
      min_lines: 30
  key_links:
    - from: "components/money/manual-transaction-dialog.tsx"
      to: "/api/money/transactions"
      via: "POST fetch to create transaction"
      pattern: "api/money/transactions"
    - from: "components/money/manual-transaction-dialog.tsx"
      to: "lib/validations/plaid.ts"
      via: "manualTransactionSchema for form validation"
      pattern: "manualTransactionSchema"
    - from: "components/money/accounts-list.tsx"
      to: "components/money/manual-transaction-dialog.tsx"
      via: "import and render ManualTransactionDialog, opened from Other options dropdown"
      pattern: "ManualTransactionDialog"
---

<objective>
Create the manual transaction entry dialog, wire it into AccountsList's "Other options" dropdown, validate all Zod schemas with tests, and do a final integration verification of the entire Phase 19 pipeline.

Purpose: Complete the fallback flow for users who want to enter cash transactions or don't use Plaid, and ensure everything works together.
Output: Manual entry dialog wired to AccountsList, validation tests, verified end-to-end pipeline.
</objective>

<execution_context>
@/home/xingdi/.claude/get-shit-done/workflows/execute-plan.md
@/home/xingdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-plaid-bank-connection-pipeline/19-CONTEXT.md
@.planning/phases/19-plaid-bank-connection-pipeline/19-01-SUMMARY.md
@.planning/phases/19-plaid-bank-connection-pipeline/19-03-SUMMARY.md
@.planning/phases/19-plaid-bank-connection-pipeline/19-04-SUMMARY.md
@.planning/phases/19-plaid-bank-connection-pipeline/19-05-SUMMARY.md
@components/money/accounts-list.tsx
@lib/validations/plaid.ts
@lib/hooks/use-accounts.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manual transaction dialog, wire to AccountsList, and add validation tests</name>
  <files>
    components/money/manual-transaction-dialog.tsx
    components/money/accounts-list.tsx
    tests/components/money/manual-transaction-dialog.test.tsx
    tests/lib/validations/plaid.test.ts
  </files>
  <action>
    **1. `components/money/manual-transaction-dialog.tsx`** -- "use client":
    - Dialog pattern (same as existing project modals -- Dialog from shadcn/ui)
    - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `accounts: MoneyAccount[]`, `onSuccess?: () => void`
    - Form using react-hook-form + zodResolver with `manualTransactionSchema`
    - Fields per locked decision:
      - **Amount** (required): number input, formatted as currency
      - **Description** (required): text input, min 1 char
      - **Date** (required): date input, defaults to today via `getLocalDateString()`
      - **Category** (optional): text input or select from common categories
      - **Account** (required): select dropdown with all user accounts + "Cash" option
    - "Cash" option in account select:
      - Value: "cash" (special string)
      - When selected, the API route handles Cash account auto-creation
    - On submit:
      - POST to `/api/money/transactions` with validated form data
      - Show success toast via sonner
      - Call `onSuccess()` to refresh parent data
      - Close dialog
    - Error handling: show inline field errors (react-hook-form), toast on server error
    - Calm Finance styling: `bg-money-surface border-money-border` for dialog content
    - Use `useTranslations('money')` for all labels

    **2. Update `components/money/accounts-list.tsx`**:
    - Import `ManualTransactionDialog`
    - Add `const [manualDialogOpen, setManualDialogOpen] = useState(false)` state
    - Wire the existing "Other options" dropdown item (created in Plan 04) to call `setManualDialogOpen(true)` on click
    - Render `<ManualTransactionDialog open={manualDialogOpen} onOpenChange={setManualDialogOpen} accounts={allAccounts} onSuccess={() => mutate()} />` in the component tree
    - `allAccounts` is derived from flattening all connections' accounts arrays from the SWR data

    **3. `tests/components/money/manual-transaction-dialog.test.tsx`** (5 tests):
    - Renders form fields (amount, description, date, category, account)
    - Shows account options including "Cash"
    - Submit button is disabled when form is invalid
    - Submits valid form data to API
    - Has no accessibility violations (axe)

    Mock react-hook-form behaviors, mock fetch for API calls.

    **4. `tests/lib/validations/plaid.test.ts`** (8+ tests):
    Test `manualTransactionSchema`:
    - Valid data passes
    - Missing amount fails
    - Negative amount fails (amounts are always positive in form; sign determined by income/expense toggle or inferred)
    - Missing description fails
    - Empty description (whitespace only) fails
    - Missing transaction_date fails
    - Invalid date format fails
    - Missing account_id fails

    Test `exchangeTokenSchema`:
    - Valid public-xxx token passes
    - Missing public_token fails
    - Token not starting with 'public-' fails (if this validation is in the schema)

    Test `webhookPayloadSchema`:
    - Valid payload passes
    - Missing webhook_type fails
  </action>
  <verify>
    - `pnpm lint` passes
    - `pnpm test:run` passes all tests
    - Manual transaction dialog renders with all required fields
    - AccountsList "Other options" dropdown opens ManualTransactionDialog
    - Validation tests cover all edge cases
  </verify>
  <done>Manual transaction dialog with react-hook-form + zod validation, wired to AccountsList "Other options" dropdown, 5 component tests, and 8+ validation schema tests all passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 19 UI and integration</name>
  <files>none (verification only)</files>
  <action>
    Human verification of the complete Phase 19 Plaid bank connection pipeline:
    1. Database migration with Plaid columns + Vault wrapper functions
    2. Plaid server library (client, token exchange, sync, webhook verification)
    3. 8 API routes (link-token, exchange, webhook, accounts, sync, disconnect, transactions, cron)
    4. Accounts page UI with Plaid Link, account cards, sync badges, net worth
    5. Manual transaction entry dialog (opened from "Other options" dropdown)
    6. i18n strings in all three locales
    7. Full test coverage

    Steps to verify:
    1. Run `pnpm test:run` -- all tests should pass (no regressions)
    2. Run `pnpm lint` -- no lint errors
    3. Run `pnpm tsc --noEmit` -- compiles without errors
    4. Start dev server: `pnpm dev`
    5. Navigate to /money -- should see updated money page with link to accounts
    6. Navigate to /money/accounts -- should see empty state with "Connect Bank Account" button
    7. Verify i18n: change locale to zh -- accounts page labels should be in Chinese
    8. Review the sync status badge: check that component renders four distinct states (syncing, synced, stale, error)
    9. Verify manual entry button is present in "Other options" dropdown (secondary, not primary CTA)
    10. Click "Other options" > "Enter transaction manually" -- should open ManualTransactionDialog

    Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>User confirms UI renders correctly and all automated checks pass</verify>
  <done>Phase 19 human-verified: accounts page renders, empty state shows, Plaid Link button present, i18n works, manual entry accessible from Other options dropdown, sync status badge shows 4 states</done>
</task>

</tasks>

<verification>
- `pnpm lint` passes across entire codebase
- `pnpm test:run` passes all tests (existing + new)
- `pnpm tsc --noEmit` compiles without errors
- /money/accounts page renders with empty state
- Manual transaction dialog opens from "Other options" dropdown and shows all fields
- Validation schemas reject invalid input
- i18n complete in all three locales
</verification>

<success_criteria>
1. Manual transaction dialog works with react-hook-form + zod validation
2. ManualTransactionDialog is wired to AccountsList "Other options" dropdown
3. Account selector includes Cash option for unbanked transactions
4. 13+ tests added (5 dialog + 8+ validation)
5. All Phase 19 tests pass together with no regressions
6. Lint and TypeScript compilation clean
7. Human verification confirms UI renders correctly
</success_criteria>

<output>
After completion, create `.planning/phases/19-plaid-bank-connection-pipeline/19-06-SUMMARY.md`
</output>
