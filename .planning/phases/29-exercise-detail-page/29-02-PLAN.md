---
phase: 29-exercise-detail-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/fitness/exercise-library/exercise-card.tsx
  - components/fitness/workout-logger/workout-exercise-card.tsx
autonomous: true
requirements: [DETL-05]

must_haves:
  truths:
    - "Clicking an exercise name in the workout logger navigates to /workouts/exercises/[id]"
    - "Clicking an exercise card in the exercise library navigates to /workouts/exercises/[id]"
  artifacts:
    - path: "components/fitness/exercise-library/exercise-card.tsx"
      provides: "Clickable card linking to exercise detail page"
      contains: "/workouts/exercises/"
    - path: "components/fitness/workout-logger/workout-exercise-card.tsx"
      provides: "Clickable exercise name linking to detail page"
      contains: "/workouts/exercises/"
  key_links:
    - from: "components/fitness/exercise-library/exercise-card.tsx"
      to: "/workouts/exercises/[id]"
      via: "Next.js Link component wrapping card"
      pattern: "Link.*workouts/exercises"
    - from: "components/fitness/workout-logger/workout-exercise-card.tsx"
      to: "/workouts/exercises/[id]"
      via: "Next.js Link on exercise name"
      pattern: "Link.*workouts/exercises"
---

<objective>
Add navigation links from existing exercise UIs to the new exercise detail page. Exercise card in library becomes clickable, and exercise name in workout logger becomes a link.

Purpose: Users can discover and navigate to the exercise detail page from both primary exercise touchpoints.
Output: Two modified components with working navigation to /workouts/exercises/[id].
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current component structure to modify -->

From components/fitness/exercise-library/exercise-card.tsx:
```typescript
interface ExerciseCardProps {
  exercise: Exercise;
  onEdit?: (exercise: Exercise) => void;
  onDelete?: (exercise: Exercise) => void;
}
// Currently renders a Card with no link wrapping
```

From components/fitness/workout-logger/workout-exercise-card.tsx:
```typescript
interface WorkoutExerciseCardProps {
  exercise: WorkoutExerciseWithPrevious;
  weightUnit: WeightUnit;
  // ... other callbacks
}
// Exercise name rendered at line 141: <h3 className="text-sm font-semibold">{exerciseInfo.name}</h3>
// exerciseInfo = exercise.exercise (has .id field)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation links to exercise card and workout exercise card</name>
  <files>
    components/fitness/exercise-library/exercise-card.tsx
    components/fitness/workout-logger/workout-exercise-card.tsx
  </files>
  <read_first>
    components/fitness/exercise-library/exercise-card.tsx
    components/fitness/workout-logger/workout-exercise-card.tsx
  </read_first>
  <action>
    **1. `components/fitness/exercise-library/exercise-card.tsx`:**
    - Import `Link` from `next/link`
    - Wrap the entire `Card` component in a `Link` with `href={/workouts/exercises/${exercise.id}}`
    - Add `className="block"` to the Link to ensure proper block layout
    - The existing dropdown menu for edit/delete on custom exercises must still work — use `e.preventDefault()` and `e.stopPropagation()` on the DropdownMenuTrigger's `onClick` to prevent Link navigation when interacting with the dropdown. Apply the same on `DropdownMenuContent` to prevent click-through.
    - Add hover effect to the Card: add `hover:border-primary/50 transition-colors cursor-pointer` to Card className

    **2. `components/fitness/workout-logger/workout-exercise-card.tsx`:**
    - Import `Link` from `next/link`
    - Convert the exercise name `<h3>` at approximately line 141 (the one showing `{exerciseInfo.name}`) into a `Link` component:
      - `href={/workouts/exercises/${exerciseInfo.id}}`
      - Keep existing className `"text-sm font-semibold"` and add `"hover:underline"` for affordance
      - The link should open in a new tab during an active workout so users don't lose workout state: add `target="_blank"` and `rel="noopener noreferrer"`
    - Do NOT wrap the entire card in a Link (the card has complex interactive elements like set inputs, buttons, etc.)
  </action>
  <verify>
    <automated>cd /Users/xingditan/Code/betterr_me && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - exercise-card.tsx imports Link from "next/link"
    - exercise-card.tsx contains href string with "/workouts/exercises/"
    - exercise-card.tsx has hover:border-primary/50 on Card
    - workout-exercise-card.tsx imports Link from "next/link"
    - workout-exercise-card.tsx contains href with "/workouts/exercises/"
    - workout-exercise-card.tsx has target="_blank" on the exercise name link
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Both exercise card components link to /workouts/exercises/[id], exercise library card is fully clickable with hover state, workout logger name opens in new tab</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. exercise-card.tsx wraps card in Link to detail page
3. workout-exercise-card.tsx has exercise name as Link
4. Dropdown actions on custom exercise cards still functional (preventDefault on trigger)
</verification>

<success_criteria>
- Clicking exercise card in library navigates to /workouts/exercises/[id]
- Clicking exercise name in workout logger opens detail page in new tab
- Custom exercise dropdown (edit/delete) still works without triggering navigation
</success_criteria>

<output>
After completion, create `.planning/phases/29-exercise-detail-page/29-02-SUMMARY.md`
</output>
