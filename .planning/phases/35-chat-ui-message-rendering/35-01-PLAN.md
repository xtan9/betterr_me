---
phase: 35-chat-ui-message-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/chat/markdown-renderer.tsx
  - components/chat/message-bubble.tsx
  - components/chat/chat-empty-state.tsx
  - components/chat/chat-input.tsx
  - components/chat/message-list.tsx
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
  - tests/components/chat/markdown-renderer.test.tsx
  - tests/components/chat/message-bubble.test.tsx
  - tests/components/chat/chat-empty-state.test.tsx
  - tests/components/chat/chat-input.test.tsx
autonomous: true
requirements:
  - CHAT-02
  - INTG-03
  - INTG-04

must_haves:
  truths:
    - "Assistant messages render bold, italic, lists, code blocks, blockquotes, tables, and links as formatted HTML"
    - "User messages render as plain pre-wrapped text (no markdown processing)"
    - "User messages are right-aligned with primary background; assistant messages are left-aligned with muted background"
    - "Enter sends the message, Shift+Enter inserts a newline, Escape stops streaming"
    - "Send button is disabled when input is empty or when streaming"
    - "Textarea auto-resizes up to ~150px then scrolls internally"
    - "Empty state shows centered greeting before first message"
    - "All colors use semantic design tokens (bg-background, bg-muted, bg-primary, text-foreground, etc.)"
    - "All user-visible strings use useTranslations() from next-intl with keys in all three locale files"
  artifacts:
    - path: "components/chat/markdown-renderer.tsx"
      provides: "Markdown rendering with Tailwind design tokens"
      exports: ["MarkdownRenderer"]
    - path: "components/chat/message-bubble.tsx"
      provides: "Single message display with role-based styling"
      exports: ["MessageBubble"]
    - path: "components/chat/chat-empty-state.tsx"
      provides: "Initial greeting state with i18n"
      exports: ["ChatEmptyState"]
    - path: "components/chat/chat-input.tsx"
      provides: "Auto-resizing textarea with keyboard shortcuts and i18n"
      exports: ["ChatInput"]
    - path: "components/chat/message-list.tsx"
      provides: "Scrollable message container with auto-scroll"
      exports: ["MessageList"]
    - path: "i18n/messages/en.json"
      provides: "English chat translation keys"
      contains: "chat"
    - path: "i18n/messages/zh.json"
      provides: "Chinese (Simplified) chat translation keys"
      contains: "chat"
    - path: "i18n/messages/zh-TW.json"
      provides: "Chinese (Traditional) chat translation keys"
      contains: "chat"
  key_links:
    - from: "components/chat/message-bubble.tsx"
      to: "components/chat/markdown-renderer.tsx"
      via: "import MarkdownRenderer for assistant messages"
      pattern: "import.*MarkdownRenderer.*from.*markdown-renderer"
    - from: "components/chat/message-list.tsx"
      to: "components/chat/message-bubble.tsx"
      via: "maps messages to MessageBubble components"
      pattern: "MessageBubble"
    - from: "components/chat/chat-empty-state.tsx"
      to: "i18n/messages/en.json"
      via: "useTranslations('chat') for greeting text"
      pattern: "useTranslations.*chat"
    - from: "components/chat/chat-input.tsx"
      to: "i18n/messages/en.json"
      via: "useTranslations('chat') for placeholder text"
      pattern: "useTranslations.*chat"
---

<objective>
Create all leaf chat UI components with tests: MarkdownRenderer (CHAT-02), MessageBubble (INTG-03), ChatInput (INTG-04), ChatEmptyState, and MessageList. These are standalone components that Plan 02 will wire together via useChat. All user-visible strings use i18n via useTranslations().

Purpose: Build testable, design-token-aware, i18n-ready UI primitives that Phase 35's chat page depends on.
Output: 5 component files in `components/chat/`, 4 test files in `tests/components/chat/`, and chat translation keys in all 3 locale files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-chat-ui-message-rendering/35-CONTEXT.md
@.planning/phases/35-chat-ui-message-rendering/35-RESEARCH.md

<interfaces>
<!-- AI SDK types the executor needs (from node_modules/ai/dist/index.d.ts) -->

```typescript
// From 'ai' package
interface UIMessage {
  id: string;
  role: 'user' | 'assistant' | 'system';
  parts: UIPart[];
  createdAt?: Date;
}

type UIPart = TextUIPart | ToolInvocationUIPart | ToolResultUIPart | SourceUIPart | FileUIPart | StepStartUIPart | ReasoningUIPart;

interface TextUIPart {
  type: 'text';
  text: string;
  state?: 'streaming' | 'done';
}

// From '@ai-sdk/react'
type ChatStatus = 'submitted' | 'streaming' | 'ready' | 'error';
```

```typescript
// From lib/utils.ts
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MarkdownRenderer, MessageBubble, ChatEmptyState, and their tests (with i18n)</name>
  <files>
    components/chat/markdown-renderer.tsx,
    components/chat/message-bubble.tsx,
    components/chat/chat-empty-state.tsx,
    i18n/messages/en.json,
    i18n/messages/zh.json,
    i18n/messages/zh-TW.json,
    tests/components/chat/markdown-renderer.test.tsx,
    tests/components/chat/message-bubble.test.tsx,
    tests/components/chat/chat-empty-state.test.tsx
  </files>
  <read_first>
    app/api/chat/route.ts,
    lib/utils.ts,
    components/ui/button.tsx,
    app/globals.css,
    tests/components/dashboard/dashboard-content.test.tsx,
    i18n/messages/en.json,
    i18n/messages/zh.json,
    i18n/messages/zh-TW.json
  </read_first>
  <behavior>
    MarkdownRenderer tests (tests/components/chat/markdown-renderer.test.tsx):
    - Test 1: Renders **bold** text as <strong> element
    - Test 2: Renders bullet list (- item) as <ul><li> elements
    - Test 3: Renders numbered list (1. item) as <ol><li> elements
    - Test 4: Renders fenced code block (```js) with monospace font class "font-mono"
    - Test 5: Renders inline `code` with bg-muted class
    - Test 6: Renders blockquote with border-l-2 class
    - Test 7: Renders links with target="_blank" and rel="noopener noreferrer"
    - Test 8: Renders GFM tables with <table> element

    MessageBubble tests (tests/components/chat/message-bubble.test.tsx):
    - Test 1: User message has class "ml-auto" (right-aligned) and "bg-primary" class
    - Test 2: Assistant message has class "mr-auto" (left-aligned) and "bg-muted" class
    - Test 3: Assistant message renders markdown via MarkdownRenderer (contains formatted HTML)
    - Test 4: User message renders plain text with "whitespace-pre-wrap" class (no markdown)
    - Test 5: Streaming assistant message shows blinking cursor element (animate-blink-cursor or animate-pulse class)
    - Test 6: Completed assistant message does NOT show blinking cursor

    ChatEmptyState tests (tests/components/chat/chat-empty-state.test.tsx):
    - Test 1: Renders translated greeting text (via useTranslations mock returning key)
    - Test 2: Has centered layout classes
  </behavior>
  <action>
    **i18n: Add chat translation keys to ALL THREE locale files** (per CLAUDE.md i18n rule):

    Add a `"chat"` top-level key to each locale file:

    `i18n/messages/en.json` — add:
    ```json
    "chat": {
      "emptyState": {
        "greeting": "How can I help?"
      },
      "input": {
        "placeholder": "Message..."
      },
      "error": {
        "generic": "Something went wrong. Please try again.",
        "retry": "Retry"
      }
    }
    ```

    `i18n/messages/zh.json` — add:
    ```json
    "chat": {
      "emptyState": {
        "greeting": "有什么我可以帮忙的？"
      },
      "input": {
        "placeholder": "输入消息..."
      },
      "error": {
        "generic": "出了点问题，请重试。",
        "retry": "重试"
      }
    }
    ```

    `i18n/messages/zh-TW.json` — add:
    ```json
    "chat": {
      "emptyState": {
        "greeting": "有什麼我可以幫忙的？"
      },
      "input": {
        "placeholder": "輸入訊息..."
      },
      "error": {
        "generic": "出了點問題，請重試。",
        "retry": "重試"
      }
    }
    ```

    **MarkdownRenderer** (`components/chat/markdown-renderer.tsx`):
    - "use client" directive
    - Import ReactMarkdown from 'react-markdown', remarkGfm from 'remark-gfm', cn from '@/lib/utils'
    - Props: `{ content: string; className?: string }`
    - Export named `MarkdownRenderer`
    - Use the exact component overrides from RESEARCH.md Pattern "Complete MarkdownRenderer with Tailwind Design Tokens" (per D-04, D-05, D-06):
      - `p`: `className="mb-2 last:mb-0 leading-relaxed"`
      - `strong`: `className="font-semibold"`
      - `ul`: `className="list-disc pl-4 mb-2 space-y-1"`
      - `ol`: `className="list-decimal pl-4 mb-2 space-y-1"`
      - `li`: `className="leading-relaxed"`
      - `code`: Check `className?.startsWith('language-')` for fenced blocks — fenced: `<pre className="rounded-md bg-muted/50 p-3 my-2 overflow-x-auto text-sm"><code className="font-mono">` — inline: `<code className="rounded bg-muted/50 px-1.5 py-0.5 text-sm font-mono">`
      - `pre`: render `<>{children}</>` to prevent double wrapping
      - `blockquote`: `className="border-l-2 border-muted-foreground/30 pl-3 my-2 italic"`
      - `a`: `target="_blank" rel="noopener noreferrer" className="text-primary underline hover:no-underline"`
      - `table`: wrapped in `<div className="overflow-x-auto my-2">`, table has `className="min-w-full border-collapse text-sm"`
      - `th`: `className="border-b border-border px-2 py-1 text-left font-semibold"`
      - `td`: `className="border-b border-border px-2 py-1"`
    - Wrapper div has `className={cn('prose-sm break-words', className)}`

    **MessageBubble** (`components/chat/message-bubble.tsx`):
    - "use client" directive
    - Import UIMessage from 'ai', MarkdownRenderer from './markdown-renderer', cn from '@/lib/utils'
    - Props: `{ message: UIMessage }`
    - Export named `MessageBubble`
    - Per D-01: user messages `className="ml-auto bg-primary text-primary-foreground"`, assistant messages `className="mr-auto bg-muted text-foreground"`
    - Per D-02: No avatars or icons
    - Max width: `max-w-[80%]`, border-radius: `rounded-2xl`, padding: `px-4 py-3`
    - Iterate `message.parts`: for `part.type === 'text'`:
      - If `role === 'assistant'`: render `<MarkdownRenderer content={part.text} />`
      - If `role === 'user'`: render `<p className="whitespace-pre-wrap">{part.text}</p>`
    - Per D-07: After parts, if `role === 'assistant'` AND any part has `part.type === 'text' && part.state === 'streaming'`, render `<span className="inline-block w-2 h-4 ml-0.5 bg-foreground animate-blink-cursor" />`
    - Per D-09: Partial responses remain visible (no conditional hiding)

    **ChatEmptyState** (`components/chat/chat-empty-state.tsx`):
    - "use client" directive
    - Import `useTranslations` from 'next-intl'
    - `const t = useTranslations('chat');`
    - Per D-13: Centered greeting using `t('emptyState.greeting')` (resolves to "How can I help?" in English, translated in zh/zh-TW)
    - Per D-14: This component is conditionally rendered by parent (ChatContent) only when messages array is empty
    - Layout: `flex items-center justify-center h-full`, text `text-2xl font-medium text-muted-foreground`
    - Export named `ChatEmptyState`

    **Tests**: Follow project mocking pattern with `vi.hoisted` + `vi.mock`. Mock `react-markdown` is NOT needed — render real markdown. For MessageBubble tests, mock `@/components/chat/markdown-renderer` to a simple div that renders content as text (to isolate bubble logic). For MarkdownRenderer tests, render with real react-markdown. For ChatEmptyState tests, mock `next-intl` with `vi.mock("next-intl", () => ({ useTranslations: () => (key: string) => key }))` — the test asserts the key "emptyState.greeting" is rendered.

    **Blinking cursor CSS**: Add to `app/globals.css` inside `@layer components`:
    ```css
    @keyframes blink-cursor {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }
    .animate-blink-cursor {
      animation: blink-cursor 1s step-end infinite;
    }
    ```
  </action>
  <verify>
    <automated>pnpm test:run -- tests/components/chat/markdown-renderer.test.tsx tests/components/chat/message-bubble.test.tsx tests/components/chat/chat-empty-state.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - i18n/messages/en.json contains `"chat"` key with `"emptyState"`, `"input"`, and `"error"` sub-keys
    - i18n/messages/zh.json contains `"chat"` key with Chinese (Simplified) translations
    - i18n/messages/zh-TW.json contains `"chat"` key with Chinese (Traditional) translations
    - components/chat/markdown-renderer.tsx contains `export function MarkdownRenderer`
    - components/chat/markdown-renderer.tsx contains `remarkPlugins={[remarkGfm]}`
    - components/chat/markdown-renderer.tsx contains `font-mono` (for code blocks)
    - components/chat/markdown-renderer.tsx contains `target="_blank"` (for links)
    - components/chat/message-bubble.tsx contains `export function MessageBubble`
    - components/chat/message-bubble.tsx contains `ml-auto` and `bg-primary` (user alignment per D-01)
    - components/chat/message-bubble.tsx contains `mr-auto` and `bg-muted` (assistant alignment per D-01)
    - components/chat/message-bubble.tsx contains `whitespace-pre-wrap` (user plain text)
    - components/chat/message-bubble.tsx contains `animate-blink-cursor` (streaming cursor per D-07)
    - components/chat/message-bubble.tsx contains `part.state === 'streaming'` or equivalent check
    - components/chat/chat-empty-state.tsx contains `export function ChatEmptyState`
    - components/chat/chat-empty-state.tsx contains `useTranslations` import from `next-intl`
    - components/chat/chat-empty-state.tsx contains `t('emptyState.greeting')` (i18n, not hardcoded)
    - app/globals.css contains `@keyframes blink-cursor`
    - tests/components/chat/markdown-renderer.test.tsx exits 0
    - tests/components/chat/message-bubble.test.tsx exits 0
    - tests/components/chat/chat-empty-state.test.tsx exits 0
  </acceptance_criteria>
  <done>
    MarkdownRenderer renders bold, lists, code blocks, blockquotes, tables, links with design tokens.
    MessageBubble shows user (right/primary) and assistant (left/muted) messages with blinking cursor during streaming.
    ChatEmptyState shows centered i18n greeting via useTranslations('chat').
    Chat translation keys added to all three locale files (en, zh, zh-TW).
    All 3 test files pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ChatInput and MessageList components with tests (with i18n)</name>
  <files>
    components/chat/chat-input.tsx,
    components/chat/message-list.tsx,
    tests/components/chat/chat-input.test.tsx
  </files>
  <read_first>
    components/ui/button.tsx,
    components/ui/textarea.tsx,
    components/chat/message-bubble.tsx,
    lib/utils.ts,
    tests/components/chat/markdown-renderer.test.tsx
  </read_first>
  <behavior>
    ChatInput tests (tests/components/chat/chat-input.test.tsx):
    - Test 1: Renders a textarea element and a send button
    - Test 2: Send button is disabled when input is empty (disabled attribute present)
    - Test 3: Send button is disabled when isStreaming=true
    - Test 4: Pressing Enter (without Shift) calls onSend with the input text
    - Test 5: Pressing Shift+Enter does NOT call onSend (allows newline)
    - Test 6: Pressing Escape when isStreaming=true calls onStop
    - Test 7: Pressing Escape when isStreaming=false does NOT call onStop
    - Test 8: Pressing Enter on empty input does NOT call onSend
    - Test 9: Stop button (Square icon) is visible when isStreaming=true instead of send button
    - Test 10: isComposing=true prevents Enter from sending (IME support for CJK)
    - Test 11: Textarea has translated placeholder text (via useTranslations mock returning key)
  </behavior>
  <action>
    **ChatInput** (`components/chat/chat-input.tsx`):
    - "use client" directive
    - Import: Button from '@/components/ui/button', cn from '@/lib/utils', useRef/useState/useCallback from 'react', Send/Square from 'lucide-react', `useTranslations` from 'next-intl'
    - `const t = useTranslations('chat');`
    - Props interface `ChatInputProps`:
      - `onSend: (text: string) => void` — called with trimmed text
      - `onStop: () => void` — called when Escape pressed during streaming
      - `isStreaming: boolean` — controls button state and Escape behavior
      - `disabled?: boolean` — optional external disable
    - Internal state: `const [input, setInput] = useState('')`
    - `textareaRef = useRef<HTMLTextAreaElement>(null)`
    - Per D-10: `adjustHeight()` function — sets `textarea.style.height = 'auto'` then `Math.min(textarea.scrollHeight, 150) + 'px'`. Called in `useEffect` on input change.
    - Per D-12: `handleKeyDown` function:
      - `if (e.key === 'Enter' && !e.shiftKey && !e.nativeEvent.isComposing)` — `e.preventDefault()`, call `handleSend()` if `input.trim() && !isStreaming`
      - `if (e.key === 'Escape' && isStreaming)` — `e.preventDefault()`, call `onStop()`
    - `handleSend()`: call `onSend(input.trim())`, `setInput('')`, reset textarea height
    - Per D-11: Layout: `div.flex items-end gap-2 p-4 border-t border-border bg-background`
      - Textarea: `flex-1 resize-none min-h-[40px] max-h-[150px] overflow-y-auto rounded-xl border border-input bg-background px-4 py-2.5 text-sm`, placeholder: `t('input.placeholder')` (resolves to "Message..." in English, translated in zh/zh-TW)
      - When isStreaming: show stop button (Square icon, variant="ghost", size="icon", onClick=onStop) per D-08
      - When not streaming: show send button (Send icon, variant="default", size="icon", onClick=handleSend, disabled when `!input.trim()` per D-11)
    - Export named `ChatInput`

    **MessageList** (`components/chat/message-list.tsx`):
    - "use client" directive
    - Import: UIMessage from 'ai', MessageBubble from './message-bubble', useRef/useState/useEffect from 'react'
    - Props: `{ messages: UIMessage[] }`
    - Per D-03: Auto-scroll behavior:
      - `scrollRef = useRef<HTMLDivElement>(null)`
      - `autoScroll` state, default true
      - `useEffect` on `[messages]`: if `autoScroll`, scroll to bottom (`scrollRef.current.scrollTop = scrollRef.current.scrollHeight`)
      - `onScroll` handler: disable auto-scroll if user scrolled up (`scrollHeight - scrollTop - clientHeight > 50`)
    - Layout: `ref={scrollRef} className="flex-1 overflow-y-auto p-4 space-y-4"`, `onScroll={handleScroll}`
    - Map messages to `<MessageBubble key={message.id} message={message} />`
    - Export named `MessageList`

    No separate test file for MessageList — it's a thin wrapper. It will be tested via the ChatContent integration test in Plan 02.

    **Tests**: Mock `next-intl` with `vi.mock("next-intl", () => ({ useTranslations: () => (key: string) => key }))` so the placeholder assertion checks for the key "input.placeholder".
  </action>
  <verify>
    <automated>pnpm test:run -- tests/components/chat/chat-input.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - components/chat/chat-input.tsx contains `export function ChatInput`
    - components/chat/chat-input.tsx contains `useTranslations` import from `next-intl`
    - components/chat/chat-input.tsx contains `t('input.placeholder')` (i18n, not hardcoded "Message...")
    - components/chat/chat-input.tsx contains `e.key === 'Enter'` and `!e.shiftKey`
    - components/chat/chat-input.tsx contains `e.key === 'Escape'`
    - components/chat/chat-input.tsx contains `isComposing` (IME guard)
    - components/chat/chat-input.tsx contains `Math.min(textarea.scrollHeight, 150)` or equivalent max-height logic
    - components/chat/chat-input.tsx contains `onSend` and `onStop` in props
    - components/chat/chat-input.tsx contains import from `lucide-react` (Send and Square icons)
    - components/chat/message-list.tsx contains `export function MessageList`
    - components/chat/message-list.tsx contains `scrollHeight - scrollTop - clientHeight` (auto-scroll detection)
    - components/chat/message-list.tsx contains `MessageBubble` import
    - tests/components/chat/chat-input.test.tsx exits 0
  </acceptance_criteria>
  <done>
    ChatInput renders textarea with i18n placeholder via useTranslations('chat'), send/stop button toggle, handles Enter/Shift+Enter/Escape correctly, auto-resizes to 150px max.
    MessageList renders message bubbles in scrollable container with auto-scroll that respects user scroll position.
    ChatInput test file passes.
  </done>
</task>

</tasks>

<verification>
All leaf components created and tested:
- `pnpm test:run -- tests/components/chat/ -x` — all chat component tests pass
- `pnpm lint` — no lint errors in new files
- All three locale files (en.json, zh.json, zh-TW.json) contain "chat" key with translation strings
</verification>

<success_criteria>
- 5 component files exist in components/chat/
- 4 test files exist and pass in tests/components/chat/
- Chat translation keys in all 3 locale files (en, zh, zh-TW) per CLAUDE.md i18n rule
- MarkdownRenderer renders GFM with design tokens (CHAT-02)
- MessageBubble has role-based alignment with dark-mode-compatible tokens (INTG-03)
- ChatInput handles Enter/Shift+Enter/Escape keyboard shortcuts (INTG-04)
- ChatEmptyState uses useTranslations('chat') for greeting text
- ChatInput uses useTranslations('chat') for placeholder text
- Blinking cursor CSS animation added to globals.css
</success_criteria>

<output>
After completion, create `.planning/phases/35-chat-ui-message-rendering/35-01-SUMMARY.md`
</output>
