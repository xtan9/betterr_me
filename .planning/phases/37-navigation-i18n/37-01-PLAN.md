---
phase: 37-navigation-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/layouts/app-sidebar.tsx
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
  - tests/components/layouts/app-sidebar.test.tsx
autonomous: true
requirements:
  - INTG-01
  - INTG-02

must_haves:
  truths:
    - "Chat link appears in the app sidebar navigation between Calendar and the bottom"
    - "Clicking the Chat nav link navigates to /chat"
    - "Chat nav link shows active state when on /chat or /chat/* routes"
    - "common.nav.chat key exists in all 3 locale files with correct translations"
    - "All chat.* i18n keys from Phases 35-36 are present in all 3 locales"
  artifacts:
    - path: "components/layouts/app-sidebar.tsx"
      provides: "Chat nav item in mainNavItems array"
      contains: "MessageSquare"
    - path: "i18n/messages/en.json"
      provides: "English nav.chat key"
      contains: "\"chat\": \"Chat\""
    - path: "i18n/messages/zh.json"
      provides: "Simplified Chinese nav.chat key"
      contains: "\"chat\":"
    - path: "i18n/messages/zh-TW.json"
      provides: "Traditional Chinese nav.chat key"
      contains: "\"chat\":"
  key_links:
    - from: "components/layouts/app-sidebar.tsx"
      to: "i18n/messages/en.json"
      via: "labelKey: chat -> common.nav.chat"
      pattern: "labelKey.*chat"
---

<objective>
Add a Chat navigation link to the app sidebar and verify all chat i18n strings are complete across all three locales.

Purpose: Make the chat feature discoverable from the main navigation and ensure full i18n coverage for v7.0 milestone completion.
Output: Updated sidebar with Chat nav item, complete i18n locale files, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-navigation-i18n/37-CONTEXT.md

<interfaces>
<!-- Existing nav item pattern from components/layouts/app-sidebar.tsx -->
```typescript
const mainNavItems = [
  {
    href: "/dashboard",
    icon: Home,
    labelKey: "dashboard",
    match: (p: string) => p === "/dashboard",
  },
  // ... other items ...
  {
    href: "/calendar",
    icon: CalendarDays,
    labelKey: "calendar",
    match: (p: string) => p.startsWith("/calendar"),
  },
];
```

<!-- i18n key pattern from i18n/messages/en.json common.nav section -->
```json
{
  "common": {
    "nav": {
      "dashboard": "Dashboard",
      "habits": "Habits",
      "tasks": "Tasks",
      "journal": "Journal",
      "workouts": "Workouts",
      "money": "Money",
      "calendar": "Calendar"
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Chat nav item to sidebar and complete i18n</name>
  <read_first>
    - components/layouts/app-sidebar.tsx (full file — see mainNavItems array and imports)
    - i18n/messages/en.json (common.nav section)
    - i18n/messages/zh.json (common.nav section)
    - i18n/messages/zh-TW.json (common.nav section)
    - tests/components/layouts/app-sidebar.test.tsx (existing test patterns)
  </read_first>
  <files>
    components/layouts/app-sidebar.tsx,
    i18n/messages/en.json,
    i18n/messages/zh.json,
    i18n/messages/zh-TW.json,
    tests/components/layouts/app-sidebar.test.tsx
  </files>
  <behavior>
    - Test: Chat nav item renders in the sidebar (screen.getByText("chat") since translations are mocked as key pass-through)
    - Test: Chat nav link has href="/chat"
    - Test: Chat nav link shows active state when pathname is /chat
    - Test: Chat nav link shows active state when pathname is /chat/some-conversation-id
  </behavior>
  <action>
    1. In `components/layouts/app-sidebar.tsx`:
       - Add `MessageSquare` to the lucide-react import (per D-02)
       - Add chat nav item to `mainNavItems` array AFTER the Calendar entry (per D-01, D-03):
         `{ href: "/chat", icon: MessageSquare, labelKey: "chat", match: (p: string) => p.startsWith("/chat") }`
       - No badge count needed (per D-04)

    2. In `i18n/messages/en.json`:
       - Add `"chat": "Chat"` to `common.nav` section (per D-05)

    3. In `i18n/messages/zh.json`:
       - Add `"chat": "聊天"` to `common.nav` section (per D-06)

    4. In `i18n/messages/zh-TW.json`:
       - Add `"chat": "聊天"` to `common.nav` section (per D-07)

    5. Verify all `chat.*` i18n keys from Phases 35-36 are present in all 3 locale files (per D-08). Grep for all keys used by `useTranslations("chat")` in chat components and cross-reference with locale files. If any are missing, add them.

    6. In `tests/components/layouts/app-sidebar.test.tsx`:
       - Add test that chat nav item renders with correct href
       - Add test that chat nav is active on /chat path
       - Add test that chat nav is active on /chat/[id] subpath
  </action>
  <verify>
    <automated>cd /home/xingdi/code/betterr_me-llm-integration && pnpm test:run -- tests/components/layouts/app-sidebar.test.tsx && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - MessageSquare icon imported and used in mainNavItems array
    - Chat nav item appears after Calendar in mainNavItems
    - common.nav.chat key exists in en.json ("Chat"), zh.json ("聊天"), zh-TW.json ("聊天")
    - All chat.* keys from Phases 35-36 confirmed present in all 3 locales
    - New tests pass for chat nav rendering and active state
    - Lint passes with no new errors
  </acceptance_criteria>
  <done>Chat appears in sidebar navigation, all i18n strings complete across 3 locales, tests and lint pass</done>
</task>

</tasks>

<verification>
1. `pnpm test:run -- tests/components/layouts/app-sidebar.test.tsx` — all tests pass including new chat nav tests
2. `pnpm lint` — no lint errors
3. `pnpm build` — production build succeeds
4. Grep verification: `grep -c '"chat"' i18n/messages/en.json i18n/messages/zh.json i18n/messages/zh-TW.json` — all show the nav key
5. Grep verification: `grep 'MessageSquare' components/layouts/app-sidebar.tsx` — import and usage present
</verification>

<success_criteria>
- Chat nav link visible in app sidebar after Calendar
- Clicking Chat navigates to /chat
- Active state highlights correctly on /chat and /chat/* routes
- All 3 locale files have common.nav.chat with correct translations
- All chat.* i18n keys complete across all locales
- All tests pass, lint clean, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/37-navigation-i18n/37-01-SUMMARY.md`
</output>
