---
phase: 31-calendar-ui-month-view-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/calendar/layout.tsx
  - app/calendar/page.tsx
  - app/globals.css
  - components/layouts/app-sidebar.tsx
  - i18n/messages/en.json
  - i18n/messages/zh.json
  - i18n/messages/zh-TW.json
autonomous: true
requirements: [VIEW-10]

must_haves:
  truths:
    - "Calendar route exists at /calendar and renders within sidebar layout"
    - "Calendar link appears in the app sidebar navigation with CalendarDays icon"
    - "Calendar domain CSS variables exist for events/tasks/habits/bills/workouts in light and dark mode"
    - "Calendar i18n namespace exists in all 3 locale files with view labels, layer names, and navigation strings"
  artifacts:
    - path: "app/calendar/layout.tsx"
      provides: "SidebarShell wrapper for calendar pages"
    - path: "app/calendar/page.tsx"
      provides: "Server component with auth check, redirects to CalendarPageContent"
    - path: "app/globals.css"
      provides: "Calendar domain color CSS variables (--calendar-event, --calendar-task, etc.)"
      contains: "--calendar-event"
    - path: "components/layouts/app-sidebar.tsx"
      provides: "Calendar nav item in mainNavItems array"
      contains: "calendar"
    - path: "i18n/messages/en.json"
      provides: "calendar namespace with all UI strings"
  key_links:
    - from: "app/calendar/page.tsx"
      to: "components/calendar/calendar-page-content.tsx"
      via: "import and render"
      pattern: "CalendarPageContent"
    - from: "components/layouts/app-sidebar.tsx"
      to: "/calendar"
      via: "mainNavItems href"
      pattern: 'href.*calendar'
---

<objective>
Set up the calendar route foundation: layout, page, sidebar nav link, design tokens, and i18n strings.

Purpose: Establish the scaffolding every subsequent plan needs — route, styling tokens, and translations.
Output: Working /calendar route with auth, sidebar nav entry, domain color tokens, and all i18n keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-calendar-ui-month-view-navigation/31-CONTEXT.md
@.planning/phases/31-calendar-ui-month-view-navigation/31-RESEARCH.md

<interfaces>
<!-- Existing patterns to follow -->

From app/habits/layout.tsx:
```tsx
import { SidebarShell } from "@/components/layouts/sidebar-shell";
export default async function HabitsLayout({ children }: { children: React.ReactNode }) {
  return <SidebarShell>{children}</SidebarShell>;
}
```

From app/habits/page.tsx:
```tsx
import { createClient } from "@/lib/supabase/server";
import { redirect } from "next/navigation";
// Server component with auth check, renders client component
```

From components/layouts/app-sidebar.tsx mainNavItems:
```tsx
const mainNavItems = [
  { href: "/dashboard", icon: Home, labelKey: "dashboard", match: (p: string) => p === "/dashboard" },
  { href: "/habits", icon: ClipboardList, labelKey: "habits", match: (p: string) => p.startsWith("/habits") },
  // ... other items
  { href: "/money", icon: Wallet, labelKey: "money", match: (p: string) => p.startsWith("/money") },
];
```

From lib/calendar/recurrence.ts:
```tsx
export type ExpandedCalendarEvent = CalendarEvent & { is_virtual: boolean };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar route, layout, sidebar nav link, and design tokens</name>
  <files>app/calendar/layout.tsx, app/calendar/page.tsx, components/layouts/app-sidebar.tsx, app/globals.css</files>
  <read_first>app/habits/layout.tsx, app/habits/page.tsx, components/layouts/app-sidebar.tsx, app/globals.css</read_first>
  <action>
1. Create `app/calendar/layout.tsx` — identical to habits layout: import SidebarShell, wrap children. (per D-02)

2. Create `app/calendar/page.tsx` — server component:
   - Auth check via `createClient()` + `supabase.auth.getUser()`
   - Redirect to `/auth/login` if not authenticated
   - Import and render `CalendarPageContent` from `@/components/calendar/calendar-page-content`
   - Do NOT fetch initial data server-side (calendar date range depends on URL params, per D-15)
   - The CalendarPageContent import will fail until Plan 02 creates it — that is fine, this plan sets up the route

3. Add Calendar to sidebar nav in `components/layouts/app-sidebar.tsx`:
   - Import `CalendarDays` from `lucide-react` (per D-03)
   - Add entry to `mainNavItems` array AFTER the "money" entry:
     ```tsx
     { href: "/calendar", icon: CalendarDays, labelKey: "calendar", match: (p: string) => p.startsWith("/calendar") },
     ```

4. Add calendar domain color CSS variables to `app/globals.css`:
   In `:root` (light mode), add a new section after the stat icon colors:
   ```css
   /* === Calendar domain colors === */
   --calendar-event: 157 63% 45%;
   --calendar-event-muted: 157 63% 93%;
   --calendar-task: 215 75% 55%;
   --calendar-task-muted: 215 75% 93%;
   --calendar-habit: 40 85% 55%;
   --calendar-habit-muted: 40 85% 93%;
   --calendar-bill: 0 72% 55%;
   --calendar-bill-muted: 0 72% 93%;
   --calendar-workout: 270 60% 55%;
   --calendar-workout-muted: 270 60% 93%;
   ```
   In `.dark`, add matching dark mode values:
   ```css
   /* === Calendar domain colors === */
   --calendar-event: 160 45% 55%;
   --calendar-event-muted: 160 30% 18%;
   --calendar-task: 215 60% 60%;
   --calendar-task-muted: 215 35% 18%;
   --calendar-habit: 40 70% 60%;
   --calendar-habit-muted: 40 40% 18%;
   --calendar-bill: 0 60% 55%;
   --calendar-bill-muted: 0 35% 18%;
   --calendar-workout: 270 50% 60%;
   --calendar-workout-muted: 270 30% 18%;
   ```
  </action>
  <verify>
    <automated>grep -q "CalendarDays" components/layouts/app-sidebar.tsx && grep -q "calendar-event" app/globals.css && test -f app/calendar/layout.tsx && test -f app/calendar/page.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- app/calendar/layout.tsx exists wrapping children in SidebarShell
- app/calendar/page.tsx exists with auth check and CalendarPageContent import
- CalendarDays icon imported and "calendar" nav item added to mainNavItems in app-sidebar.tsx
- 10 calendar domain CSS variables defined in both :root and .dark sections of globals.css
  </acceptance_criteria>
  <done>Calendar route exists with layout+page, sidebar shows Calendar link, and domain color tokens are in globals.css for both light and dark mode.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar i18n strings in all 3 locale files</name>
  <files>i18n/messages/en.json, i18n/messages/zh.json, i18n/messages/zh-TW.json</files>
  <read_first>i18n/messages/en.json, i18n/messages/zh.json, i18n/messages/zh-TW.json</read_first>
  <action>
Add a new top-level `"calendar"` namespace to all 3 locale files with these keys:

**English (en.json):**
```json
"calendar": {
  "title": "Calendar",
  "views": {
    "day": "Day",
    "week": "Week",
    "month": "Month"
  },
  "navigation": {
    "today": "Today",
    "previous": "Previous",
    "next": "Next"
  },
  "layers": {
    "events": "Events",
    "tasks": "Tasks",
    "habits": "Habits",
    "bills": "Bills",
    "workouts": "Workouts"
  },
  "overflow": {
    "more": "+{count} more"
  },
  "sidebar": {
    "newEvent": "+ New Event"
  },
  "comingSoon": "Coming soon",
  "noEvents": "No events"
}
```

**Simplified Chinese (zh.json):**
```json
"calendar": {
  "title": "日历",
  "views": {
    "day": "日",
    "week": "周",
    "month": "月"
  },
  "navigation": {
    "today": "今天",
    "previous": "上一个",
    "next": "下一个"
  },
  "layers": {
    "events": "事件",
    "tasks": "任务",
    "habits": "习惯",
    "bills": "账单",
    "workouts": "锻炼"
  },
  "overflow": {
    "more": "+{count} 更多"
  },
  "sidebar": {
    "newEvent": "+ 新事件"
  },
  "comingSoon": "即将推出",
  "noEvents": "暂无事件"
}
```

**Traditional Chinese (zh-TW.json):**
```json
"calendar": {
  "title": "日曆",
  "views": {
    "day": "日",
    "week": "週",
    "month": "月"
  },
  "navigation": {
    "today": "今天",
    "previous": "上一個",
    "next": "下一個"
  },
  "layers": {
    "events": "事件",
    "tasks": "任務",
    "habits": "習慣",
    "bills": "帳單",
    "workouts": "鍛鍊"
  },
  "overflow": {
    "more": "+{count} 更多"
  },
  "sidebar": {
    "newEvent": "+ 新事件"
  },
  "comingSoon": "即將推出",
  "noEvents": "暫無事件"
}
```

Add the `"calendar"` block as the last top-level key in each file (after "money" or whichever is last).
  </action>
  <verify>
    <automated>node -e "const en=require('./i18n/messages/en.json'); const zh=require('./i18n/messages/zh.json'); const tw=require('./i18n/messages/zh-TW.json'); if(en.calendar && zh.calendar && tw.calendar && en.calendar.views.month && zh.calendar.views.month && tw.calendar.views.month) console.log('PASS'); else console.log('FAIL');"</automated>
  </verify>
  <acceptance_criteria>
- "calendar" top-level key exists in en.json, zh.json, and zh-TW.json
- All 3 files have identical key structure: title, views (day/week/month), navigation (today/previous/next), layers (events/tasks/habits/bills/workouts), overflow.more, sidebar.newEvent, comingSoon, noEvents
- English, Simplified Chinese, and Traditional Chinese translations are correct
  </acceptance_criteria>
  <done>Calendar i18n namespace exists in all 3 locale files with all needed strings for Phase 31 UI.</done>
</task>

</tasks>

<verification>
1. `test -f app/calendar/layout.tsx` — layout exists
2. `test -f app/calendar/page.tsx` — page exists
3. `grep -q "CalendarDays" components/layouts/app-sidebar.tsx` — sidebar nav link added
4. `grep -q "calendar-event" app/globals.css` — domain tokens exist
5. `node -e "const en=require('./i18n/messages/en.json'); console.log(!!en.calendar)"` — i18n keys exist
</verification>

<success_criteria>
- /calendar route renders within sidebar layout with auth protection
- Calendar link visible in app sidebar with CalendarDays icon
- All 5 domain color pairs (event/task/habit/bill/workout) defined in light+dark CSS
- Calendar i18n strings present in en, zh, zh-TW with view labels, navigation, layers, overflow
</success_criteria>

<output>
After completion, create `.planning/phases/31-calendar-ui-month-view-navigation/31-01-SUMMARY.md`
</output>
